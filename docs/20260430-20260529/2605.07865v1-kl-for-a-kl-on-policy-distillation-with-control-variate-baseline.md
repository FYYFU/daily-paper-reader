---
title: "KL for a KL: On-Policy Distillation with Control Variate Baseline"
title_zh: KL之于KL：带控制变量基线的同策蒸馏
authors: "Minjae Oh, Sangjun Song, Gyubin Choi, Yunho Choi, Yohan Jo"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07865v1"
tags: ["query:opd"]
score: 9.0
evidence: 提出控制变量基线降低同策略蒸馏方差
tldr: 针对在线策略蒸馏(OPD)因单样本蒙特卡洛估计导致高梯度方差而不稳定的问题，本文提出vOPD方法，将OPD建模为策略梯度强化学习，并引入控制变量基线——即价值函数——来降低方差。该价值函数具有闭式解，即学生与教师模型间逐token的负反向KL散度，可直接从前向传播中获取，无需额外批评网络或推理。vOPD保留轻量级单样本估计器，通过减去解耦的基线保持梯度无偏并降低方差。实验表明，vOPD在数学和科学推理基准上稳定优于标准OPD，且性能媲美昂贵的全词汇基线，实现了通过原则性RL方差缩减对OPD的高效稳定化。
source: arxiv
selection_source: fresh_fetch
motivation: 在线策略蒸馏(OPD)在大型语言模型后训练中表现优异，但其单样本蒙特卡洛估计导致高梯度方差，训练不稳定且缺乏成熟配方。
method: 提出vOPD，将OPD转化为策略梯度强化学习，引入控制变量基线（价值函数），其闭式为逐token负反向KL散度，可从前向传播中无额外成本获得。
result: 在数学和科学推理基准上，vOPD一致优于标准OPD，性能匹配昂贵的全词汇基线，且通过top-k近似基线进一步降低成本。
conclusion: vOPD通过原则性的RL方差缩减方法，为在线策略蒸馏提供了高效且稳定的训练方案，无需额外模型开销。
---

## 摘要
同策蒸馏已成为大语言模型后训练的主导范式，尤其在推理领域。然而，由于其单样本蒙特卡洛估计的高梯度方差，同策蒸馏在实践中仍不稳定，且稳定训练的配方尚不成熟。我们提出vOPD（带控制变量基线的同策蒸馏），该方法将同策蒸馏视为策略梯度强化学习，并通过引入强化学习文献中的控制变量基线（典型为价值函数）来稳定训练。我们证明同策蒸馏的价值函数具有闭式解，即学生与教师之间每个token的负反向KL散度，可直接从已计算的前向传播中获得，无需额外的评论家网络或推理过程。现有稳定方法要么在整个词汇表上计算完整的token级反向KL，增加显著开销，要么将其限制在top-k支持集上，导致目标有偏。vOPD则保留了轻量级的单样本估计器，减去价值函数作为分离基线，保持梯度无偏的同时降低方差。此外，我们表明基线的top-k近似可进一步降低成本而不影响性能。在数学和科学推理基准上，vOPD始终优于原始同策蒸馏，并能匹配最昂贵的完整词汇表基线，通过原则性的强化学习方差缩减实现了同策蒸馏的高效稳定。

## 速览
**TLDR**：在策略蒸馏（OPD）中，单样本蒙特卡洛估计的高方差导致训练不稳定。本文提出vOPD，引入RL中的控制变量基线（值函数）来降低方差。该值函数具有闭式解：每token的负反向KL散度，可直接从已有前向计算获得，无需额外模型。vOPD保持单样本估计的轻量性，通过减去分离基线保持梯度无偏，同时显著降低方差。实验表明，vOPD在数学与科学推理基准上一致优于原始OPD，且性能媲美昂贵的全词汇基线，实现了高效稳定的策略蒸馏。 \
**Motivation**：OPD因单样本蒙特卡洛估计的高方差导致训练不稳定，现有降方差方法（如全词汇反向KL或top-k近似）要么开销大，要么引入偏差。 \
**Method**：vOPD将OPD转化为策略梯度RL，引入控制变量基线（值函数=负反向KL散度），基于已有前向计算直接获取，减去分离基线实现无偏降方差，并可进一步采用top-k近似降低成本。 \
**Result**：在数学与科学推理基准上，vOPD稳定提升性能，一致优于原始OPD，且与全词汇基线效果相当，而计算开销更低。 \
**Conclusion**：vOPD通过原则性的RL方差缩减技术，为OPD提供了高效稳定的训练方案，无需额外模型或牺牲梯度无偏性。

---

## Abstract
On-Policy Distillation (OPD) has emerged as a dominant post-training paradigm for large language models, especially for reasoning domains. However, OPD remains unstable in practice due to the high gradient variance of its single-sample Monte Carlo estimator, and recipes for stable training are still immature. We propose vOPD (On-Policy Distillation with a control variate baseline), which casts OPD as policy-gradient RL and stabilizes it by introducing a control variate baseline-canonically a value function -- from the RL literature. We show that the OPD value function admits a closed form as the per-token negative reverse KL divergence between the student and the teacher, available directly from the already-computed forward pass with no additional critic or inference. Existing stabilization methods either compute the full token-level reverse KL over the entire vocabulary, adding significant overhead, or restrict it to a top-k support, biasing the objective. vOPD instead preserves the lightweight single-sample estimator, subtracting the value function as a detached baseline to keep the gradient unbiased while reducing variance. Furthermore, we show that a top-k approximation of the baseline further lowers cost without compromising performance. Across mathematical and scientific reasoning benchmarks, vOPD consistently outperforms vanilla OPD and matches the most expensive full-vocabulary baseline, offering an efficient stabilization of On-Policy Distillation through principled RL variance reduction.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义
- **核心问题**：On-Policy Distillation（OPD）在大型语言模型后训练中表现优异，但其单样本蒙特卡洛估计导致梯度方差极高，训练不稳定，且缺乏通用的稳定训练配方。
- **整体含义**：现有稳定方法（全词汇反向KL或top-k近似）要么计算开销大，要么引入偏差。本文提出一种基于强化学习控制变量基线的无偏降方差方法，在保持OPD原有目标的同时，显著提升训练稳定性和性能。

## 2. 方法论
- **核心思想**：将OPD建模为策略梯度强化学习，引入控制变量基线（value function）来降低梯度方差，同时保持梯度的无偏性。
- **关键技术细节**：
    - OPD的每token奖励定义为：\( r_t(c_t, y_t) = \log \pi_T(y_t|c_t) - \log \pi_\theta(y_t|c_t) \)。
    - 对应的价值函数为：\( V^{\pi_\theta}(c_t) = \mathbb{E}_{y_t \sim \pi_\theta}[r_t(c_t, y_t)] = - D_{\text{KL}}(\pi_\theta(\cdot|c_t) \parallel \pi_T(\cdot|c_t)) \)，即每token的负反向KL散度，可直接从前向传播中获得，无需额外批评网络。
    - vOPD的梯度估计器：\( \nabla_\theta J_{\text{vOPD}}(\theta) = \mathbb{E}\left[ \sum_t (r_t + D_{\text{KL}}(\pi_\theta \parallel \pi_T)) \nabla_\theta \log \pi_\theta(y_t|c_t) \right] \)，其中KL项作为分离基线（detached），不传播梯度，仅用于降低方差。
    - 进一步提出vOPD top-k：在student的top-k支持集上近似计算KL基线，进一步提高计算效率，同时保持梯度无偏（与OPD top-k不同，后者改变目标有偏）。
- **公式流程**：用文字说明即可。

## 3. 实验设计
- **数据集/场景**：
    - 数学推理：DAPO-Math-17K（14K训练样本）训练，在MATH500、Minerva Math、AMC23、AIME24/25上评估。
    - 科学推理：SciKnowEval（化学子集，75/5/20划分）训练，评估自身测试集和GPQA-Diamond。
- **基准（benchmark）**：在数学与科学推理基准上对比pass@n和avg@n。
- **对比方法**：Base OPD、OPD full-V（全词汇反向KL）、OPD top-k（top-20）、vOPD top-k、vOPD full-V。
- **模型配置**：
    - 主要：Qwen3-1.7B蒸馏到Qwen3-1.7B-Base。
    - 扩展：Qwen3-4B到Qwen3-4B-Base、Qwen3-1.7B到Qwen3-0.6B-Base（尺寸不匹配）、Olmo-3-7B-Think到Olmo-3-7B-Base（模型族）。

## 4. 资源与算力
- 论文明确提到：所有实验在单张NVIDIA H200 NVL或A100 NVL GPU上进行，训练时长2小时（1.7B on A100）到6小时（7B on H200）。未提及具体使用的GPU数量（均为单卡），也未提及总计算量（如FLOPs）。

## 5. 实验数量与充分性
- **实验组数**：多个模型尺度（1.7B、4B、0.6B、7B）、两个学科（数学与科学）、多种对比方法（5种方法）、消融实验（top-k参数量 k=5,20,50,100）、梯度范数分析、奖励vs优势分布、壁钟时间对比。
- **充分性与公平性**：
    - 实验覆盖了不同模型族、不同师生尺度、不同领域，较为全面。
    - 对比了有偏/无偏、高开销/低开销的各种变体，消融了超参数k。
    - 但缺乏更大模型尺度（如70B）的验证，且仅使用LoRA微调，未验证全参数微调。
    - 未进行多次随机种子实验，统计显著性未报告。

## 6. 主要结论与发现
- vOPD（full-V和top-k）在所有设置下一致优于原始OPD，平均绝对准确率提升+3%以上，最高+6.2%（MATH500）。
- vOPD top-k与vOPD full-V性能几乎相同，但计算成本更低，优于OPD top-k（后者因有偏仅获微小提升）。
- vOPD的梯度范数比原始OPD低1-2个数量级，表明其有效压制了干扰性的大梯度。
- 优势分布对比奖励分布展示出右移且长尾压缩，说明vOPD选择性抑制了高不匹配token的梯度噪声。
- 壁钟时间：vOPD top-k与vOPD full-V仅略慢于原始OPD（因前向计算KL），但远快于OPD full-V和OPD top-k（后者因反向传播变慢）。

## 7. 优点
- **原则性强**：从RL控制变量基线理论出发，推导了OPD价值函数的闭式解，方法数学优雅。
- **无额外模型**：基线直接从已有前向计算获得，无需额外批评网络或推理。
- **无偏降方差**：tOP-k基线保持梯度无偏（与OPD top-k不同）。
- **计算高效**：单token反向传播不变，仅增加前向KL计算（可用top-k近似）。
- **泛化性**：跨模型尺度、模型族、任务领域均有效。
- **稳定性证据**：梯度范数、优势分布等分析支撑了方法有效性。

## 8. 不足与局限
- **规模局限**：实验限于7B及以下模型，未验证更大规模（如70B）上的效果。
- **资源报告**：未详细说明总计算量（FLOPs/GPU·小时），且仅使用单卡，可扩展性未充分验证。
- **统计显著性**：未报告多次运行的平均值和标准差，单次结果可能存在随机波动。
- **方法依赖**：需访问一个更强的教师模型，不能用于自蒸馏或教师不可得场景。
- **仅LoRA微调**：未测试全参数微调下的行为。
- **序列级KL**：本文只关注token级KL，序列级KL目标未探讨。

（完）
