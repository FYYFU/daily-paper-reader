---
title: "DiffusionOPD: A Unified Perspective of On-Policy Distillation in Diffusion Models"
title_zh: DiffusionOPD：扩散模型中在线策略蒸馏的统一视角
authors: "Quanhao Li, Junqiu Yu, Kaixun Jiang, Yujie Wei, Zhen Xing, Pandeng Li, Ruihang Chu, Shiwei Zhang, Yu Liu, Zuxuan Wu"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15055v1"
tags: ["query:opd"]
score: 8.0
evidence: 扩散模型中的在线策略蒸馏
tldr: 扩散模型的多任务优化面临交叉干扰和遗忘问题。DiffusionOPD通过先独立训练单任务教师再蒸馏至统一学生，解耦了探索与集成。理论推导了连续状态空间的在线策略蒸馏逐步骤KL目标，降低了方差。实验在多个基准上达到最佳性能，训练效率更高。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.15055v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1448, \"height\": 568, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.15055v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1432, \"height\": 742, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.15055v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1437, \"height\": 293, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.15055v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1450, \"height\": 347, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.15055v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1438, \"height\": 408, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.15055v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1437, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.15055v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1434, \"height\": 1796, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.15055v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1292, \"height\": 1993, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.15055v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1443, \"height\": 462, \"label\": \"Table\"}]"
motivation: 现有强化学习方法仅限单任务优化，多任务联合存在干扰和失衡，级联RL易遗忘，亟需高效多任务训练范式。
method: DiffusionOPD先分别训练任务特定教师，再沿学生自身轨迹蒸馏教师能力，并推导连续马尔可夫过程的逐步骤KL目标实现统一。
result: 在多个基准上超越多奖励RL和级联RL基线，取得最佳性能，训练效率显著提升。
conclusion: 为扩散模型多任务学习提供有效范式，理论推导与实验验证了其优越性和通用性。
---

## 摘要
强化学习已成为改进基于扩散的文本到图像模型的强大工具，但现有方法大多局限于单任务优化。将强化学习扩展到多任务具有挑战性：联合优化受制于跨任务干扰和不平衡，而级联强化学习既笨拙又容易导致灾难性遗忘。我们提出DiffusionOPD，一种基于在线策略蒸馏（OPD）的扩散模型多任务训练新范式。DiffusionOPD首先独立训练任务特定的教师模型，然后沿着学生自身的 rollout 轨迹将教师的能力蒸馏到一个统一的学生模型中。这解耦了单任务探索与多任务集成，避免了从头开始联合求解所有任务的优化负担。理论上，我们将OPD框架从离散令牌扩展到连续状态马尔可夫过程，推导出闭式每步KL目标，通过均值匹配统一了随机SDE和确定性ODE细化。我们正式且经验性地证明，与传统的PPO式策略梯度相比，该解析梯度具有更低的方差和更好的泛化性。大量实验表明，DiffusionOPD在训练效率和最终性能上持续超越了多奖励强化学习和级联强化学习基线，并在所有评估基准上取得了最先进的结果。

## 速览
**TLDR**：扩散模型多任务强化学习面临干扰与遗忘难题。DiffusionOPD提出通过独立训练单任务教师、再沿学生轨迹在线蒸馏的统一范式，将单任务探索与多任务集成解耦。理论推导了连续状态Markov过程的闭式KL散度目标，等价于均值匹配且方差低于PPO。实验表明该方法在训练效率和最终性能上均超越多奖励RL和级联RL基线。 \
**Motivation**：现有扩散模型强化学习方法仅限于单任务优化，多任务扩展存在跨任务干扰和级联遗忘问题。 \
**Method**：先独立训练多个任务教师，再沿学生在线轨迹，通过推导的闭式KL散度（均值匹配）蒸馏统一学生。 \
**Result**：在多个基准任务上达到最优，训练效率和最终性能一致超越多奖励RL和级联RL基线。 \
**Conclusion**：DiffusionOPD为扩散模型多任务微调提供了解耦-蒸馏的高效统一框架。

---

## Abstract
Reinforcement learning has emerged as a powerful tool for improving diffusion-based text-to-image models, but existing methods are largely limited to single-task optimization. Extending RL to multiple tasks is challenging: joint optimization suffers from cross-task interference and imbalance, while cascade RL is cumbersome and prone to catastrophic forgetting. We propose DiffusionOPD, a new multi-task training paradigm for diffusion models based on Online Policy Distillation (OPD). DiffusionOPD first trains task-specific teachers independently, then distills their capabilities into a unified student along the student own rollout trajectories. This decouples single-task exploration from multi-task integration and avoids the optimization burden of solving all tasks jointly from scratch. Theoretically, we lift the OPD framework from discrete tokens to continuous-state Markov processes, deriving a closed-form per-step KL objective that unifies both stochastic SDE and deterministic ODE refinement via mean-matching. We formally and empirically demonstrate that this analytic gradient provides lower variance and better generality compared to conventional PPO-style policy gradients. Extensive experiments show that DiffusionOPD consistently surpasses both multi-reward RL and cascade RL baselines in training efficiency and final performance, while achieving state-of-the-art results on all evaluated benchmarks.

---

## 论文详细总结（自动生成）

# 论文总结：DiffusionOPD: A Unified Perspective of On-Policy Distillation in Diffusion Models

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前强化学习（RL）在扩散模型微调领域虽然有效，但大多局限于**单任务优化**（如仅优化美学或OCR）。实际应用中用户希望单个模型同时满足多个目标（如图像美观、文字渲染准确、提示遵循度高）。将RL扩展到多任务面临两大困难：
  - **联合优化**：跨任务目标冲突与难度不平衡导致干扰；
  - **级联RL**：分阶段顺序训练，过程繁琐且容易灾难性遗忘。
- **研究动机**：希望设计一种解耦的多任务训练范式，先让各任务独立探索，再高效集成能力。
- **整体含义**：提出**DiffusionOPD**，将在线策略蒸馏（On-Policy Distillation, OPD）从LLM引入扩散模型，实现多任务能力的高效蒸馏，避免上述问题，提升训练效率和最终性能。

## 2. 论文提出的方法论

### 核心思想
- **两阶段解耦**：
  1. **第一阶段（单任务教师训练）**：对每个任务 \( m \)，使用现有扩散RL算法（GRPO-Guard或DiffusionNFT）独立训练一个教师模型 \( v_{\phi_m} \)。
  2. **第二阶段（多任务在线蒸馏）**：将多个教师的能力蒸馏到一个统一的学生模型 \( v_\theta \) 中。蒸馏过程中，学生沿**自身的rollout轨迹**生成图像，教师提供每步密集监督。

### 关键技术细节
- **从LLM到扩散模型的OPD推广**：
  - 将LLM中原有的离散token自回归链推广为**连续状态马尔可夫链**（扩散去噪过程）。
  - 学生和教师的单步转移核均为高斯分布（SDE离散化所得），且协方差相同（仅依赖于调度器与噪声水平）。
  - 因此逐步骤的**反向KL散度**具有闭式解：
    \[
    \text{KL}\big(\mathcal{N}(\mu_S,\bar\sigma^2 I) \|\mathcal{N}(\mu_T,\bar\sigma^2 I)\big) = \frac{\|\mu_S - \mu_T\|^2}{2\bar\sigma^2}.
    \]
    即等价于**均值匹配**。
- **统一SDE与ODE**：
  - 对于随机SDE采样器，使用上述KL目标。
  - 对于确定性ODE采样器，KL简化为直接的L2均值匹配损失。
- **梯度优势**：
  - 直接优化闭式KL目标，梯度不引入额外噪声（无PPO中的得分函数项），**方差更低**。
  - 理论证明PPO风格目标在期望上与闭式KL等价，但多了高斯噪声项，导致方差增大。

### 训练流程（算法）
```
输入：任务集合、各任务提示数据集、预训练扩散策略vref、去噪调度。
阶段1：对每个任务m，独立训练教师v_{\phi_m}。
阶段2：初始化学生v_θ ← vref，循环（每个训练轮次）：
   初始化总损失L_total ← 0；
   对每个任务m：
      采样提示c；
      用当前学生进行on-policy rollout得到轨迹；
      用教师计算OPD损失（式11或12，优先式12）；
      L_total += L_m；
   累积M个任务后，一次反向传播更新学生参数。
```
- 使用**轮询累积梯度**（gradient accumulation factor = 任务数M），每轮更新一次，减少更新方差。

## 3. 实验设计

### 数据集/场景
- **基础模型**：SD3.5-Medium（512×512分辨率）。
- **奖励信号**：
  - 规则型：GenEval（组合生成）、OCR（视觉文本渲染）。
  - 模型型：PickScore、CLIPScore、HPSv2.1、Aesthetics、ImageReward、UnifiedReward。
- **训练数据**：FlowGRPO splits for GenEval & OCR；Pick-a-Pic for model-based rewards；DrawBench for evaluation。

### Benchmark
- **评估基准**：GenEval、OCR（特定指标）、各模型型奖励的得分（PickScore、CLIPScore、HPSv2.1、Aesthetics、ImageReward、UnifiedReward）。另附Average（min-max归一化均值）。
- **对比方法**：
  - 单任务教师：GenEval Teacher、OCR Teacher、Aes Teacher。
  - 多任务RL基线：Multi-Task GRPO-Guard、Multi-Task NFT（联合训练）。
  - 级联RL：Cascade NFT（顺序训练）。
  - 其他蒸馏方法（消融）：DMD、TDM、SFT。

### 评价指标
- 各任务具体得分；训练收敛曲线（效率对比）；定性图像展示。

## 4. 资源与算力

- 论文**未明确说明**使用的GPU型号和数量。
- 提供了**wall-clock时间**（如表1）：单任务教师训练时间（GenEval Teacher 46.92h, OCR Teacher 33.17h, Aes Teacher 85.75h）；DiffusionOPD蒸馏时间 11.26h，总时间 = max(教师时间) + 蒸馏时间 = 85.75+11.26h。
- Multi-Task RL基线耗时约129-148h，Cascade NFT 148.49h。DiffusionOPD总时间明显更少。
- 未提及分布式训练细节、显存消耗等具体算力信息。

## 5. 实验数量与充分性

- **主实验**（表1）：对比了5类方法（基线+本方法）在7个指标上的性能，展示了单任务教师、多任务RL、级联RL、DiffusionOPD的优缺点。
- **训练效率对比**（图3）：展示收敛曲线，证明DiffusionOPD更快、天花板更高。
- **消融实验**：
  1. **蒸馏方法对比**（图5）：对比DMD、TDM、SFT，DiffusionOPD最快且最优。
  2. **损失公式对比**（图6a）：闭式KL vs. PPO风格梯度，证明闭式KL更快、更好。
  3. **采样噪声水平**（图6b）：对比不同噪声水平（SDE 0.7, 0.3, 0.1, L2 for ODE），发现噪声越低越好，尤其是ODE（噪声0）收敛快5倍。
- **定性结果**（图2/4/7/8）：直观展示图像质量比较。
- **充分性评价**：实验较为充分，覆盖了主要多任务学习基线、多种蒸馏方法、关键设计组件消融，且在同一设置下公平比较。但缺乏在更大模型（如SDXL、FLUX）上的测试，也未进行多种种子重复实验（虽然表1数据看起来是单次结果，但无明显偏差标注）。

## 6. 论文的主要结论与发现

- **主要结论**：DiffusionOPD通过解耦单任务探索和多任务蒸馏，有效避免了联合优化中的任务干扰与级联RL的遗忘问题，在训练效率和最终性能上均优于所有现有多任务扩散RL方法。
- **关键发现**：
  - 闭式KL目标比PPO风格策略梯度方差更低，训练更稳定。
  - 蒸馏时使用ODE（噪声0）比SDE（高噪声）更高效。
  - 不同任务需要选择不同的教师训练算法（如OCR用GRPO-Guard防止reward hacking，GenEval用DiffusionNFT更高效）。
  - 定性结果也显示DiffusionOPD能够同时保持高美学质量、高OCR准确度和良好组合遵循。

## 7. 优点

1. **范式新颖**：将在线策略蒸馏引入扩散模型多任务学习，有效解耦探索与集成，思路清晰。
2. **理论扎实**：从LLM的离散OPD推广到连续状态马尔可夫过程，推导出闭式KL目标，严密证明了与PPO的等价性及方差优势。
3. **方法统一**：同时覆盖SDE和ODE采样器，通用性强。
4. **实验全面**：在主任务、多种基线、消融实验上都做了比较，结果一致优势。
5. **效率高**：总训练时间（教师训练+蒸馏）远小于联合或级联RL。
6. **性能顶尖**：在所有评估指标（平均得分）上达到SOTA。

## 8. 不足与局限

1. **教师依赖**：教师模型的质量直接影响学生上限；如果某个任务教师训练不足或存在reward hacking，可能影响最终结果。论文中已提到针对不同任务选择不同算法，但未给出自动化的选择策略。
2. **多轮整合成本**：虽然蒸馏时间短，但教师训练仍需不少算力（尤其Aes Teacher 85.75h），且需要存储多个教师模型。
3. **场景局限**：实验仅使用SD3.5-Medium 512×512；未验证更大模型或更高分辨率下的有效性。
4. **评估指标多样性**：GenEval和OCR的评估依赖特定规则，模型型奖励可能存在偏差（如与人类偏好的一致性有限）。
5. **稳定性和可重复性**：未报告多次运行结果的统计量（如方差、置信区间），仅提供单次得分，可能影响结果可靠性。
6. **应用限制**：方法假设所有任务共享同一基础模型结构，若任务差异极大（如不同输出模态），可能不适合蒸馏。

（完）
