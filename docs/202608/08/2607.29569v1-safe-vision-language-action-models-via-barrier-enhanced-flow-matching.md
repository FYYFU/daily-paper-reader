---
title: Safe Vision Language Action Models via Barrier Enhanced Flow Matching
title_zh: 安全视觉语言动作模型：基于屏障增强的流匹配方法
authors: "Kasra Sinaei, Hung-Chieh Wu, Donald Ebeigbe"
date: 2026-07-31
pdf: "https://arxiv.org/pdf/2607.29569v1"
tags: ["query:ai-safety"]
score: 9.0
evidence: 通过控制障碍函数为视觉语言动作模型提供安全保障，直接涉及智能体安全
tldr: 现有安全机制通常在模型输出后施加外部过滤器，可能改变语义且需要额外数据。本文提出在Flow Matching去噪过程中内嵌CBF屏障，利用Log-Sum-Exp聚合屏障保障整个动作块安全，计算开销极小且不改变模型意图。理论证明生成分布与目标分布的2-Wasserstein距离有界。在两类机器人操作平台和2D导航任务上验证，实现可靠安全同时不降低任务成功率，且无需安全数据集或模型重训练。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.29569v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 846, \"height\": 557, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29569v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 727, \"height\": 718, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29569v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 712, \"height\": 709, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29569v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 814, \"height\": 311, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29569v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 724, \"height\": 744, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29569v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 860, \"height\": 374, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.29569v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 901, \"height\": 278, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.29569v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 903, \"height\": 264, \"label\": \"Table\"}]"
motivation: 避免外部安全过滤器对模型语义的干扰，同时消除对安全数据或重训练的依赖，实现生成过程内生的安全保证。
method: 将LSE聚合的Control Barrier Function嵌入Flow Matching去噪迭代，约束动作块整体安全，并保持额外计算开销极小。
result: 在机器人操作与2D导航基准上，框架在保证安全性的同时维持原有成功率，且生成分布与目标分布的Wasserstein距离有界。
conclusion: 该模块化安全推理框架无需重新训练即可为视觉语言动作模型提供形式化安全保证，具备良好通用性。
---

## 摘要
本文提出了一种模块化推理框架，将流匹配生成模型与形式化的控制屏障函数（CBF）安全保证相结合。与现有方法对模型最终输出施加外部安全过滤器不同，我们的方法修改了模型内部的流匹配去噪过程，从而固有地生成安全轨迹。通过采用平滑的对数和指数聚合屏障，我们在整个动作片段上强制执行安全性。这种聚合屏障确保计算开销的增加最小，并且不改变模型的语义意图。我们证明，在所提出的框架内，生成分布与目标分布之间的2-Wasserstein距离保持有界。我们的方法无需特定安全数据集或昂贵的模型重训练，为安全推理提供了一种通用解决方案。我们在两个机器人操作平台和一个2D导航基准上验证了该方法，确认我们的框架在实现可靠安全性的同时不降低模型成功率。

## Abstract
This article presents a modular inference framework that integrates Flow Matching generative models with formal Control Barrier Function (CBF) safety guarantees. Unlike existing methods that apply external safety filters to a model's final output, our approach modifies the Flow Matching denoising process within the model to inherently generate safe trajectories. By employing a smooth Log-Sum-Exponential aggregate barrier, we enforce safety over entire action chunks. This aggregate barrier ensures a minimal increase in computational overhead and does not alter the semantic intent of the model. We show that, within the proposed framework, the 2-Wasserstein distance between the generated distribution and the target distribution remains bounded. Our method eliminates the need for safety-specific datasets or costly model retraining, providing a versatile solution for safe inference. We validate the approach on two robotic manipulation platforms and a 2D navigation benchmark, verifying that our framework achieves reliable safety without degrading the success rate of the model.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机与背景）

- **背景**：视觉语言动作（VLA）模型通过结合视觉-语言理解与机器人控制，使通用机器人策略成为可能。典型架构如 OpenVLA（7B）、SmolVLA（550M）和 Physical Intelligence π0 采用“VLM 主干 + Action Expert（AE）”的双 Transformer 结构，并使用 **流匹配（Flow Matching）** 作为去噪生成过程，以支持实时、高频的机器人控制。
- **核心问题**：VLA 模型生成的轨迹缺乏形式化安全保证，在真实环境中可能产生碰撞或违反安全约束的行为。现有安全方案（如外部 CBF 后处理过滤器）存在两个主要缺陷：
  - 后置过滤会生成**分布外样本**，损害轨迹质量与语义一致性；
  - 安全类方法通常需要安全专用数据集、模型重训练或额外的引导信号，成本高昂、通用性差。
- **整体含义**：本文提出一种**模块化推理框架**，将控制屏障函数（CBF）直接嵌入流匹配去噪过程中，使模型**内生地**生成安全轨迹，同时不改变语义意图、不增加显著的计算开销，也无需重新训练模型。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：在流匹配 ODE 的去噪过程中，加入一个由 CBF 二次规划（QP）求解的最小扰动项 δ_s，使整个动作片段满足安全约束。通过在**每个去噪步骤内**施加安全修正，而不是在最终输出上做后处理，避免了分布外偏移问题。
- **关键技术细节**：

  - **动作块的安全定义**：将单个状态的安全概念扩展到长度为 H 的动作块 z（式 6），即要求轨迹中所有状态 h(q_i) ≥ 0。
  - **Log-Sum-Exp（LSE）聚合屏障**（式 8）：

    h(z̄) = −(1/κ) ln(∑ e^(−κh(q_i)))

    该函数光滑地逼近 min 算子，避免 min 的不连续性，且满足下界（式 9）：min h(q_i) − lnH/κ ≤ h(z̄) ≤ min h(q_i)。κ 增大时逼近更精确但安全裕度更小。
  - **CBF-QP 约束**（式 12）：在去噪每次迭代中求解

    δ_s = argmin (1/2)‖δ‖²，满足 ∂h/∂z · (v_θ + δ) ≥ −αh(z)，

    得到的闭式解为 ReLU 形式的投影（式 13）：
    δ_s = ReLU(−αh(z) − ∇h(z)v_θ) / ‖∇h‖² · ∇h。
  - **最终推理 QP**（式 19）：在目标函数中加入动作块平滑项，并使用稀疏矩阵 D 对相邻动作之间的速度施加箱型约束（v_min ≤ D(z̄ + (v_θ + δ)Δτ) ≤ v_max），从而同时保障安全与轨迹平滑。
  - **算法流程**（Algorithm 1）：采样噪声 → 进行 τ ∈ [0,1] 的去噪循环 → 当 τ ≥ τ_s 时计算 LSE 屏障及其梯度，求解 QP 得到 δ_s → 更新 z；τ < τ_s 时 δ_s = 0，以避免在结构化不足的噪声阶段施加无意义的屏障约束。
- **理论保证**：

  - **Theorem 3（Wasserstein 界）**：在三个假设条件下，生成的分布与目标分布的 2-Wasserstein 距离存在上限：W₂(π̂₁, π₁) ≤ ε·exp(∫₀¹ L dτ)。
  - **Corollary 1**：加入 CBF 扰动后，Lipschitz 常数与新误差界均保持有限，安全修正不会导致严重的分布偏移。
  - **Theorem 4（安全保证）**：当采样的噪声安全值 h₀ > 0 且 τ_s ≤ h₀/M 时，过滤后的生成过程保证 h(ψ(z,1)) > 0，即生成轨迹始终安全。

## 3. 实验设计：数据集 / 场景 / Benchmark / 对比方法

- **2D Maze（模拟导航基准）**：
  - 使用 100 次试验，比较 BS1（障碍物 1 安全度）、BS2（障碍物 2 安全度）、困在陷阱中的百分比（Trap %）、单次规划计算时间（Time）、轨迹曲率（κ）和加速度。
  - 对比方法：FM（无安全）、SafeDiffuser [15]、SafeFlowMatcher（SafeFM）[16]，以及本文的 CBF-FM。
  - 结果：CBF-FM 在所有安全指标上均正（BS1=0.109，BS2=0.046），无陷阱，计算时间最低（3.3ms，远优于 SafeFM 的 195.9ms），轨迹曲率和加速度也最低。
- **机器人操作实验（硬件验证）**：
  - **平台 1**：SO-101（5-DoF 机械臂 + 两指夹爪），执行**拾取与放置**任务。微调使用约 150 次遥操作演示、84k 帧、30FPS 的训练数据。
  - **平台 2**：Quanser QArm（4-DoF 机械臂），执行**桌面清理**任务，使用 180k 帧数据集。两个数据集均无安全标注。
  - 对比方法：无过滤（No Filter）、端到端 CBF 后处理（E2E-CBF，即传统式安全过滤器），以及本文的 CBF-FM。
  - 障碍物为软性障碍，碰撞不致命，因此“安全性”和“成功率”可独立衡量。
- **消融实验**（图 2）：针对 κ（1/5/50/250）和 τ_s（0.6/0.7/0.8/0.9）进行消融，考察对安全裕度的影响；另展示速度约束对关节角速度的抑制效果（图 3）。

## 4. 资源与算力

- 论文中**未明确说明**使用的 GPU 型号、数量或训练时长。仅提到训练数据量为 84k 帧（SO-101）和 180k 帧（QArm）、帧率 30FPS，以及基于 π0 预训练模型进行微调（fine-tuning）。
- 实验推理端使用了公共 Python 库 **qpth** 作为 QP 求解器，CPU 上的规划计算时间（3.3ms/次）已有报告，但训练阶段硬件配置未给出。如果资源信息对读者重要，这是论文的一个信息缺口。

## 5. 实验数量与充分性

- **实验数量**：
  - 2D Maze：每个方法 100 次试验，4 个方法（含无安全基线）共约 400 次。
  - 硬件操作：每个平台 × 每个方法 ≥ 20 次重复，两个平台共约 120 次以上试运行。
  - 另有消融实验组（κ、τ_s 各 4 组）和速度约束展示。
- **充分性与客观性**：
  - 优点：多场景覆盖（模拟+两组真实机械臂）、独立评估安全率与成功率、存在消融、SOTA对比。
  - 不足：真实硬件每个方法仅不少于 20 次，未报告置信区间/标准差；2D 对比中没有包含更多近期安全规划方法（如 SafeFlow、VLSA）；2D 中未报告成功率（因所有方法均为 100%），比较维度有限。整体而言，实验足以支持核心结论，但严谨细节仍有提升空间。

## 6. 论文的主要结论与发现

- 将 CBF 滤波器嵌入流匹配去噪过程可以在**保持模型语义和任务成功率**的同时实现**100% 的安全率**（在两个硬件平台上）。
- 对比后处理过滤器（E2E-CBF，安全率仅 68.2%-70%），CBF-FM 显著提高安全性且不损害成功率（SO-101 上 77.4% vs 基线 75%；QArm 上 100% vs 基线 100%）。
- 通过 LSE 聚合屏障将多个障碍约束归并为一个约束，加上速度约束和平滑项，有效减少了计算时间（对比 SafeFM 约 60 倍的提速）并生成了更平滑、低屈曲、低加速度的轨迹。
- 修改后的生成分布与目标分布之间的 2-Wasserstein 距离保持有界，安全过滤不会导致明显的分布偏移。
- 整体表明，模块化安全推理框架**无需安全专用数据或重训练**，即可为预训练 VLA 提供形式化安全保证，具有较好的通用性。

## 7. 优点（方法论与实验亮点）

- **无需重训练**：安全过滤仅发生在推理阶段，适用于任何预训练的 Flow Matching VLA。
- **内生安全而非后置过滤**：直接在去噪过程中保障安全性，避免后处理造成的分布外问题。
- **计算开销极小**：LSE 聚合屏障 + 稀疏矩阵速度约束使 QP 规模适中，可在 3.3ms 内完成规划，适合实时控制。
- **形式化保证**：同时提供 Wasserstein 分布误差上界和轨迹安全下界，理论依据扎实。
- **轨迹平滑性**：在优化目标中引入速度差分项和显式速度箱型约束，兼顾安全与执行质量。
- **可扩展的安全描述**：支持平面壁屏障和球区域屏障，并兼容其他（如多智能体、自动驾驶）安全规范。

## 8. 不足与局限性

- **算力信息缺失**：未报告训练时使用的 GPU 型号、数量、时长等细节，难以评估方法在真实部署中的资源门槛。
- **实验规模偏小**：真实机器人每个方法只重复 20 次以上，样本量有限且未给出统计置信区间；2D 对比实验对成功率指标失报。
- **对比方法不够全**：2D 中只对 SafeDiffuser 和 SafeFM，硬件中只对无过滤和 E2E-CBF，未与近期其他安全 VLA 框架（如 SafeVLA、VLSA、SafeFlow）直接对照。
- **安全范围有限**：目前只考虑静态、位置级（一阶）空间安全障碍（平面/球体），未处理动态障碍、接触力、速度级的高阶安全约束。
- **对模型假设依赖**：Theorem 3 与 Corollary 1 依赖于屏障函数有界梯度、紧凑工作空间、远离运动奇异点等假设；实际复杂构型下这些条件可能被破坏。
- **τ_s 选择依赖经验**：τ_s 的选取对安全性影响较大（图 2），没有自动调节机制，需要用户根据任务和环境调参。

（完）
