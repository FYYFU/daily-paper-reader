---
title: "Steering Beyond the Support: Adversarial Training on Unsupervised Jailbroken Activation Simulation"
title_zh: 超越支持范围的导向：无监督越狱激活模拟上的对抗训练
authors: "Luoyu Chen, Weiqi Wang, Zhiyi Tian, Chenhan Zhang, Feng Wu, Jianhuan Huang, Ahmed Asiri, Shui Yu"
date: 2026-05-23
pdf: "https://arxiv.org/pdf/2605.24535v1"
tags: ["query:ai-safety"]
score: 9.0
evidence: 通过无监督激活模拟的对抗训练解决越狱攻击与防御
tldr: "针对对齐LLM在面对未知越狱提示时安全导向方法失效的问题，本文提出一种基于无监督潜在方向发现的双层对抗训练框架。内层通过外推拒绝状态下的有害请求激活来模拟多样越狱激活，扩展真实越狱激活子空间覆盖；外层训练势诱导导向场将这些对抗性越狱状态推入拒绝区域，同时保持良性不变。在三个LLM和六类经典越狱家族上，攻击成功率大多低于5%，且训练中子空间覆盖率的上升解释了泛化能力的提升。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.24535v1/fig-001.webp\", \"caption\": \"\", \"page\": 2, \"index\": 1, \"width\": 748, \"height\": 542}, {\"url\": \"assets/figures/arxiv/2605.24535v1/fig-002.webp\", \"caption\": \"\", \"page\": 5, \"index\": 2, \"width\": 975, \"height\": 711}]"
motivation: 现有安全导向方法受限于静态训练集，无法处理未知越狱攻击，亟需无监督的零样本防御方法。
method: 提出双层对抗训练：内层通过无监督潜在方向发现外推激活模拟越狱，外层训练势诱导导向场将对抗状态推向拒绝区域。
result: "在三个LLM和六类越狱家族上攻击成功率低于5%，子空间覆盖率随训练上升。"
conclusion: 该方法有效提升了零样本越狱防御能力，通过扩展激活子空间覆盖实现泛化。
---

## 摘要
越狱提示可以触发对齐语言模型的有害完成，为此提出了安全导向：在测试时进行激活干预，将越狱激活导向拒绝响应，同时保留良性效用。然而，现有的导向方法本质上是监督式的，且依赖于静态、有限的训练集，而真实的越狱是不断演化的，且往往与训练集分布不同，导致在未见攻击上失败。在本文中，我们基于无监督潜在方向发现来解决未见越狱的失败问题。我们提出了一种双层对抗训练框架，用于零样本越狱防御。在内层步骤中，我们通过无监督潜在方向发现，从拒绝状态下的有害请求激活进行外推，模拟出多样化的越狱激活，从而扩展真实越狱激活子空间的覆盖范围。在外层步骤中，我们训练一个势诱导导向场，将这些对抗性越狱状态推入拒绝区域，同时保持良性状态不变。在三个语言模型和六个经典越狱家族上的实验表明，我们的方法实现了强大的防御，攻击成功率大多低于5%，并且训练过程中子空间覆盖率的提升有助于解释泛化能力的改进。

## Abstract
Jailbreak prompts can trigger harmful completions on aligned LLMs, In accordance, safety steering has been proposed: test-time activation interventions that steer jailbreak activations to trigger refusal while preserving benign utility. However, existing steering methods are fundamentally supervised and tied to a static, limited training set, whereas real jailbreaks evolve and are often out-of-distributed from the training set, leading to failures on unseen attacks. In this paper, we tackle the failure on unseen jailbreaks problem, base on unsupervised latent direction discovery. We propose a bi-level adversarial training framework for zero-shot jailbreak defense. In the inner step, we simulate diverse jail-broken activations by extrapolating from refusal-state harmful-request activations via unsupervised latent direction discovery, which expands the coverage of real jailbreak activation subspaces. In the outer step, we train a potential-induced steering field to push these adversarial jailbroken states into refusal regions while keeping benign unchanged. Across three LLMs and six classical jailbreak families, our method achieves strong defense with attack success rates mostly below 5%, and rising subspace coverage throughout training helps explain the improved generalization.

---

## 论文详细总结（自动生成）

### 论文总结：Steering Beyond the Support: Adversarial Training on Unsupervised Jailbroken Activation Simulation

#### 1. 核心问题与整体含义（研究动机和背景）
- **问题**：现有安全导向（safety steering）方法通过测试时激活干预（如线性向量加法）将越狱激活推向拒绝区域，但它们本质上是**监督式**的，依赖静态且有限的训练集（如少量已知越狱类型）。真实世界中的越狱攻击不断演化，常处于训练集分布之外（OOD），导致防御在未知越狱上严重失效。
- **动机**：为了实现零样本、泛化性强的越狱防御，需要一种无需监督标签、能自动扩展训练支持（training support）的方法。作者受无监督潜在方向发现（unsupervised latent direction discovery）启发，提出通过外推拒绝状态下的有害请求激活来模拟多样化的越狱激活，然后在此模拟分布上进行对抗训练，从而使导向场学会对抗未见越狱。

#### 2. 方法论：核心思想、关键技术细节与算法流程
- **核心思想**：利用**双层对抗训练**框架：
  - **内层（Inner Step）**：通过无监督潜在方向发现（UFD）从拒绝状态激活（harmful request activations）外推出多样化的“越狱激活”（jailbroken activations），作为对抗样本对当前导向场进行压力测试。
  - **外层（Outer Step）**：训练一个**势诱导导向场（potential-induced steering field）**，其梯度提供逐点变化的导向方向，将这些模拟越狱激活推向拒绝区域，同时对良性激活保持近恒等变换。
- **关键技术与公式**：
  - **势函数（Potential Function）**：采用最优传输（OT）的 Kantorovich-Rubinstein 对偶形式，学习一个 1-Lipschitz 标量函数 \( f_\phi: \mathbb{R}^d \to \mathbb{R} \)，使得拒绝激活的势值高于其他激活。其梯度 \( \nabla f_\phi(h) \) 即为局部导向方向。
  - **三类损失**：
    - **通用导向性（\(L_g\)）**：通过 OT 对偶最大化拒绝激活与其他激活之间的势差，并加梯度惩罚保证 1-Lipschitz。
    - **良性零导向性（\(L_b\)）**：惩罚良性激活附近的梯度范数，使导向场近似恒等。
    - **越狱强导向性（\(L_j\)）**：鼓励越狱激活附近梯度范数大，便于多步梯度上升快速纠正。
  - **无监督潜在方向发现（\(L_{UFD}\)）**：在源层注入 K 个单位方向向量，观察目标层激活偏移，通过优化使偏移一致且方向多样，从而生成模拟越狱激活 \( h_{\text{adv}} = h_r + R v \)。
- **算法流程**：交替进行：
  1. 内层：固定当前 \( f_\phi \)，优化 V 使 \( L_j (h_{\text{adv}}; f_\phi) \) 最大（即生成最难导向的模拟越狱激活），同时使用 UFD 目标 \( L_{UFD} \)。
  2. 外层：固定 V，优化 \( f_\phi \) 联合最小化 \( L_g \)、\( L_b \) 和 \( L_j \)（即增强对这些模拟激活的导向能力）。

#### 3. 实验设计
- **数据集/场景**：
  - **越狱鲁棒性**：HarmBench 上的六种经典攻击：GCG, AutoDAN, GPTFuzz, PAIR, TAP, FewShot。使用 StrongReject (SR) 指标（越低越好）。
  - **过拒绝（Over-refusal）**：OR-Bench 边界案例，报告假阳性率 (FPR)。
  - **能力保持**：ARC-C, TruthfulQA, GSM8K 上的准确率。
- **基准方法**：
  - 安全导向：AlphaSteer（监督式条件导向）
  - 表示安全工程：Circuit-Breaker (CB), Latent Adversarial Training (LAT), ROSI（权重编辑）
- **模型**：LLaMA-3-8B, Mistral-v2-7B, Qwen-2.5-7B
- **训练数据**：500 条 ALPACA EVAL（良性）、500 条 OR-Bench（安全边界）、1000 条 AdvBench 与 OR-Bench-Toxic 混合（有害请求），均不包含任何越狱标签。

#### 4. 资源与算力
- **未明确说明**：论文未提及 GPU 型号、数量或训练时长。仅提到底层势网络为轻量 MLP（隐藏层 [d, d/2, d/4, 1]），推理时测试时间导向每样本仅需 O(K·P) 计算，批大小为 100 时约 1.23 秒。

#### 5. 实验数量与充分性
- **充分性**：实验设计全面，覆盖了：
  - 主实验结果（表 3、表 4）：6 种越狱攻击 × 3 个模型，对比 4 个基线，报告 SR、FPR、能力准确率及综合效用分数。
  - 消融实验（图 6、图 7）：
    - 对比 **Targeted AT**（固定前缀模拟） vs. **Ours**（无监督 AT）
    - 对比 **Without AT**（去掉外层对抗损失） vs. **Ours**
    - 显示子空间覆盖率（Coverage）与安全性能（Avg. SR）的对应关系。
  - 机械理解实验（图 4、图 5）：可视化模拟激活扩展和真实越狱的可导向性。
  - 梯度步数影响（附录 H，图 10）：安全-效用权衡扫描。
- **公平性**：对比方法均为公开强基线，且使用了标准指标（StrongReject, FPR）。训练集不含越狱样本，避免了数据泄露。实验客观可信。

#### 6. 主要结论与发现
- 本文方法在**零越狱标签**情况下，在所有模型和攻击上取得了与甚至优于监督式方法的防御效果，攻击成功率（SR）大多低于 5%。
- **子空间覆盖率**随训练步骤不断增加，且与防御性能正相关，表明对抗激活模拟扩展了训练支持，从而提升了泛化能力。
- 良性能力保持显著优于其他方法（如 CB、LAT 导致过拒绝剧增），效用分数更高。

#### 7. 优点
- **新颖性**：首次针对安全导向的监督式缺陷，提出无监督激活模拟+双层对抗训练的范式，无需任何越狱标签。
- **有效性**：在三个模型、六种越狱家族上验证，性能稳定。
- **可解释性**：通过子空间覆盖率指标和可视化，解释了泛化能力提升的机制。
- **实用性**：推理时仅需轻量 MLP 的前向和反向计算，效率高。
- **设计均衡**：三部分损失函数分别保证了通用导向、良性零干扰和越狱强导向，安全与效用兼顾。

#### 8. 不足与局限
- **依赖模拟质量**：方法有效性取决于无监督潜在方向发现是否能覆盖所有真实越狱激活区域。对于非自然语言（如 base64、密码）等非线性变换可能弱可达，存在遗漏。
- **缺乏形式保证**：仅凭经验验证了子空间覆盖率提升，但无理论保证模拟分布逼近真实分布。
- **仅适用线性干预假设**：当前外推力法基于线性方向外推，可能无法处理更复杂的非线性越狱行为。
- **实验覆盖**：评估仅集中在 7B 级别模型和文本域，未扩展到更大模型（如 70B）或多模态场景；未测试实际部署中的延迟与鲁棒性-效用权衡上限。
- **潜在滥用风险**：方法可被用于更强越狱信号合成，需谨慎发布代码。

（完）
