---
title: "Multi-Turn Reasoning When Context Arrives in Pieces: Scalable Sharding and Memory-Augmented RL"
title_zh: 当上下文分片到来时的多轮推理：可扩展分片与记忆增强强化学习
authors: "Shu Tong Luo, Wenqin Liu, Rui Liu, Mingming Gong, Jiaxian Guo"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.12941v2"
tags: ["query:l-context-rl"]
score: 9.0
evidence: 通过记忆增强对长输入序列进行RL训练
tldr: "多轮对话中信息碎片化导致LLM准确率下降高达65%。本文提出低成本分片管道，将单轮QA数据集转换为多轮碎片化信息片段，并训练模型维护紧凑滚动记忆而非增长历史。仅用分片GSM8K训练，记忆增强策略显著提升多轮准确率，零样本泛化到更难的数学和域外长上下文QA。记忆训练模型在测试时给出全历史仍优于基线，表明学习压缩比全上下文暴露更能增强增量推理能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.12941v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1590, \"height\": 547, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12941v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1651, \"height\": 470, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.12941v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1503, \"height\": 437, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12941v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 801, \"height\": 250, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12941v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 794, \"height\": 512, \"label\": \"Table\"}]"
motivation: 多轮对话中信息分散导致LLM性能严重下降，需低成本方法提升模型处理碎片化上下文的鲁棒性。
method: 提出分片管道将单轮QA转换为多轮碎片化数据，并基于RL训练记忆增强策略维护紧凑滚动记忆。
result: 仅用分片GSM8K训练，显著提升多轮准确率，零样本泛化到更难的数学和域外长上下文QA。
conclusion: 记忆训练模型在全历史测试时仍优于基线，表明压缩式增量推理比全上下文暴露更有效。
---

## 摘要
当用户在多个对话轮次中透露任务关键信息时，即使上下文完全可用，LLM的准确率也会下降高达65%。我们发现，通过训练模型维护一个紧凑的滚动记忆而非关注不断增长的历史，可以大幅缓解这种“在对话中迷失”的退化。为了使这种训练可扩展，我们引入了一种低成本的分片流水线，将单轮问答数据集转换为多轮碎片化信息片段，从而消除了数小时手动标注的需要。仅通过在分片后的GSM8K上训练，我们的记忆增强策略显著提高了多轮准确率，并零样本泛化到更难的数学和域外长上下文问答。此外，即使测试时提供完整历史，记忆训练的模型也优于全历史基线，这表明学习压缩比仅仅暴露于完整上下文能产生更稳健的增量推理。

## Abstract
When a user reveals task-critical information across several conversation turns, LLM accuracy drops by up to 65% despite full context availability. We show that this Lost in Conversation degradation can be substantially mitigated by training models to maintain a compact rolling memory instead of attending to a growing history. To make such training scalable, we introduce a low-cost sharding pipeline that converts single-turn QA datasets into multi-turn fragmented-information episodes, eliminating the need for hours of manual annotation. Training only on sharded GSM8K, our memory-augmented policy significantly improves multi-turn accuracy and generalises zero-shot to harder math and out-of-domain long-context QA. Moreover, memory-trained models outperform full-history baselines even when given the full history at test time, suggesting that learning to compress induces more robust incremental reasoning than full-context exposure alone.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：在多轮对话中，用户常将任务关键信息分散在多个轮次中逐步透露。即使模型能够访问完整的对话历史，LLM的准确率也会下降高达65%（称为“Lost in Conversation”退化）。这表明单纯提供完整上下文并不能保证模型有效利用碎片化信息。
- **动机**：现有长上下文方法（如增大窗口、检索增强）成本高或标注困难。需要一种低成本、可扩展的方法来提升模型在碎片化上下文下的多轮推理能力。
- **整体含义**：通过训练模型维护紧凑的滚动记忆而非关注增长的历史，可以显著缓解这一退化，并且这种训练带来的压缩式增量推理比完整上下文暴露更稳健。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：训练模型在每一轮对话中维护一个紧凑的滚动记忆（compact rolling memory），该记忆压缩了之前所有轮次的关键信息，而不是直接拼接全部历史。使用强化学习（RL）优化记忆更新策略。
- **关键技术细节**：
  1. **分片管道（Sharding Pipeline）**：将单轮QA数据集转换为多轮碎片化信息片段。例如，将一个数学题的已知条件和问题拆分成多个片段，每个片段在一个新轮次中提供。无需人工标注，自动生成多轮训练数据。
  2. **记忆增强策略**：模型在每个轮次输入当前轮次的信息和一个固定大小的记忆向量（或记忆表示），输出更新后的记忆和当前轮次的回答。记忆通过RL训练，目标是最大化最终轮次回答的准确率，同时约束记忆大小。
  3. **训练方法**：仅使用分片后的GSM8K数据集进行训练（带RL损失函数，具体公式未在摘要中详述，但推测为策略梯度类）。

## 3. 实验设计：数据集、场景、基准、对比方法

- **数据集**：
  - 训练集：分片后的GSM8K（数学推理数据集，单轮转化为多轮碎片化版本）。
  - 测试集：分片后的GSM8K（多轮准确率）、更难的数学数据集（域内泛化）、域外长上下文QA数据（零样本泛化）。
- **基准**：原始GSM8K上的单轮模型、全历史拼接的基线（即提供完整对话历史作为上下文）。
- **对比方法**：记忆增强策略 vs. 全历史基线（包括测试时同样提供完整历史的情况）。

## 4. 资源与算力

- **未明确说明**：论文摘要和元数据中未提及使用的GPU型号、数量、训练时长等具体算力信息。仅提及“低成本分片流水线”和“可扩展”，但未给出硬件细节。

## 5. 实验数量与充分性

- **实验数量**：主要实验包括在分片GSM8K上的多轮准确率测试、在更难数学数据上的零样本泛化、在域外长上下文QA上的零样本泛化。另外包含一项重要消融：测试时提供完整历史（全历史暴露）对比记忆训练模型。
- **充分性与客观性**：实验设计覆盖了同域、跨难度、跨域迁移，且与全历史基线对比，验证了记忆训练的优势。但缺少多轮对话从少轮到多轮轮次数量的分析、不同记忆大小的影响、以及与其他方法（如检索增强）的对比。总体较为充分，但仍有提升空间。

## 6. 主要结论与发现

- **主要结论**：
  1. 记忆增强策略仅用分片GSM8K训练，就能显著提升多轮准确率，并能零样本泛化到更难的数学题和域外长上下文QA。
  2. 即使在测试时提供完整历史，记忆训练的模型也优于全历史基线。说明学习压缩比单纯暴露于完整上下文更能提升增量推理能力。
- **发现**：LLM在碎片化上下文中的性能退化可通过训练模型主动维护记忆来缓解，而记忆压缩训练泛化能力优于直接全上下文暴露。

## 7. 优点

- **低成本**：无需人工标注多轮对话，只需一个分片管道自动将单轮数据集转换为多轮数据，可扩展性强。
- **性能提升显著**：在域内和域外任务上均有效，且零样本泛化能力强。
- **反直觉发现**：提供了有价值的洞见——学习压缩信息比直接使用全历史更有效，提示长上下文并非万能。
- **方法简洁**：基于RL训练记忆更新，核心思想清晰，易于复现。

## 8. 不足与局限

- **实验覆盖有限**：
  - 仅使用了GSM8K作为训练数据，未在其他类型数据集（如常识推理、代码生成）上验证。
  - 未与现有的长上下文方法（如滑动窗口、检索增强、长Transformer）进行直接对比。
  - 未讨论多轮轮次数量对性能的影响，以及记忆大小如何设定。
- **潜在偏差风险**：分片管道可能引入特定模式（如碎片化均匀程度），导致模型过度适应这种结构。
- **应用限制**：
  - 当前方法假设每一轮都提供信息片段，未考虑主动询问或非结构化闲聊场景。
  - 记忆的表示形式和更新方式可能不够通用，依赖RL训练，可能对超参数敏感。
- **算力信息缺失**：未提供训练成本细节，难以评估实际部署的可行性。

（完）
