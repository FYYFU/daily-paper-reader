---
title: Scenario Generation for Risk-Aware Reinforcement Learning with Probably Approximately Safe Guarantees
title_zh: 面向风险感知强化学习的场景生成与近似安全的概率保证
authors: "Mohit Prashant, Arvind Easwaran"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04812v2"
tags: ["query:ai-safety"]
score: 9.0
evidence: 为风险感知强化学习生成场景并提供安全性保证
tldr: 针对强化学习策略因转移扰动导致不安全行为的问题，提出利用变分自编码器近似状态分布，构建上下界概率屏障证书以标定安全区域。通过采样上下界差集（非鲁棒区域）优化训练，收紧边界以提供更严格的安全概率保证。实验验证了边界紧致性，为RL安全部署提供可验证的风险感知方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04812v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 874, \"height\": 607, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.04812v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 582, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04812v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1093, \"height\": 757, \"label\": \"Table\"}]"
motivation: 强化学习策略易受转移不确定性影响导致不安全行为，现有安全验证方法难以获得紧致的概率违例边界。
method: 使用VAE近似状态空间分布，基于潜在特征构建上下界概率屏障证书，并利用非鲁棒区域采样优化以收紧边界。
result: 实验表明该方法有效收紧安全概率的上下界，比传统方法提供更尖锐的保证。
conclusion: 本工作为强化学习安全验证提供了可证明的紧致边界，增强了策略在未知状态下的风险感知能力。
---

## 摘要
保证安全性对于强化学习（RL）智能体在现实世界中的部署至关重要，尤其是当使用深度RL学习的策略可能表现出对转换扰动的敏感性，导致未知或不安全行为时。一种策略验证方法是通过在安全约束下对策略轨迹进行采样来构建概率障碍证书，从而将已知安全行为与未知行为区分开来。如果策略容易受到转换不确定性或扰动的影响，使智能体陷入探索不充分的状态，那么获得这些约束违反概率的严格上下界可能很困难。为了解决这个问题，我们使用变分自编码器（VAE）近似遇到的状态空间分布，并利用状态的潜在特征构建上下界障碍证书，以高置信度优化已知安全行为的区域。在我们的工作中，这被表述为一个对偶优化问题，其中下界障碍证书比上界障碍证书提供了对安全区域更保守的估计。在训练过程中，采样位于两者集合差（即非鲁棒区域）内的状态，使我们能够收紧上下界，从而提供更尖锐的安全概率保证。在我们的研究中，我们描述了所提出的保证，并通过实验证明了我们界限的紧致性。

## Abstract
Guaranteeing safety is critical to the deployment of reinforcement learning (RL) agents in the real-world, especially as policies learned using deep RL may demonstrate susceptibility to transition perturbations that result in unknown or unsafe behaviour. A method of policy verification is to construct probabilistic barrier-certificates by sampling policy trajectories with respect to safety constraints, thereby demarcating known safe behaviour from unknown behaviour. Obtaining tight upper and lower bounds on the probability of violation of these constraints may be difficult if the policy is susceptible to transition uncertainty or perturbation that places the agent in insufficiently explored states. To address this, we approximate the distribution of the encountered state-space using a variational autoencoder (VAE) and construct upper and lower-bound barrier-certificates using latent characteristics of states to optimize for regions of known, safe behaviour with high confidence. We frame this in our work as a dual optimization problem where the lower-bound barrier-certificate presents a more conservative estimate of the safe region than the upper-bound barrier-certificate. Sampling states that lie within the set difference of the two during training, i.e. the non-robust region, allows us to tighten the upper and lower bounds to provide sharper probabilistic guarantees on safety. Within our study, we describe the guarantees placed and demonstrate the tightness of our bounds experimentally.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：深度强化学习（RL）策略在实际部署中易受转移扰动影响，导致进入未充分探索的状态而产生不安全行为。传统的安全验证方法（如模型检测）依赖已知的转移动力学，在模型不可知或环境复杂时难以应用。概率近似正确（PAC）保证方法通过采样轨迹给出安全约束违反概率的上下界，但若策略存在探索不足或转移不确定性，上下界会变得宽松，难以提供可靠的风险评估。
- **整体含义**：本文旨在通过生成性建模（VAE）和概率障碍证书（PABC）的联合优化，不仅提供可验证的安全保证，而且主动生成位于“非鲁棒区域”（即上下界证书之间的状态）的场景，以收紧安全概率的上下界，从而提升策略在未知状态下的风险感知能力，为RL安全部署提供可证明的紧致边界。

### 2. 论文提出的方法论

- **核心思想**：将安全保证建模为一个对偶的随机机会约束规划（CCP）：下界障碍证书（保守估计）和上界障碍证书（宽松估计）。利用VAE将状态空间映射到潜在空间，在此空间上构建线性障碍函数。通过采样策略轨迹，借助场景优化方法（基于Camp等人定理）求解障碍证书的参数，并给出违反概率的PAC上下界（𝜖₁, 𝜖₂）。然后，利用VAE的潜在高斯分布特性，识别并生成位于上界证书内但下界证书外的“可疑”状态，作为初始状态继续训练策略，从而缩小上下界差距。
- **关键技术细节**：
  - **障碍证书构建**：定义两个线性函数 B₁(z)=φ_w1·z+φ_b1, B₂(z)=φ_w2·z+φ_b2。下界证书优化目标为：最大化当前区域中所有状态的下一个状态的最小B₁评分，约束为所有采样轨迹安全。上界证书优化目标相反：最小化下一个状态的最大B₂评分，约束为轨迹不安全。两者同时优化，共享相同采样轨迹池。
  - **PAC保证计算**：利用定理5和推论6，根据采样数N、允许违反数k、参数数量|φ|和置信度δ，计算下界𝜖₁和上界𝜖₂。
  - **VAE训练**：使用ELBO损失，KL项加权系数0.6，每个训练阶段后更新。
  - **场景生成（Phase 2）**：使用算法2，识别潜在空间中位于上界证书内但下界证书外的状态，再通过VAE解码还原为状态空间内的初始状态，继续训练策略。
  - **两种模型无关方法**：①专家引导（模拟器或人类专家提供初始状态）；②基于初始状态的邻域采样（算法2）。
- **算法流程**（文字说明）：
  - Phase 1：使用PPO训练策略，并持续训练VAE。
  - 算法1：构造上/下界障碍证书。步骤包括：初始化证书包含初始状态；收集N条轨迹；识别违反安全条件（U(τ)<0或U(τ)>0）的轨迹数k₁/k₂；对非违反轨迹中的状态更新证书参数（梯度上升/下降），直至收敛。
  - Phase 2（算法2）：从Phase 1采集的轨迹中提取位于非鲁棒区域的状态，并在此基础上添加微小的潜在空间邻域（距离β），解码得到新初始状态集S_init'。从此集合中均匀采样继续训练策略。

### 3. 实验设计

- **环境和场景**：使用OpenAI Gym中的**Ant**和**CartPole**环境。Ant代表高维连续控制，CartPole代表经典控制问题。
- **Benchmark**：预设安全约束（如避免跌倒、保持平衡），定义轨迹级安全/不安全标签。
- **对比方法**：
  - Vanilla PPO（基线）
  - Epsilon-Greedy PPO（ε=0.2）
  - Perturbed Exploration（添加高斯噪声到转移函数）
  - CoDE（环境生成方法）
  - Genetic Curriculum（遗传课程学习）
  - Curriculum Adversarial Training（对抗训练课程）
- **评价指标**：上下界(𝜖₁, 𝜖₂)、平均奖励（归一化到[0,1]）。
- **超参数**：N=1000，δ=0.001，β=0.05，α=0.01。PPO总训练1e5 episodes（Phase 1），再1e5 episodes（Phase 2）。

### 4. 资源与算力

- **文中未明确说明**：没有提及使用的GPU型号、数量、训练时长或具体计算资源。仅提到使用默认的Stable-Baselines3实现PPO，VAE使用标准ELBO损失。推测实验可在单GPU或CPU上完成，但未提供量化信息。

### 5. 实验数量与充分性

- **实验数量**：在Ant和CartPole两个环境上，每个方法运行多次（文中提到“均值 across a range of seeds”，但未具体说明种子数）。每个方法在Phase 1和Phase 2后给出(𝜖₁, 𝜖₂)和平均奖励。此外，图1给出了CartPole环境下边界随采样数N的变化。
- **充分性评价**：
  - **优点**：对比了多种主流课程/鲁棒学习方法，包括基线、基于扰动、基于生成的三种范式。跨两个不同复杂度的环境验证，增强了结论的一般性。
  - **不足**：未提供方差或置信区间（仅均值），种子数未明确，可能影响统计显著性。消融实验缺失（如单独验证VAE、β、α等超参数的影响）。未在更多环境（如机器人、自动驾驶）上测试，推广性受限。此外，对安全违例概率的实际值与其上下界的比较仅用图1示意，未定量分析实际违例概率。

### 6. 论文的主要结论与发现

- **核心发现**：提出的Phase 2场景生成方法能在两个环境中有效收紧安全概率的上下界（𝜖₁, 𝜖₂）至更紧的区间，相比其他方法（如Epsilon Greedy、Perturbed Exploration、CoDE等）提供更尖锐的安全保证。具体来说，Ant环境下(𝜖₁,𝜖₂)从Phase 1的(0.11,0.41)降至(0.09,0.18)；CartPole环境下从(0.21,0.48)降至(0.10,0.15)。在性能（平均奖励）方面，本方法与最优方法（如Curriculum Adversarial Training、CoDE）相当，未发生严重性能损失。
- **附加结论**：直接针对策略的认知不确定性（而非随机探索或转移扰动）是提升鲁棒性的更有效方式。图1展示了随着采样轨迹数N增加，上下界逐渐收窄，验证了方法的统计一致性。

### 7. 优点

- **理论贡献**：将PAC保证与生成性模型结合，提供了可证明的紧致边界，弥补了传统安全验证方法无法提供概率下界的不足。
- **方法创新**：对偶CCP框架同时给出上下界，并利用VAE的潜在空间实现可解释性的场景生成，无需环境动力学知识。
- **实用性**：提出两种模型无关的实现方式（专家引导和基于初始状态邻域），适应不同场景（有/无模拟器）。实验证明其收敛速度和最终保证质量优于常见课程学习与扰动方法。
- **复现性好**：使用公开的Gym环境和Stable-Baselines3库，超参数详细，算法伪代码清晰。

### 8. 不足与局限

- **实验覆盖有限**：仅在两个环境上测试，缺乏对高维连续控制（如Humanoid、Fetch）或离散大型动作空间（如Atari）的验证。也未涉及多智能体或现实机器人场景。
- **缺乏统计细节**：未报告多次运行的标准差或种子数，难以判断结果的稳定性。
- **超参数敏感性未分析**：β（潜在空间邻域范围）、α（学习率）、N（采样数）、δ（置信度）等可能显著影响性能，但未进行消融实验。
- **计算成本未量化**：未说明Phase 2的训练时间或额外开销，也未与对比方法的计算成本比较。
- **安全假设的局限性**：假设初始状态全部安全（Assumption 1），但在复杂系统中此假设可能不成立（如初始状态本身可能因传感器噪声而不安全）。另外，假设分布偏移（train vs. deployment）可忽略（Section 3.4末尾），实际中可能不满足。
- **应用限制**：Phase 2中的专家引导方法依赖人类或模拟器，自动化邻域采样方法虽然模型无关，但可能因解码不准确导致状态无效，且样本效率较低（需大量邻域采样）。文中未探讨状态有效性的验证机制。

（完）
