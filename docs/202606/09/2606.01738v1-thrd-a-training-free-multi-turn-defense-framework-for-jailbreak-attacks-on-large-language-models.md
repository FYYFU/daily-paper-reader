---
title: "THRD: A Training-Free Multi-Turn Defense Framework for Jailbreak Attacks on Large Language Models"
title_zh: THRD：一种针对大语言模型越狱攻击的无训练多轮防御框架
authors: "Zhiqing Ma, Zhonghao Xu, Dong Yu, Chen Kang, Changliang Li, Pengyuan Liu"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.01738v1"
tags: ["query:ai-safety"]
score: 9.0
evidence: 面向大语言模型的多轮越狱攻击的无训练防御框架
tldr: "多轮越狱攻击利用对话动态逐步升级风险，现有防御依赖重训练或单轮分析，无法捕获轨迹风险积累。THRD提出首个无训练框架，通过时间演进评分机制整合瞬时风险评估、历史意图检测、响应评估等模块，将攻击成功率降至0.2-4.0%，且模型效用下降仅1.5%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01738v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1493, \"height\": 913, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01738v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 796, \"height\": 305, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01738v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 798, \"height\": 475, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01738v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 799, \"height\": 506, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01738v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1644, \"height\": 547, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01738v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 685, \"height\": 413, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01738v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 635, \"height\": 343, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01738v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 647, \"height\": 366, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01738v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 682, \"height\": 177, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01738v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 762, \"height\": 474, \"label\": \"Table\"}]"
motivation: 多轮对话中安全行为是轨迹依赖的，现有单轮分析无法捕获跨轮风险积累。
method: THRD包含Turn级风险评估、历史上下文分析、响应评估和决策模块，使用衰减调制和趋势感知的时间演进评分。
result: "在多种最前沿多轮攻击下，ASR降至0.2-4.0%，MMLU和GSM8K效用下降<1.5%。"
conclusion: 首次显式建模时间风险累积的多轮防御，有效且泛化。
---

## 摘要
多轮越狱攻击通过利用对话动态（如逐步升级和跨轮协作）对大语言模型构成日益严重的威胁。现有防御要么依赖代价高昂的重新训练（通常降低模型效用），要么在每一轮独立应用单轮分析，无法捕捉风险沿交互轨迹累积的过程。我们观察到多轮交互中的安全行为具有轨迹依赖性：对话历史不断重塑模型的条件上下文，使得孤立地评估每一轮变得不够充分。受此洞察启发，我们提出了THRD，这是第一个明确建模多轮越狱防御中时间风险累积的无训练框架。THRD集成了四个模块：用于即时风险估计的轮次风险评估器（TRA）、用于跨轮意图升级检测的历史上下文分析器（HCA）、用于识别助长性输出的响应评估器（RE），以及通过时间演化评分机制（结合衰减调制和趋势感知调整）组合这些信号的决策模块。针对最先进的多轮攻击（包括基于树搜索和多智能体协作方法）在两个目标模型上的实验表明，THRD将攻击成功率降至0.2%–4.0%，同时将模型效用在MMLU和GSM8K上的退化控制在1.5%以内。消融研究确认了模块的非冗余贡献和稳定的跨架构泛化能力。对首次拒绝触发点的分析显示，超过70%的多轮攻击需要在第2轮或之后才能检测到，验证了显式时间聚合的必要性。

## Abstract
Multi-turn jailbreak attacks pose a growing threat to LLMs by exploiting conversational dynamics such as gradual escalation and cross-turn coordination. Existing defenses either rely on costly retraining -- often degrading model utility -- or apply single-turn analysis independently at each turn, failing to capture how risk accumulates along interaction trajectories. We observe that safety behavior in multi-turn interaction is trajectory-dependent: dialogue history continuously reshapes the model's conditioning context, making it insufficient to evaluate each turn in isolation. Motivated by this insight, we present THRD, the first training-free framework that explicitly models temporal risk accumulation for multi-turn jailbreak defense. THRD integrates four modules: a Turn-level Risk Assessor (TRA) for instantaneous risk estimation, a Historical Context Analyzer (HCA) for cross-turn intent escalation detection, a Response Evaluator (RE) for identifying facilitative outputs, and a Decision Module that combines these signals through a time-evolving scoring mechanism with attenuation-based modulation and trend-aware adjustment. Experiments against state-of-the-art multi-turn attacks -- including tree-search-based and multi-agent collaborative methods -- across two target models show that THRD reduces ASR to 0.2--4.0% while preserving model utility within 1.5% degradation on MMLU and GSM8K. Ablation studies confirm non-redundant module contributions and stable cross-architecture generalization. Analysis of first rejection triggers reveals that over 70% of multi-turn attacks require Turn~2 or later to detect, validating the necessity of explicit temporal aggregation.

---

## 论文详细总结（自动生成）

# THRD：一种针对大语言模型多轮越狱攻击的无训练防御框架——详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：多轮越狱攻击（Multi-turn jailbreak attacks）利用对话的渐进升级和跨轮协调，逐步侵蚀大语言模型的安全防线。现有防御方法要么依赖代价高昂的重新训练（可能导致模型效用下降），要么在每轮独立应用单轮分析，无法捕获风险沿交互轨迹累积的过程。作者观察到多轮交互中的安全行为是**轨迹依赖的**：对话历史不断重塑模型的条件上下文，孤立评估每一轮是不够的。
- **核心问题**：如何在无需额外训练的情况下，显式建模跨轮时间风险累积，从而有效防御多轮越狱攻击？
- **整体含义**：提出首个无训练框架THRD，通过时间演进评分机制整合即时风险、历史意图和响应评估，将攻击成功率降至极低水平（0.2%–4.0%），同时保持模型效用退化在1.5%以内，为多轮安全防御提供了新范式。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
将多轮越狱防御建模为**时间风险聚合问题**，安全决策依赖于风险沿轮次的累积，而非孤立输入。THRD通过四个模块分解对话风险，并通过时间演化评分机制融合信号。

### 关键技术细节
- **框架组成**（图1）：
  - **Turn-level Risk Assessor (TRA)**：评估当前轮次的即时风险，输出整数分数 \( R_i \in [1,5] \)。采用两阶段分析（语义+结构），并设计了三个提示变体以减少过度拒绝。
  - **Historical Context Analyzer (HCA)**：分析所有历史对话，检测跨轮意图升级、策略协调和累积安全风险，输出二元判断 \( J_h \in \{\text{SAFE}, \text{UNSAFE}\} \)。采用多阶段框架（会话结构、历史意图、历史策略、历史安全分析）。
  - **Response Evaluator (RE)**：评估已生成的模型响应是否可能助长后续攻击（即使内容本身合规），输出 \( J_r \in \{\text{MAY\_FACILITATE}, \text{WILL\_NOT\_FACILITATE}\} \)。在中等风险或特定条件下触发，判断反馈给下一轮的决策模块。
  - **Decision Module**：整合TRA、HCA、RE信号，计算时间演进风险分数 \( F(i) \)，并映射到低/中/高三个风险等级，对应正常生成、受控生成、直接拒绝。一旦触发拒绝，后续所有轮次无条件拒绝（持久拒绝机制）。

- **评分公式**：
  \[
  F(i) = R_i + \Lambda(R_i) \cdot \Omega(\hat{J}_h^{(i-1)}, \hat{J}_r^{(i-1)})
  \]
  其中 \( \Lambda(R_i) = 1 - \frac{R_i - 1}{4} \) 为衰减函数；\( \Omega \) 为L2范数聚合（默认）或消融中的加权和/最大值/几何均值；同时引入趋势调整（连续三轮单调递增时加惩罚 \( +\delta \)）。

- **算法流程**：每轮依次执行TRA、HCA（分析前i-1轮）、RE（评估上一轮响应），决策模块计算 \( F(i) \)，根据阈值决定响应策略。

## 3. 实验设计

### 目标模型与推理基础设施
- **目标模型**：Qwen2.5-7B-Instruct、Llama-3-8B-Instruct（本地部署）
- **攻击模型**：Qwen3-32B（作为攻击者）
- **安全评判**：GPT-4o
- **硬件**：单张NVIDIA RTX 4090 GPU，使用SGLang推理框架。

### 攻击方法
- **多轮攻击**：X-Teaming（多智能体协作）、Tempest（广度优先树搜索，分支因子5）
- **单轮攻击**：AutoDAN（用于验证多轮防御不影响单轮鲁棒性）

### 基线方法
- **PROACT** 和 **SAGE**（原为单轮防御，实验中独立应用于每轮模拟多轮部署）

### 评估指标与数据集
- **安全**：攻击成功率（ASR）在HarmBench（Mazeika et al., 2024）和100个采样自AdvBench的指令上，使用GPT-4o作为安全评判。
- **效用**：MMLU（0-shot，11个选定领域）、GSM8K
- **过度拒绝率**：XSTest

### 实验设置
- 每个攻击方法在每种目标模型上重复，记录ASR、效用、过度拒绝率。
- 消融实验：移除各模块、移除持久拒绝、改变决策公式、混淆历史顺序、阈值敏感性分析、首轮拒绝触发分布统计。

## 4. 资源与算力

- **明确说明**：使用**单张NVIDIA RTX 4090 GPU**，通过SGLang本地部署目标模型。攻击模型Qwen3-32B和评判模型GPT-4o通过API调用（未指定具体算力）。
- **未说明训练时长**：由于THRD是无训练框架，训练开销为0；但推理延迟有报告：每轮总延迟约15.2秒（Qwen2.5-7B）和22.2秒（Llama3-8B），其中HCA为瓶颈。
- **算力充足性**：单卡4090足以完成所有实验，但未报告整体实验耗时。

## 5. 实验数量与充分性

- **实验组数**：
  - 主要防御性能表（表1）：2个目标模型 × 2个攻击 × 3种防御（包括无防御） = 12组，另有ASR在HarmBench和AdvBench两个子列。
  - 单轮攻击对比（表2）：2个模型 × 1个攻击 × 3种防御 = 6组。
  - 消融实验（表3）：7种配置（全框架、移除持久拒绝、移TRA/移HCA/移RE/移HCA&RE）。
  - 决策公式消融（表6）：4种Λ × 4种Ω = 16组（实际报告7种配置）。
  - 历史顺序敏感性（表5）：2种模型 × 2种顺序 = 4组。
  - 阈值敏感性分析（图3）：多阈值扫描。
  - 首轮拒绝触发分布（图2）：2个模型。
  - 还有风险分数分布、代价延迟测量等。
- **充分性**：
  - 优点：覆盖了当前最先进的多轮攻击（树搜索和协作方法），包含多个基线，做了全面的消融和灵敏度分析。
  - 不足：目标模型只有两个（7B/8B规模），未测试更大模型（如13B/70B）或不同架构（如Mistral、Falcon等）；攻击方法仅两种多轮，可能未覆盖所有变种（如CRESCENDO虽在相关工作中提到但未直接测试）；基线的单轮防御在每轮独立应用，可能不是最优配置；过度拒绝率在Llama上仍较高（18.8%），需要进一步改进。
- **客观性与公平性**：作者声明使用GPT-4o作为安全评判，并报告手动注释一致性高；但未公开评判prompt细节，可能存在评判偏差。对比方法（PROACT、SAGE）按原始论文配置实现，但未报告是否针对多轮微调（它们原为单轮设计），因此对比可能有利于THRD。

## 6. 论文的主要结论与发现

- **防御效果**：THRD在X-Teaming和Tempest上实现ASR 0.2%–4.0%，远优于基线（PROACT最高63.5%，SAGE最高86.2%），且模型效用下降<1.5%。
- **模块重要性**：消融实验表明TRA、HCA、RE均提供非冗余贡献，移除任一模块导致ASR显著上升（+15%至+25%）；同时移除HCA和RE最差（+31.71%）。
- **多轮检测的必要性**：超过70%的攻击在**第2轮或之后**才被首次拒绝（首轮检测率仅18%–29%），验证了显式时间聚合的必要性。
- **决策公式鲁棒性**：所有变体ASR均<2%，但“线性衰减+L2范数”在跨架构（Qwen/Llama）上表现最稳定。
- **历史顺序敏感**：打乱对话顺序后ASR上升（0.7%→1.3%），但绝对低值说明内容级信号也起作用。
- **阈值选择**：τ_low=2.0, τ_high=3.5为最佳平衡点。
- **单轮鲁棒性保持**：在AutoDAN上同样达到0% ASR。

## 7. 优点

### 方法优点
- **首次显式建模时间风险累积**：将多轮安全防御视为轨迹依赖问题，而非独立单轮，具有理论创新性。
- **无训练架构**：避免重新训练带来的效用损失和算力开销，易于部署和更新。
- **模块化设计**：TRA、HCA、RE各司其职，可独立替换或升级，便于未来扩展。
- **持久拒绝机制**：防止攻击者在拒绝后通过绕过性后续轮次恢复攻击，符合安全优先原则。
- **轻量决策公式**：使用简单的线性和L2范数组合，计算开销可忽略，且对参数不敏感。

### 实验亮点
- **全面消融**：验证了每个模块的非冗余贡献。
- **阈值和公式灵敏度分析**：确认了框架对不同参数的鲁棒性。
- **首轮拒绝触发分析**：提供了多轮攻击检测时机分布的有力证据。
- **历史顺序实验**：直观展示了时间序的重要性。

## 8. 不足与局限

### 实验与评估局限
- **目标模型规模有限**：仅测试7B/8B模型，未涵盖更大或更先进的模型（如Llama-3-70B、GPT-4等），泛化性需进一步验证。
- **攻击覆盖不全**：仅测试X-Teaming和Tempest两种多轮攻击，未包括Crescendo、ActorAttack、RACE等，也未测试自适应攻击（如针对THRD的对抗性设计）。
- **基线可能未优化**：PROACT和SAGE是单轮防御，独立应用在每个轮次可能不是最优多轮策略；未与最新多轮防御（如ARMOR）对比（ARMOR虽在HCA部分被引用但未作为整体基线）。
- **过度拒绝率仍有提升空间**：在Llama上为18.8%，高于无防御的16.0%，虽然作者归因于TRA对关键词的敏感，但未完全解决。
- **评判依赖GPT-4o**：可能引入偏差，且未公开评判prompt以复现。

### 方法局限
- **推理延迟较高**：每轮15–22秒（主要瓶颈HCA），不适合实时交互场景。虽然作者提到可用上下文压缩优化，但未实验验证。
- **依赖评估模型能力**：TRA、HCA、RE均基于提示词驱动的语义判断，若未来攻击针对这些判断模式，可能失效。
- **持久拒绝机制过于保守**：一旦拒绝即永久拒绝，对对话连续性有影响，可能不适合需要情境恢复的通用系统。
- **未处理长轨迹挑战**：当前多轮攻击最多7轮，但对于更长轨迹（如20轮以上），简单的时间聚合公式可能不够。
- **决策公式缺乏学习能力**：基于固定规则，无法自适应攻击演化。

### 应用限制
- **仅限文本对话**：不支持多模态输入。
- **需要额外安全模型调用**：每个请求需调用多次辅助模型（TRA/HCA/RE），增加API成本。
- **安全性优先级高于可用性**：对于需要低延迟平衡的应用，需要进一步优化。

（完）
