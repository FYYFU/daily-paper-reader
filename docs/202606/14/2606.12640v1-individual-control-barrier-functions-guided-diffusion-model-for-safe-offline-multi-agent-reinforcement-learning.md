---
title: Individual Control Barrier Functions-Guided Diffusion Model for Safe Offline Multi-Agent Reinforcement Learning
title_zh: 基于个体控制屏障函数的扩散模型用于安全离线多智能体强化学习
authors: "Qingyun Guo, Junyi Shi, Jianuo Huang, Tianyu Shi"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.12640v1"
tags: ["query:ai-safety"]
score: 9.0
evidence: 使用控制屏障函数和扩散模型的安全离线多智能体强化学习
tldr: 针对离线多智能体强化学习中的安全问题，现有方法多聚焦单智能体，本文提出将个体控制障碍函数嵌入扩散模型以约束轨迹生成，通过逆动力学恢复策略。实验表明该方法在多个基准上显著提升安全性，同时保持竞争力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.12640v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 901, \"height\": 514, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12640v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 689, \"height\": 374, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.12640v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1440, \"height\": 694, \"label\": \"Table\"}]"
motivation: 离线多智能体强化学习的安全挑战未被充分探索，现有方法无法有效保证多智能体轨迹安全性。
method: 将神经个体控制障碍函数作为扩散模型的条件，约束生成的轨迹满足安全约束，并通过逆动力学恢复控制策略。
result: 在多个多智能体基准上，所提算法在保证高回报的同时，显著降低了不安全事件发生次数。
conclusion: 个体控制障碍函数引导的扩散模型是提升离线多智能体强化学习安全性的有效途径。
---

## 摘要
离线强化学习允许直接从数据中学习控制策略而无需在线交互，因此适用于安全关键任务。最近的研究将扩散模型应用于离线强化学习，以利用其对复杂数据分布建模的强大能力。然而，现有方法主要关注单智能体场景，多智能体环境中的安全挑战尚未得到充分探索。本文提出一种安全的离线多智能体强化学习算法，将神经个体控制屏障函数嵌入扩散模型，以在轨迹生成过程中增强安全性，并通过逆动力学恢复控制策略。我们在多个基准上评估了算法，表明在保持具有竞争力的奖励的同时，显著提升了安全性。

## Abstract
Offline reinforcement learning allows control policies to be learned directly from data without online interaction, making it suitable for safety-critical tasks. Recent studies have applied diffusion models to offline reinforcement learning to leverage their strong capacity for modeling complex data distributions. However, existing approaches primarily focus on single-agent settings, leaving the safety challenges in multi-agent environments largely unexplored. In this work, we propose a safe offline multi-agent reinforcement learning algorithm that embeds neural individual control barrier functions into the diffusion model to enhance safety during trajectory generation, with control policies recovered through inverse dynamics. We evaluate our algorithm across diverse benchmarks, demonstrating substantial safety improvements while maintaining competitive rewards.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：离线多智能体强化学习（Offline MARL）中的安全性保障尚未被充分研究。现有安全离线强化学习方法主要针对单智能体场景，难以处理多智能体系统中的协调与碰撞避免问题；同时，现有方法依赖学习代价函数来定义可行域，在离线设置中易受外推误差影响，且难以适应复杂多智能体环境。
- **研究动机**：安全关键应用（如机器人、自动驾驶、车辆调度）要求控制策略同时满足高奖励和安全约束。在线强化学习成本高且存在风险，而离线学习可从静态数据集学习策略，但安全约束的引入面临挑战。扩散模型因其强大的数据分布建模能力而被用于离线强化学习，但现有方法缺乏对多智能体安全性的建模。
- **整体含义**：本文提出将个体控制屏障函数（Individual CBFs）嵌入扩散模型的轨迹生成过程，通过逆动力学模块恢复安全控制策略，实现安全离线多智能体强化学习。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将神经个体控制屏障函数（Neural Individual CBFs）的损失反向传播到多智能体扩散模型中，引导生成的轨迹向高奖励且满足个体安全集（Safe Set）的方向偏移，从而在不依赖在线交互和代价函数的情况下提升安全性。
- **关键技术细节**：
  - **多智能体扩散模型**：基于MADIFF框架，采用集中训练-分散执行（CTDE）范式。输入为每个智能体的特征剪枝后的全局状态与局部观察的拼接 ˜s_i，轨迹 τ_i = [˜s_i(t), ..., ˜s_i(t+H-1)]。扩散模型以奖励为条件，通过分类器自由引导生成高奖励轨迹。
  - **个体控制屏障函数学习**：为每个智能体i学习一个神经网络 h_{ω_i}，其损失函数包含三项：初始状态满足屏障条件、危险状态屏障值为负、安全状态下状态转移满足离散CBF条件（h_i(˜s_i(t+1)) ≥ α h_i(˜s_i(t))）。α采用时间松弛项 α = m - η_k_i，以避免过度保守。
  - **策略恢复**：通过逆动力学模块 f_φ_i 从生成的相邻状态对 (˜s_i(t), ˜s_i(t+1)) 推断动作 a_i(t)，损失为均方误差。
  - **整体算法流程**（Algorithm 1）：每次迭代从数据集抽取轨迹 → 扩散模型正向加噪 → 反向去噪（同时用扩散损失更新θ）→ 对每个智能体计算CBF损失并反向传播更新 ω_i 和 θ → 更新逆动力学 φ_i → 生成状态并计算动作。
- **关键公式**：
  - 扩散损失：L_D(θ) = E[∥ϵ - ϵ_θ(τ^k, (1-β)r+β∅, k)∥²]
  - CBF损失：L_C_i(ω_i, θ) = Σ_{初始} max(0, δ - h_{ω_i}(˜s_θ_i)) + Σ_{危险} max(0, δ + h_{ω_i}(˜s_θ_i)) + Σ_{安全} max(0, δ - h_{ω_i}(˜s_θ_i(t+1)) + α h_{ω_i}(˜s_θ_i(t)))
  - 逆动力学损失：L_P_i(φ_i) = E[∥a_i - f_{φ_i}(˜s_i(t), ˜s_i(t+1))∥²]

### 3. 实验设计：使用的数据集/场景、benchmark、对比方法

- **环境与数据集**：
  - **Simple Spread**（低维）：PettingZoo中的多智能体覆盖任务，数据集来自Pan et al. (2021)，包含Expert、Medium、Medium-Replay、Random四种质量级别。
  - **Safe Multi-Agent MuJoCo (Safe MAMUJOCO)**（高维）：三个配置：2x3 HalfCheetah、2x4 Ant、4x2 Ant。数据集来自Off-the-Grid offline MARL数据集，包含Good、Medium、Poor三种质量。
- **基准对比方法**：
  - **MA-TD3BC**：多智能体版本的TD3+Behavior Cloning。
  - **OMAR**：基于局部修正的离线多智能体强化学习方法。
  - 由于缺乏现成的安全离线MARL基线，采用这两种离线MARL算法作为对比。
- **评估指标**：在线部署时的回合奖励（越高越好）和回合代价（越低越好）。代价和奖励均进行了归一化处理。

### 4. 资源与算力

- 论文中没有明确说明使用的GPU型号、数量或训练时长。仅提及在“Aalto Science-IT”计算资源上完成，未提供具体算力细节。

### 5. 实验数量与充分性

- **实验数量**：在2个环境的共7个数据集（Simple Spread的4个 + Safe MAMUJOCO的3个配置各有3个质量等级）上进行，每个实验使用5个随机种子。共约7*5=35组主要实验结果。
- **充分性**：覆盖低维（Simple Spread）和高维（MuJoCo）场景，数据集质量从随机到专家，具有一定的全面性。但未包含消融实验（如CBF引导有无的比较、不同α方案等），也未与安全离线MARL的直接基线（如SafeDiffuser或FISOR的多智能体扩展）对比（因为尚无此类方法）。实验设计基本客观公平，使用相同归一化参数和随机种子。

### 6. 论文的主要结论与发现

- 所提算法在几乎所有任务上取得了最低的代价（即最高的安全性），同时保持具有竞争力的奖励。
- 在Simple Spread上，在Medium、Medium-Replay、Random数据集上实现了零碰撞（代价为0）；在2ant-*等任务上，奖励和代价均优于基线。
- 对比方法MA-TD3BC在高质量数据集上表现好，但随数据集质量下降奖励骤降；OMAR易陷入局部最优，奖励较低。本文方法在奖励和安全性之间取得了更好的平衡。
- 证实了神经个体CBF引导能有效将安全约束注入扩散模型生成的轨迹，避免使用代价函数带来的外推误差。

### 7. 优点：方法或实验设计上的亮点

- **方法创新**：首次将个体控制屏障函数与扩散模型结合用于安全离线多智能体强化学习，无需在线交互和显式代价函数，避免了外推误差。
- **理论基础**：利用CBF的离散时间条件，保证安全集前向不变性；通过个体CBF的局部安全性推理全局安全（引理3.1）。
- **实用技巧**：采用特征剪枝的个体全局状态 ˜s_i 兼顾协调与个体性；引入时间松弛的α避免过度保守。
- **实验展示**：在多种质量数据集上验证了方法的鲁棒性，奖励和代价均有明确报告，且使用5个种子统计均值和标准差。

### 8. 不足与局限

- **实验覆盖有限**：仅测试了Simple Spread和Safe MAMUJOCO两个环境，且Safe MAMUJOCO为速度约束，碰撞约束等其他类型安全性未覆盖。缺乏更复杂的安全约束（如多机器人避碰、编队保持等）。
- **缺乏消融实验**：未单独分析CBF引导的效果、α松弛因子的贡献、特征剪枝的作用等，难以量化各组件贡献。
- **对比基线不全**：由于领域空白，未与直接的安全离线多智能体方法（如基于安全约束策略优化或验证的方法）对比；仅与无安全意识的离线MARL基线比较，说服力有限。
- **未讨论外推风险**：尽管声称避免了代价函数的外推误差，但扩散模型本身可能产生分布外状态，导致逆动力学推理动作不可行，论文对闭环安全验证未涉及。
- **计算资源未报告**：缺乏可复现的算力信息。

（完）
