---
title: "Weak Critics Make Strong Learners: On-Policy Critique Distillation for Scalable Oversight"
title_zh: 弱批评者造就强学习者：基于策略的批评蒸馏实现可扩展监督
authors: "Can Jin, Jiakang Li, Rui Wu, Eddy Zhang, Dimitris N. Metaxas"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2606.00424v1"
tags: ["query:opd"]
score: 9.0
evidence: 在线策略批评蒸馏用于可扩展监督
tldr: 弱监督者难以提供可靠标签，本文提出用弱模型作为批评者，仅提供修正方向。通过渐进式在线批评蒸馏（OPCD）过滤高质量批评，并自适应蒸馏到强模型。实验表明该方法在推理和alignment任务上持续提升强模型性能，为可扩展监督提供了有效路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00424v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 869, \"height\": 982}, {\"url\": \"assets/figures/arxiv/2606.00424v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1719, \"height\": 456}, {\"url\": \"assets/figures/arxiv/2606.00424v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1784, \"height\": 834}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.00424v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1708, \"height\": 725}, {\"url\": \"assets/tables/arxiv/2606.00424v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 775, \"height\": 979}, {\"url\": \"assets/tables/arxiv/2606.00424v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 685, \"height\": 446}]"
motivation: 强模型能力提升后，弱监督者无法提供可靠标签或判断，需探索更易处理的弱监督形式。
method: 提出OPCD，用弱批评者提供非误导修正方向，通过在线过滤和自适应自教师蒸馏到强模型。
result: 在推理和alignment基准上，强模型随训练轮次改进，验证了弱批评监督的有效性。
conclusion: 弱批评者监督结合OPCD可推动可扩展监督，实现弱到强泛化。
---

## 摘要
随着大语言模型变得更强，弱监督者可能无法为复杂输出提供可靠的标签、偏好或最终判断，从而限制了弱到强的泛化和可扩展监督。我们研究了一种更易处理的弱监督形式：将弱模型用作批评者而非标注者或评判者。弱批评者不需要解决任务或选择正确答案，只需提供非误导性的修订方向，帮助强模型更好地利用自身知识。我们将这种设置称为*弱批评者强监督*。我们首先证明，弱批评可以在推理时改进冻结的强模型，而批评质量是这种改进的关键。然后，我们提出渐进式同策略批评蒸馏（**OPCD**），该方法过滤高质量批评，并通过自适应自教师信号将批评引导的行为蒸馏到强模型中。在推理和对齐基准上的实验表明，我们的方法在训练轮次中改进了强模型，为利用弱监督实现可扩展监督提供了一条有效路径。

## Abstract
As large language models become stronger, weak supervisors may fail to provide reliable labels, preferences, or final judgments for complex outputs, limiting both weak-to-strong generalization and scalable oversight. We study a more tractable form of weak supervision: using a weak model as a critic rather than as a labeler or judge. Instead of solving the task or selecting the correct answer, the weak critic only needs to provide a non-misleading revision direction that helps the strong model better use its own knowledge. We call this setting *weak-critic strong oversight*. We first show that weak critiques can improve frozen strong models at inference time, and that critique quality is key to this improvement. We then propose progressive on-policy critique distillation (**OPCD**), which filters high-quality critiques and distills critic-guided behavior into the strong model through adaptive self-teacher signals. Experiments on reasoning and alignment benchmarks show that our method improves strong models over training epochs, suggesting an effective path for scalable oversight with weak supervision.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：随着大语言模型（LLMs）能力不断增强，人类或较弱模型作为监督者时，难以对复杂输出（如推理、代码、证明）提供可靠的标签、偏好或最终判断，这限制了弱到强泛化（weak-to-strong generalization）和可扩展监督（scalable oversight）的发展。
- **研究动机**：现有方法要求弱监督者提供完整答案或偏好标签，这超出了弱监督者的能力范围，导致信号噪声大或系统错误。作者提出一种更易处理的弱监督形式：让弱模型担任“批评者”（critic），而非标注者或评判者。弱批评者只需给出非误导性的修订方向（如“推理不完整”“检查边界情况”等），帮助强模型更好地利用自身知识，而无需解决完整任务。
- **整体含义**：本文探索“弱批评者强监督”（weak-critic strong oversight）范式，旨在通过批评方式实现可扩展监督，为弱到强泛化提供可行路径。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：使用弱模型作为批评者，对强模型生成的回答提供修订方向，然后通过渐进式同策略批评蒸馏（On-Policy Critique Distillation, **OPCD**）将批评引导的行为内化到强模型中，使得强模型在测试时无需批评即可改进输出。
- **关键技术细节**：
  - **推理时验证**：强模型先生成初始答案，弱模型对其做批评，强模型再根据批评修正答案。公式表示为：\( a_i = S(x),\ c_i = W(x,a_i),\ r_i = S(x,a_i,c_i) \)。
  - **批评生成与过滤**：在每个训练轮次，强模型生成同策略（on-policy）答案，弱模型给出批评。通过两步过滤保留高质量批评：
    1. **结果检查** (\(r_{out}\))：修正后的答案是否正确。
    2. **准则检查** (\(r_{rub}\))：批评是否相关且有用。
    只有同时通过的批评 (\(h_i = r_{out} \cdot r_{rub}\)) 才用于训练。
  - **OPCD训练目标**：使用批评条件化的强模型（冻结）作为自教师（self-teacher），学生（可训练）不含批评。最小化逐令牌KL散度：
    \[
    \mathcal{L}_{\text{OPCD}} = \frac{1}{|S_e|} \sum_{(x,y,f) \in S_e} \sum_{t=1}^{|y|} \mathrm{KL}\left[ \pi_\theta(\cdot|x,y_{<t}) \ \big\| \ \text{stopgrad}[\pi_\theta(\cdot|x,f,y_{<t})] \right]
    \]
  - **多样性**：同策略生成保证批评适应当前强模型错误模式，随训练轮次动态更新。
- **算法流程**（文字描述）：
  1. 初始化强模型 \(\pi_{\theta_0}\)，弱批评者 \(\pi_w\)。
  2. 对每个轮次 \(e\)，从当前强模型采样 \(G\) 个回答，弱批评者给出批评。
  3. 对于每个三元组，生成精炼答案并进行过滤，只保留高质量批评。
  4. 在过滤后的集合 \(S_e\) 上优化 \(\mathcal{L}_{\text{OPCD}}\)，更新强模型。
  5. 重复直到达到指定轮次。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **推理场景**（reasoning）：
  - 弱模型：Qwen3-1.7B-base；强模型：Qwen3-4B-base。
  - 数据集：GPQA main split（排除Diamond），训练129题，验证121题（非Diamond子集）。
  - Benchmark：AIME 2024 & 2025（共60题，用于推理时测试）。
- **对齐场景**（alignment）：
  - 弱模型：Phi-4-mini-instruct（经SFT微调）；强模型：Phi-4-14B。
  - 数据集：Magpie对齐语料（IFEval风格），训练2500条，验证使用IFEval测试集。
- **评估指标**：pass@k（k=1,2,4,8,16）和mean@8（8个采样答案的平均准确率）。
- **对比方法**：
  - 推理时实验：S only（强模型独立采样）、S+W critic+refine、S+best/random/worst critic+refine（构造高质量/随机/低质量批评）。
  - 训练时实验：基线为初始强模型（未训练）的性能，OPCD训练过程中随步骤报指标，无其他外部对比方法（如直接SFT弱模型答案等未在正文中比较，但论文在引言中提及与标准弱到强方法对比的思想）。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。

- 论文中**未明确说明**使用的GPU型号、数量或训练时长。仅在超参数部分提及“训练批量大小为128，PPO mini-batch size为32，micro-batch size per GPU为1”，暗示使用了多GPU配置，但具体算力资源未报告。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平。

- **推理时实验**：
  - 覆盖2个任务（GPQA Diamond和IFEval），使用非思考模型（Phi-4系列）和思考模型（Qwen3系列，AIME）。
  - 包含了批评质量消融（best/random/worst），以及不同预算（k=1,2,4,8,16）的scaling分析。
  - 实验较充分，验证了弱批评的有效性和批评质量的关键性。
- **训练时实验**：
  - 针对两个场景：对齐（IFEval）和推理（GPQA non-Diamond）。
  - 报告了训练过程中多个步骤的mean@8和pass@8，以及loss和训练分数变化。
  - 但缺少与其他弱监督方法（如直接蒸馏弱模型logits、仅结果检查等）的横向对比，仅以初始模型为基线，消融也不够全面（如无过滤对比、不同批评模型对比）。
- **总体**：实验设计合理，但对比基线较少，消融实验仅涉及推理时批评质量，未在训练时系统消融过滤机制的影响。在多任务、多模型上验证了有效性，但样本量（如AIME仅60题）较小。

### 6. 论文的主要结论与发现

- **弱批评者可在推理时提升强模型性能**：即使弱模型本身能力弱，其批评也能帮助强模型（包括思考模型）提升准确率，且改进随采样预算增加而保持。
- **批评质量至关重要**：高质量批评带来显著提升，低质量批评甚至可能损害性能。
- **OPCD训练持续改进强模型**：在对齐和推理任务上，OPCD训练后强模型的mean@8和pass@8均超过初始值，表明批评引导的蒸馏有效。
- **不同任务训练动态差异**：对齐任务训练更稳定（loss下降），推理任务变化更大，说明推理场景中弱批评提供的监督噪声更高。

### 7. 优点：方法或实验设计上有哪些亮点

- **创新性弱监督形式**：将弱模型角色从标签/偏好提供者转变为批评者，降低了对弱监督者的能力要求，更贴近实际人机交互。
- **过滤机制**：结合结果检查与准则检查，保证了用于训练的批评质量，避免误导性信号。
- **同策略蒸馏**：批评随强模型进化而更新，自适应性强，解决了静态批评不适配的问题。
- **推理时和训练时统一验证**：先验证推理时可行性，再设计训练方法，逻辑自洽。
- **跨任务、跨模型系列验证**：涵盖了推理和对齐，非思考与思考模型，提升了结论的泛化性。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **对比实验不足**：训练时仅与初始模型对比，缺少与其他弱监督方法（如直接SFT弱模型答案、仅结果蒸馏等）的对比，削弱了方法领先性的说服力。
- **消融不完整**：未分离分析过滤机制、同策略生成、教师/学生结构等各组件的贡献；准则检查的具体实现（如是否人工判断）未透明说明，可能引入主观偏差。
- **算力与复现**：未报告完整训练配置（GPU数量、时间），不利于复现和评估效率。
- **泛化性局限**：仅在特定模型对（Phi-4系列、Qwen3系列）和有限数据集（GPQA non-Diamond 250题、IFEval 2500条）上实验，在更大规模、更复杂任务（如代码、数学证明）上的效果未知。
- **应用限制**：弱批评者需要生成与任务相关的批评，若弱模型完全不理解任务（如领域外问题），可能无法提供有用批评。且训练依赖outcome check，对无法自动判题的任务（如开放生成）不适用。

（完）
