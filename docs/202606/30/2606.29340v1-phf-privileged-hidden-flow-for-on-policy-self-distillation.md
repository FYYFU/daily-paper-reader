---
title: "PHF: Privileged Hidden Flow for On-Policy Self-Distillation"
title_zh: PHF：面向在线策略自蒸馏的特权隐藏流
authors: "Yuhan Li, Mingxu Zhang, Dazhong Shen, Ying Sun"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29340v1"
tags: ["query:opd"]
score: 10.0
evidence: 在线策略自蒸馏，融入特权隐藏流
tldr: 现有在线自我蒸馏仅监督输出分布，未直接指导内部计算。本文提出特权隐藏流（PHF），额外蒸馏教师模型隐藏状态的转移方向与轨迹几何，而非逐点匹配。在Qwen3-1.7B/4B/8B上均提升约1.5-2.2个点。PHF作为紧凑的隐藏流扩展，对共享偏移和正交变换具有不变性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29340v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1873, \"height\": 1154, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29340v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1901, \"height\": 565, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29340v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1343, \"height\": 729, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29340v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 886, \"height\": 524, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29340v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 846, \"height\": 797, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29340v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 843, \"height\": 901, \"label\": \"Table\"}]"
motivation: 现有在线自我蒸馏只约束输出分布，缺少对模型内部计算过程的直接监督。
method: PHF对齐师生模型在相同滚动轨迹上的隐藏状态转移方向和轨迹几何，避免逐点匹配。
result: "在Qwen3-1.7B/4B/8B上，Average@12比在线自我蒸馏基线分别提升约2.2、1.5和1.7点。"
conclusion: PHF是一种紧凑、有效的隐藏流扩展，在不增加计算开销的前提下提升在线自我蒸馏性能。
---

## 摘要
在线策略自蒸馏（OPSD）通过匹配也能看到已验证参考解决方案的特权教师，在从其自身策略采样的轨迹上训练推理模型。现有的OPSD目标仅监督输出分布，因此特权上下文通过词元级别的散度影响训练，而不直接监督产生该分布的内部计算。我们提出了特权隐藏流（PHF），它额外蒸馏了特权教师的隐藏状态沿同一轨迹移动的方式。PHF并非强制每个学生隐藏向量与同一词元位置的教师向量匹配，而是在选定的生成位置上对齐词元到词元的转移方向和轨迹几何。全层方案还包括从这些相同转移计算的相邻层关系，无需逐点隐藏状态模仿。在相同的100步训练计划下，PHF在我们复现的OPSD基线基础上，改进了Qwen3-1.7B、4B和8B上的Average@12聚合指标，观察到的增益分别约为+2.2、+1.5和+1.7分。传输目标对共享轨迹偏移是精确不变的；其局部几何项也对转移方向的正交变换保持不变。消融实验将固定的PHF方案与逐点隐藏状态匹配、单通道转移损失和层子集选择区分开来，支持PHF作为OPSD的紧凑隐藏流扩展。

## Abstract
On-policy self-distillation (OPSD) trains a reasoning model on rollouts sampled from its own policy by matching a privileged teacher that also sees verified reference solutions. Existing OPSD objectives supervise only the output distribution, so privileged context affects training through a token-level divergence without directly supervising the internal computation that produced that distribution. We propose Privileged Hidden Flow (PHF), which additionally distills how a privileged teacher's hidden states move along the same rollout. Rather than forcing each student hidden vector to match the teacher vector at the same token position, PHF aligns token-to-token transition directions and trajectory geometry over selected generated positions. The all-layer recipe also includes an adjacent-layer relation computed from these same transitions, without pointwise hidden-state imitation. Under the same 100-step training schedule, PHF improves the Average@12 aggregate over our reproduced OPSD baseline on Qwen3-1.7B, 4B, and 8B, with observed gains of about +2.2, +1.5, and +1.7 points. The transport objective is exactly invariant to shared trajectory offsets; its local geometry term is also invariant to orthogonal transformations of transition directions. Ablations distinguish the fixed PHF recipe from pointwise hidden-state matching, single-channel transition losses, and layer-subset choices, supporting PHF as a compact hidden-flow extension to OPSD.

---

## 论文详细总结（自动生成）

# PHF：面向在线策略自蒸馏的特权隐藏流 - 论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **现有问题**：在线策略自蒸馏（OPSD）通过匹配一个也看到参考解的特权教师来训练模型，但只监督输出分布（token级散度），没有直接监督产生该输出的内部计算过程（隐藏状态轨迹）。
- **研究动机**：如何更有效地利用训练时的特权信息（参考解），不仅约束输出，还约束模型内部的隐藏状态移动过程，从而提升推理能力。
- **整体含义**：提出特权隐藏流（PHF），在OPSD基础上额外蒸馏教师隐藏状态的转移方向和轨迹几何，使学生在不增加推理成本的情况下，更全面地模仿教师的内部推理过程。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：对齐学生与特权教师在同一滚动轨迹上的**隐藏状态转移（transition）**，而非逐点匹配隐藏向量。转移定义为相邻选定生成位置之间隐藏状态的变化，并归一化为单位方向。
- **技术细节**：
  - **方向损失 L_dir**：学生与教师归一化转移方向的余弦相似度补（1 - cosine），对齐局部移动方向。
  - **几何损失 L_geo**：比较转移方向集的Gram矩阵（刻画转移间的平行/正交关系）的Frobenius距离，对齐轨迹的整体几何结构。
  - **每层损失 L_layer** = (L_dir + L_geo)/2，所有层平均得 L_local。
  - **相邻层关系损失 L_adj**：匹配相邻层之间转移方向矩阵的Gram矩阵（即layer ℓ 与 layer ℓ+1 的关系），保持深度维度的一致性。
  - **整体隐藏流损失 L_Flow** = 0.5*L_local + 0.5*L_adj。
  - **总目标**：L_PHF = L_OPSD + α L_Flow，α=0.05。
  - **特权教师**：EMA在线模型（ρ=0.999），条件化在参考解上，训练时使用，推理时不用。
  - **不变性**：对共享轨迹偏移、标量缩放后归一化、随机正交变换（几何项）具有代数不变性，增强稳定性。

## 3. 实验设计：数据集、Benchmark、对比方法
- **数据集**：
  - 训练：OPSD官方数学训练集（29,434个示例），学生滚动长度1024。
  - 评估：AIME 2024、AIME 2025、HMMT 2025（各30题），使用thinking-mode协议，每个问题采样12次（n=12），报告Average@12（平均准确率），聚合为Average列。
- **模型**：Qwen3-1.7B（28层）、Qwen3-4B（36层）、Qwen3-8B（36层）。
- **对比方法**：
  - 主要基线：复现的OPSD（输出损失，相同训练计划）。
  - 上下文行：Base（未训练）、SFT（监督微调）、GRPO（组相对策略优化）。
  - 消融变体：方向only、几何only、选定层（selected layers）。
- **训练细节**：LoRA（rank=64, α=128），100优化步，温度1.1，JSD token clip 0.05/0.06（依规模），学习率5e-6余弦退火，有效batch 32。

## 4. 资源与算力
- **硬件**：单节点 8×NVIDIA H20 GPU（每卡143GB显存）。
- **总开销**：约600 GPU-hours（包括主实验和消融实验）。
- **训练**：每个实验使用相同的硬件配置，未明确给出单次训练时间。

## 5. 实验数量与充分性
- **主实验**：3个模型规模 × 3个数据集（9个指标）+ 聚合Average，共12个数值。每个规模下PHF vs OPSD的直接比较。
- **消融实验**：在全部3个规模上测试了方向only、几何only、选定层与完整PHF比较（Table 2），共4×3=12组。
- **检查点动态**：Figure 2展示AIME 2024上OPSD和PHF的训练曲线，证明非单调性但最终一致正收益。
- **充分性评价**：
  - 实验设计干净、可控：PHF仅加一个损失项，不改变滚动分布、评价协议、优化器计划。
  - 跨规模一致提升表明方法稳健。
  - 但局限：仅在一个模型家族（Qwen3）、一个训练集（数学推理）、一个检查点（step 100）下测试，缺乏其他任务和更长时间训练的验证。

## 6. 论文的主要结论与发现
- PHF在三个规模上一致改进OPSD基线：Average@12分别提升+2.2（1.7B）、+1.5（4B）、+1.7（8B）点。
- 方向only和几何only都不能达到完整PHF的性能，说明两者互补。
- 选定层方法不如全层方法，表明隐藏过程信号分布在各层。
- PHF不增加推理开销，只增加训练时一次特权前向传递和隐藏状态存储。
- 关键贡献：将过程级监督引入OPSD，且具有理论不变性，更稳定。

## 7. 优点
- **方法创新**：使用隐藏转移而非点对点状态匹配，具有偏移和正交不变性，更适应表示漂移。
- **实验严谨**：在OPSD框架下增量添加，控制变量干净；跨模型规模且消融充分。
- **计算友好**：不引入奖励模型、正确性路由、额外滚动集，只增加一个前向传播和有限存储。
- **验证普遍性**：在多个规模和多个数据集上获得一致提升，说明方法稳健。

## 8. 不足与局限
- **依赖参考解**：继承OPSD的特权假设，若参考解质量差则方法失效。
- **存储限制**：固定128位置窗口，更长滚动需要更多内存；未尝试自适应预算。
- **评估覆盖面窄**：仅Qwen3模型家族和数学推理任务；未测试其他领域（如代码、一般推理）或其他模型。
- **训练深度有限**：只使用100步检查点，未探索更长时间训练下的收益趋势。
- **评价指标单一**：Average@12只衡量正确率，未评估推理质量、证明结构、鲁棒性等。
- **未研究适应性**：未对层选择或位置选择进行更深入的分析或自适应机制。

（完）
