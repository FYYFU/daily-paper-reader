---
title: Agent Safety Is Action Alignment
title_zh: 智能体安全即行为对齐
authors: "Shawn Li, Yue Zhao"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.28739v1"
tags: ["query:ai-safety"]
score: 9.0
evidence: 论证智能体安全是行为对齐而非内容安全
tldr: 传统内容安全通过拒绝不安全输入来保护模型输出，但这种范式不适用于智能体安全。本文提出行动对齐的概念，认为智能体安全在于行动与用户授权的关系，而非输出文本。实验表明，防御训练只会让模型学习表面模式，导致多步代理崩溃且仍可被利用，而未防御模型在常规使用中也经常超出授权。结论是行动安全不能植入模型权重，必须在行动边界外强制执行最小权限原则，并基于部署条件的关系属性进行评估。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28739v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1713, \"height\": 780, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28739v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1388, \"height\": 205, \"label\": \"Table\"}]"
motivation: 现有内容安全方法直接用于智能体安全是范畴错误，导致性能下降且无安全增益。
method: 提出行动对齐框架，将安全定义为行动与用户授权的关系，在模型外以最小权限原则强制执行。
result: 防御训练学习表面模式，多步代理崩溃且可被利用；未防御模型在常规使用中超出授权。
conclusion: 行动安全不能通过权重实现，必须在行动边界外强制最小权限，评估行动对齐而非拒绝分数。
---

## 摘要
大语言模型越来越多地充当智能体：它们调用工具、转移资金、删除记录以及代表用户发送消息。为了确保安全，从业者将聊天机器人时代的做法（训练模型拒绝不安全输入）引入智能体场景，并将由此导致的能力损失视为可管理的“对齐代价”。我们认为这是一个“范畴错误”。拒绝是“内容安全”的原始机制，其危害在于模型的输出，因此是输出的可学习函数。智能体危害在性质上不同：它不在于任何输出，而在于行为所行使的权限与用户授予的权限之间的关系，这超出了模型所看到的文本范围。将内容安全方法引入这一领域并不是用能力换取安全；而是付出了能力却买来了负面安全。我们通过三条跨越自主性光谱的证据来支持这一点：经过防御训练的模型学习的是表面模式而非意图；同样的训练在威胁出现之前就破坏了多步智能体，同时使它们仍可被利用；即使是未防御的前沿模型在常规使用中也会超出授予的权限。我们得出结论：行为安全无法嵌入模型权重中。它必须被表达为“最小权限”，在模型外部的行为边界上强制执行，并作为“行为对齐”（一种关系的、依赖于部署条件的属性）而非拒绝分数来评估。

## Abstract
Large language models increasingly act as agents: they call tools, move money, delete records, and send messages on a user's behalf. To keep them safe, practitioners imported the chatbot-era recipe (train the model to refuse unsafe inputs) into the agentic setting, and treat the resulting capability loss as a manageable ``alignment tax.'' We argue this is a \emph{category error}. Refusal is a primitive for \emph{content safety}, where the harm is in the model's output and is therefore a learnable function of it. Agentic harm is different in kind: it lies not in any output but in the relation between the authority an action exercises and the authority the user granted, which is absent from the text the model sees. Importing content-safety methods into this regime does not trade capability for safety; it pays capability and buys negative security. We support this with three lines of evidence spanning the autonomy spectrum: defense-trained models learn surface patterns rather than intent; the same training collapses multi-step agents before any threat appears while leaving them exploitable; and even undefended frontier models exceed granted authority under ordinary use. We conclude that action safety cannot be installed in weights. It must be expressed as \emph{least privilege}, enforced \emph{outside} the model at the action boundary, and evaluated as \emph{action alignment} (a relational, deployment-conditioned property) rather than a refusal score.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题来源**：大语言模型（LLM）越来越多地作为智能体执行操作（调用工具、转账、删除记录等），从业者将聊天机器人时代的“拒绝不安全输入”的方法直接移植到智能体安全，并认为能力损失是可接受的“对齐代价”。
- **核心论点**：论文指出这是**范畴错误（category error）**。内容安全（content safety）中危害在于模型输出（如有毒文本），因此“拒绝输出”是合理的工具；而智能体危害（action harm）不在于任何输出，而在于行动所行使的权限与用户授予权限之间的关系（即权限关系）。这种关系不在模型看到的文本中，因此内容安全方法无法解决行动安全问题。
- **整体含义**：将内容安全方法用于智能体安全，不是用能力换安全，而是付出能力却买来**负面安全**（negative security）——即防御训练后的模型可能比未防御模型更不安全。因此必须重新定义智能体安全，并提出新的范式。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- **行动安全 ≠ 内容安全**：安全不再由“是否拒绝”决定，而由行动是否满足三个坐标决定：能力（Competence）、约束（Restraint）、抵抗（Resistance）。这三者共同构成**行动对齐（Action Alignment, S★ = C ∧ R ∧ B）**。
- **安全不能植入权重**：因为意图和授予权限（A）不在模型输入中，所以任何基于输入-输出目标的训练只能学习表面模式，无法学到真实意图。
- **三个转变**：
  1. **用最小权限（Least Privilege）取代拒绝作为基本安全原语**：安全目标是“在授权范围内完成任务”，拒绝只是边界情况（空范围）。
  2. **在模型外部、行动边界强制执行**：使用外部参考监视器（external reference monitor）在工具调用时检查行动是否匹配授权和来源，从而避免快捷学习。
  3. **关系性、部署条件化的评估**：安全度量应分别报告C、R、B三个坐标，而不是单一拒绝分数；评估需覆盖多步级联和日常非对抗性模糊场景。

### 关键技术细节
- 形式化定义了三个坐标：
  - 能力 C: 行动是否完成用户真实意图。
  - 约束 R: 行动使用的权限auth(a)是否 ⊆ 授予权限A。
  - 抵抗 B: 行动是否不受对抗性控制（注入）。
- 指出当前主流安全评分 S_field = (refuse correct) 只能反映拒绝/服从一维信息，完全忽略R，并将C和B混为一谈。
- 提出两个可证伪主张：
  - 主张1：安全训练是非单调的（干预可能降低安全性）。
  - 主张2：行为性行动安全难以学习（因意图和权限不在输入中）。

## 3. 实验设计

- **论文本身是观点/立场论文**，没有进行全新实验，而是整合了作者团队先前发表的三项研究作为证据。这三项研究分别对应自主性光谱的不同台阶：
  1. **单轮防御（Single-turn defense）**：引用[19]（Li et al., 2026），显示防御训练模型学到表面形式（如一个触发词使假拒绝率上升50%），无法泛化到真实恶意意图。
  2. **多步智能体（Multi-step agent）**：引用[21]（Li and Zhao, 2026），显示防御模型在第一步失败率高达47-77%（vs基线模型3%），且单次假拒绝会导致级联超时（从13%上升到99%），同时仍易受缺乏关键词的攻击。
  3. **工具/技能智能体（Tool/Skill agent）**：引用[20]（Li et al., 2026），即使未防御的前沿模型，在常规非对抗任务中也会倾向于超出授权（小于15%的请求满足最小权限模式；强的模型在超过1/3的调用中错误路由）。
- **基准（Benchmark）**：每个研究使用内部或标准注入基准，但论文未详细列出名称。
- **对比方法**：防御训练模型 vs 未防御基础模型；不同防御方法（如结构化查询训练、偏好优化等）间的比较。

## 4. 资源与算力

- **论文未明确说明**使用了何种GPU、数量、训练时长等计算资源。各项实验的算力细节见于引用的原始论文，本论文仅作总结和推理。因此读者需要参考[19]、[20]、[21]获取具体资源信息。

## 5. 实验数量与充分性

- **实验数量**：论文未进行新实验，而是基于作者团队的三组已发表研究（覆盖三个自主性层级）。每组研究都包含多组子实验（如不同防御方法、不同模型、不同攻击类型等），但论文仅提供关键数字作为论据。
- **充分性**：作为立场论文，这些证据作为支撑观点是充分的，但客观上实验覆盖有限（均来自同一团队，依赖特定数据集和模型）。论文的结论主要基于推理和几个案例，缺乏大规模、第三方复现的验证。实验在论证“范畴错误”上有效，但在提供替代方案的实证效果方面仅给出概念框架。

## 6. 论文的主要结论与发现

1. **内容安全范式（拒绝）不适用于智能体安全**：行动安全本质上是关系性属性（权限关系），不包含在输出文本中，因此无法通过拒绝训练有效解决。
2. **防御训练导致负安全**：在单轮、多步、工具使用三种场景下，防御训练都会降低模型能力、引入新的脆弱性，甚至使未防御模型原有的判断消失。
3. **行动安全必须在模型外部强制执行**：通过最小权限原则、外部参考监视器，在行动边界检查权限和来源，而不是依靠模型内部权重。
4. **评估必须转向关系性多坐标度量**：拒绝分数掩盖了真实安全风险，应该分别报告能力、约束、抵抗三个坐标，并在真实部署条件下评估（包括级联失败和日常模糊性）。

## 7. 优点

- **清晰的概念区分**：首次明确指出内容安全与行动安全在危害位置上的本质区别，为领域提供正确的靶心。
- **有力的逻辑论证**：通过形式化定义（S★ 三坐标）和可证伪主张，使论点具批判性可检验性。
- **系统化证据组织**：将已有零散发现（快捷学习、级联失败、权限漂移）统一解释为同一“范畴错误”的推论，增强说服力。
- **具体可行方向**：提出最小权限、外部执行、关系评估三个非空泛的转变，并指出相关已有工作（Saltzer & Schroeder, Progent, MiniScope等），为后续研究提供奠基。
- **直面实践问题**：指出“对齐代价”是人为造物，而非不可避免的权衡；防御训练的成本收益需要重新审视。

## 8. 不足与局限

- **实验覆盖面有限**：所有证据来自同一团队，可能引入确认偏差。未引入第三方数据集、不同来源模型、更广泛的任务类型。
- **缺乏新实验**：作为立场论文，没有提供大量全新实验来直接验证所提议的“外部强制执行”方案相比防御训练的实证优势（如具体安全提升、能力保留等）。现有引用中的实验主要用于批判现状，而非验证替代方案。
- **开放问题未解决**：论文承认权限规范、约束测量、级联感知指标、代理委托等开放问题，但未给出具体解法。实际工程落地的可行性、开销、错误率等trade-off没有详细评估。
- **范畴边界的模糊性**：论文承认“内容-行动边界”仍有一些残留属性可能确实属于模型内部（如输出中直接包含攻击指令），但未划定清晰界限，可能导致某些场景下仍需要混合方案。
- **潜在对抗性攻击**：外部参考监视器本身也可能被绕过（如权限表达错误、监视器实现漏洞），论文未深入讨论其安全性分析。
- **资源消耗**：外部执行可能引入延迟和复杂系统依赖，论文未系统量化性能开销。

（完）
