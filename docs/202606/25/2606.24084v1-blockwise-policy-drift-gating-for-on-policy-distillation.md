---
title: Blockwise Policy-Drift Gating for On-Policy Distillation
title_zh: 基于块策略漂移门控的在策略蒸馏
authors: "Liwen Zheng, Haiyun Jiang"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24084v1"
tags: ["query:opd"]
score: 9.0
evidence: 直接针对on-policy蒸馏提出块策略漂移门控
tldr: "在线策略蒸馏（OPD）在长程推理任务中易因策略漂移导致性能脆弱。本文提出分块策略漂移门控方法，通过计算行为学生与当前学生在采样轨迹上的log概率偏移，分块聚合后归一化，作为权重重加权OPD损失。在Qwen3数学推理基准上，固定64-token分块门控将平均pass@8从0.4978提升至0.5160。该方法不改变教师目标或滚动策略，简单有效，可提升推理任务的求解鲁棒性。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24084v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1353, \"height\": 413, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24084v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1272, \"height\": 456, \"label\": \"Table\"}]"
motivation: 在线策略蒸馏在长程推理任务中因学生策略漂移而脆弱，现有教师支持匹配虽有效但需修改教师端，亟需更轻量的学生端漂移控制方法。
method: 计算行为学生与当前学生在采样token路径上的log概率偏移，分块（如64token）聚合后取均值并归一化，作为门控系数重加权OPD逐位置损失。
result: "固定64-token块门控在AIME24等四个数学基准上平均pass@8从0.4978提升至0.5160，且优于其他蒸馏变体。"
conclusion: 局部旧-当前策略漂移可作为实用控制信号，分块门控是简单默认选择，能有效提升OPD在推理任务中的求解鲁棒性。
---

## 摘要
在策略蒸馏（OPD）利用在学生自身采样的轨迹上计算的教师信号来训练学生策略。近期研究表明，采样标记OPD在长程推理任务上可能脆弱，而局部教师支持匹配是一种简单有效的修复方法。本文引入块策略漂移门控，一种轻量级、仅适用于学生的旧-当前漂移控制器，用于轨迹复用下的OPD。该方法计算行为学生与当前学生在采样标记路径上的对数概率偏移，将这些偏移在固定块或跨度上聚合，并使用得到的分离、均值归一化的门控来重新加权OPD位置损失。它不改变教师目标、教师Top-K支持或轨迹策略。在一个六变体Qwen3数学推理基准中，所有训练变体均采用统一的200步训练预算，我们使用pass@8作为主要的问题级求解率指标。固定64标记块门控将采样标记OPD在AIME24、AIME25、MATH500和AMC23上的平均pass@8从0.4978提升至0.5160。在Teacher-TopK/LSM上，Block64在训练学生中取得了最佳的四个基准平均pass@8。结果识别出局部旧-当前策略漂移作为复用OPD轨迹的实用控制信号，并激发块级门控作为提高求解率鲁棒性的简单默认方法。

## Abstract
On-policy distillation (OPD) trains a student policy using teacher signals computed on trajectories sampled by the student itself. Recent work shows that sampled-token OPD can be fragile on long-horizon reasoning tasks and that local teacher-support matching is a simple and effective repair. This paper introduces blockwise policy-drift gating, a lightweight student-only old-current drift controller for OPD under rollout reuse. The method computes log-probability shifts between the behavior student and the current student on the sampled token path, aggregates these shifts over fixed blocks or spans, and uses the resulting detached, mean-normalized gates to reweight OPD position losses. It does not change teacher targets, teacher top-K supports, or the rollout policy. In a six-variant Qwen3 math reasoning benchmark with a uniform 200-step training budget for all trained variants, we use pass@8 as the primary problem-level solve-rate metric. Fixed 64-token block gating improves sampled-token OPD mean pass@8 from 0.4978 to 0.5160 across AIME24, AIME25, MATH500, and AMC23. On Teacher-TopK/LSM, Block64 gives the best four-benchmark mean pass@8 among trained students. The results identify local old-current policy drift as a practical control signal for reused OPD rollouts and motivate block-level gating as a simple default for improving solve-rate robustness.

---

## 论文详细总结（自动生成）

# 1. 核心问题与整体含义（研究动机和背景）
- **问题背景**：在线策略蒸馏（OPD）在长程推理任务中面临训练信号脆弱的问题，因为学生自身采样的轨迹前缀可能偏离教师提供可靠指导的区域。现有工作如 Teacher-TopK 局部支持匹配（LSM）虽能通过修改教师端信号来缓解，但本文关注另一个不匹配来源：在 PPO 式训练中，同一学生轨迹可能被复用多个优化轮次，导致当前学生与行为学生在同一 token 上的概率分布发生偏移（旧-当前策略漂移）。
- **研究动机**：这种漂移信号可以指示复用轨迹的“新鲜度”，论文探索是否可以用它来调制 OPD 损失，以及采用何种粒度（token级、序列级或块级）最有效。
- **整体含义**：提出一种轻量级、仅作用于学生端的门控机制，通过局部聚合旧-当前对数概率偏移来重新加权 OPD 位置损失，在不改变教师目标或采样策略的前提下提升长程推理任务的求解鲁棒性。

# 2. 方法论：核心思想、关键技术细节
- **核心思想**：在复用轨迹上计算当前学生与行为学生对同一采样 token 的对数概率偏移 \(d_{i,t} = \log\pi_\theta(y_{i,t}) - \log\pi_\text{old}(y_{i,t})\)，在固定块或换行跨度内平均得到聚合分数 \(s\)，进而生成门控权重 \(g = \exp(-\tau|s|)\)（\(\tau=1\)）。再对全响应 token 做均值归一化得到 \(\tilde{g}\)，并以 \(\tilde{g}\) 作为乘性权重重新加权原有的 OPD 位置损失。
- **关键技术细节**：
  - 漂移计算仅基于学生自身，不涉及教师。
  - 门控与原有损失分离（stop-gradient），避免影响策略学习的目标。
  - 支持两种聚合粒度：Block64（固定64 token块）和 NewlineSpan（按换行符分割的跨度）。
  - 最终损失形式：\(L_\text{gate} = \frac{\sum_{i,t} m_{i,t} \tilde{g}_{i,t} \ell^\text{base}_{i,t}}{\sum_{i,t} m_{i,t} + \epsilon}\)，分母保持原响应掩码归一化。
- **公式流程**（文字说明）：
  1. 对每个响应 token，计算 \(d_{i,t}\)（公式1）。
  2. 在块/跨度内平均 \(d_{i,t}\) 得 \(s\)。
  3. 计算门控 \(g = \exp(-|s|)\)（公式2）。
  4. 对全响应有效 token 做均值归一化得 \(\tilde{g}\)（公式3）。
  5. 将 \(\tilde{g}\) 作为权重乘到原 OPD 位置损失上（公式4）。

# 3. 实验设计
- **模型与代码库**：学生模型为 Qwen3-1.7B-Base，教师模型为 Qwen3-4B-Base-GRPO（经过 GRPO 数学推理训练）。基于 revisiting_opd 代码库修改。
- **训练数据**：DAPO-Math-17k 数学语料，共约 1,791,700 行（非去重提示数），采用数学环境格式。
- **评估基准**：MATH500、AIME24、AIME25、AMC23（AMC23 含 40 道题）。
- **主要指标**：pass@8（每个问题采样 8 次，任一正确即算通过）。辅助指标 avg@8（平均样本准确率）。
- **对比方法**：共六种训练变体：
  - 采样 token OPD（无门控）
  - OPD + Block64
  - OPD + NewlineSpan
  - Teacher-TopK/LSM（无门控）
  - LSM + Block64
  - LSM + NewlineSpan
- **训练配置**：统一 200 步训练预算；最大提示长度 2048，最大响应长度 16384；rollout group size=8；top-p=0.9，温度1.0；Teacher-TopK K=32；学习率 2e-6；2 个 PPO epoch。

# 4. 资源与算力
- 文中提及：每单次训练运行使用 A800 80GB GPU，训练 batch size=4。未明确说明使用的 GPU 数量、总训练时长或具体能耗。因此仅能确认使用了 A800 80GB 显卡，其他算力细节未给出。

# 5. 实验数量与充分性
- **实验数量**：6 种方法 × 4 个基准 = 24 个主要 pass@8 结果（表1），此外还报告了 avg@8 辅助指标（文中提及但未列出具体表）。无多次随机种子重复。
- **充分性评估**：
  - **正面**：统一训练预算（200步）使对比公平；覆盖四个常见数学推理 benchmark；包括了 LSM 和采样 token OPD 两大基线。
  - **不足**：
    - 未进行多次重复以评估方差（尤其 AIME 样本量小）。
    - 缺少关键的消融控制：未对比 Token-Gate（token级门控）、Token-IS（裁剪重要性权重）、Sequence-Gate（序列级门控）等基线。
    - 未探索不同块大小（32, 64, 128）的对比。
    - 未分析门控分布（如 log-ratio 百分位数、有效 token 比例等）。
    - 仅使用一种学生-教师对，单一数学领域，未推广到其他任务或模型尺寸。
- **结论**：实验初步可控但不够全面，存在偏差风险，作者也明确承认了这些局限。

# 6. 论文的主要结论与发现
- Block64 门控在采样 token OPD 上将平均 pass@8 从 0.4978 提升至 0.5160，主要提升来自 AIME25（13.3→20.0）和 AMC23 保持。
- 在 LSM 基础上添加 Block64 获得六种变体中最高的四基准平均 pass@8（0.533）。
- NewlineSpan 门控在 LSM 上接近 Block64，但在采样 token OPD 上一致性较差（AMC23 反而下降）。
- 块级（64 token）聚合优于 token 级噪声和序列级粗糙度，是简单实用的默认粒度。
- 旧-当前学生漂移是复用 OPD 轨迹的有效控制信号。

# 7. 优点
- **方法简洁轻量**：不修改教师目标、支持集或采样策略，仅需计算学生自身 log-prob 偏移并做块级聚合，易于嵌入现有 OPD 框架。
- **有效性显著**：在固定预算下，仅通过重加权即可提升 solve-rate，且对 LSM 也有正交增益。
- **信号新颖**：从“旧-当前策略漂移”角度切入，不同于已有教师可靠性或验证器信号。
- **分析合理**：块级粒度介乎 token 噪声和序列粗糙之间，直觉合理且实验支持。

# 8. 不足与局限
- **实验覆盖不足**：
  - 仅 1 个学生模型（1.7B）和 1 个教师（4B），未测试其他规模或架构。
  - 仅 4 个数学 benchmark，未涉其他推理任务（如代码、多步问答）。
  - 未使用多次随机种子，无法评估统计显著性。
  - 缺少关键消融：无 token 级/序列级/重要性权重基线，无法定位最优粒度。
- **方法内在局限**：
  - 块内符号抵消：平均 signed drift 可能因正负抵消而低估实际漂移量（文中已提及）。
  - 门控是非对称的（仅降低大漂移位置的权重），无法处理小漂移位置不足的问题。
  - 非无偏 off-policy 估计器，理论分析缺失。
- **应用限制**：
  - 仅适用于轨迹复用的 OPD 场景，不适用于异步或样本即时刷新设置。
  - 需要行为策略 log-prob 的存储/重计算，增加一定工程开销。
- **文档完整性**：无源码链接，未给出超参敏感度分析。

（完）
