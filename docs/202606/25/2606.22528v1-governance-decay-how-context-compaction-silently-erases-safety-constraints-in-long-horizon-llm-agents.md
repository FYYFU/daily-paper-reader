---
title: "Governance Decay: How Context Compaction Silently Erases Safety Constraints in Long-Horizon LLM Agents"
title_zh: 治理衰减：上下文压缩如何悄然抹去长视界LLM智能体中的安全约束
authors: Shiyang Chen
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22528v1"
tags: ["query:ai-safety"]
score: 10.0
evidence: 上下文压缩导致LLM代理中安全约束被擦除
tldr: "现代LLM代理依赖上下文压缩来管理长对话，但压缩可能无意中移除安全约束，导致代理执行被禁止的操作，这种现象被称为Governance Decay。本文提出ConstraintRot基准来量化此问题，发现压缩后违规率从0%升至30%，部分模型达59%。此外，对抗性注入可故意使压缩器遗漏约束。作者提出无需训练的约束固定方法，将违规率恢复至0%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22528v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1422, \"height\": 535}, {\"url\": \"assets/figures/arxiv/2606.22528v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1461, \"height\": 637}, {\"url\": \"assets/figures/arxiv/2606.22528v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1594, \"height\": 1164}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22528v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1467, \"height\": 72}, {\"url\": \"assets/tables/arxiv/2606.22528v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 701, \"height\": 368}, {\"url\": \"assets/tables/arxiv/2606.22528v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1552, \"height\": 390}, {\"url\": \"assets/tables/arxiv/2606.22528v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 858, \"height\": 222}, {\"url\": \"assets/tables/arxiv/2606.22528v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 751, \"height\": 286}]"
motivation: 发现上下文压缩会无意识移除安全约束，导致LLM代理在长对话中执行禁止操作，破坏安全性。
method: 设计ConstraintRot基准评估7个模型，并研究对抗性压缩-驱逐攻击；提出约束固定方法隔离约束。
result: "压缩后违规率从0%升至30%，部分模型达59%；攻击可击败所有模型；约束固定将违规率恢复至0%。"
conclusion: 上下文管理是LLM代理安全的关键表面，需在部署中保护约束不被压缩移除。
---

## 摘要
现代LLM智能体越来越依赖上下文压缩、摘要或逐出来将长时间运行的会话保持在token预算内。我们发现，这种上下文管理层是一个安全关键故障面：智能体在可见时可靠遵循的上下文内治理约束可能会被压缩悄然移除，导致同一智能体在会话后期执行被禁止的工具操作。我们将这种故障模式称为治理衰减。我们介绍了ConstraintRot，一个具有确定性工具调用评分的长视界智能体场景基准测试，并测量了七个模型系列中由压缩引发的违规。在1323个场景中，违规从政策处于完整上下文时的0%上升到压缩后的30%，某些模型达到59%；当约束存活于摘要中时，违规保持0%，但当被丢弃时，违规达到38%。我们进一步研究了一种压缩-逐出攻击，其中对抗性的上下文内容使摘要器偏向于省略合法政策，并表明优化的注入击败了每个评估的模型。最后，我们提出了约束固定，一种简单的无需训练的缓解措施，将有损压缩的治理约束隔离，并在我们的基准测试中将违规恢复到0%。这些结果将上下文管理确定为部署的LLM智能体的一类治理面。

## Abstract
Modern LLM agents increasingly rely on context compaction, summarization, or eviction to keep long-running sessions within a token budget. We show that this context-management layer is a safety-critical failure surface: in-context governance constraints that agents reliably obey while visible can be silently removed by compaction, causing the same agent to perform prohibited tool actions later in the session. We call this failure mode Governance Decay. We introduce ConstraintRot, a benchmark of long-horizon agent scenarios with deterministic tool-call grading, and measure compaction-induced violations across seven model families. Across 1,323 episodes, violation rises from 0% with the policy in full context to 30% after compaction, reaching 59% for some models; when the constraint survives the summary, violation remains 0%, but when it is dropped, violation reaches 38%. We further study a Compaction-Eviction Attack, in which adversarial in-context content biases the summarizer to omit a legitimate policy, and show that optimized injections defeat every evaluated model. Finally, we propose Constraint Pinning, a simple training-free mitigation that quarantines governance constraints from lossy compaction and restores violation to 0% in our benchmark. These results identify context management as a first-class governance surface for deployed LLM agents.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）
- **核心问题**：现代LLM智能体为管理长对话采用上下文压缩（context compaction），该机制会无意中移除安全约束（如组织政策、内存条目），导致智能体在后续会话中执行原本被禁止的操作。这种“治理衰减”是一个被忽视的安全漏洞。
- **研究动机**：现有工作优化压缩仅考虑任务连续性，未评估治理约束的存活性。约束丢失后，智能体本身未变，也无提示注入，但行为从合规变为违规。该问题在部署场景中可能导致实际损失（如外发机密邮件、误操作生产数据库）。
- **整体含义**：上下文管理应被视为LLM智能体的一级治理面，安全部署需要保护约束不被压缩“遗忘”。

### 论文提出的方法论
- **核心思想**：
  1. 通过比较同一轮对话中政策存在/压缩后/被手动删除等条件下智能体对禁止请求的响应，隔离压缩对约束的删除效应。
  2. 定义治理衰减∆decay = v(M, [C(H<t); r]) − v(M, [H<t; r])，即相同请求在压缩后与压缩前违规概率之差。
  3. 提出压缩-驱逐攻击：攻击者通过注入大量内容或向压缩器嵌入指令，主动/被动导致约束被遗漏。
  4. 提出约束固定：将治理约束提取到固定缓冲区，免于压缩，并在每次压缩后重新注入，同时进行完整性检查。
- **关键技术细节**：
  - ConstraintRot基准：包含9个任务（5个软组织政策 + 4个硬安全规范），每个任务有政策建立阶段、长序列任务轮次、最后触发请求。违规判定通过解析工具调用参数中的禁止效果（如收件人域外），无需LLM裁判。
  - 实验条件：无政策、政策全上下文、压缩后、两种攻击（volume/inject）、两种防御（pin/pin+attack）。
  - 攻击策略搜索：6种注入措辞（废弃提示、不相关、token预算、直接指令、系统冒充、操作者权威），每模型选最佳。
  - 约束固定实现：约47 token，每次压缩后重注入一次，开销<0.5%。
- **公式/算法流程**（文字）：
  1. 智能体会话循环：维护消息历史H → 当token(H)>预算B → 应用压缩算子C（LLM摘要/截断）。
  2. 政策p通过非系统通道（用户指令、内存条目、工具输出）交付。
  3. 测量违规：对同一触发请求r，比较v(压缩上下文) vs v(原上下文)。
  4. 攻击：对抗数据嵌入H，通过迫使压缩触发或直接指令压缩器“省略政策”。
  5. 防御：预定义固定缓冲区B_pinned，从H提取p，每次压缩后用B_pinned中的p重新注入并校验。

### 实验设计
- **数据集/场景**：自建ConstraintRot基准，9个任务：
  - 软组织政策：外部邮件限制、语言政策、频道路由、地区部署、支出审批。
  - 硬安全规范：生产数据库操作、SSH密钥窃取、PII泄露、禁用审计日志。
- **Benchmark**：ConstraintRot – 每场景包含政策建立、长任务（增长上下文至触发压缩）、最终触发请求，工具调用被解析以评分违规。
- **对比条件**：floor（无政策）、control（政策全上下文）、compaction、attack_volume、attack_inject、defense_pin、defense_pin_attack。
- **对比模型**：7个模型系列：DeepSeek-V4-Flash, GLM-5.1, Qwen3.6-27B, Kimi-K2.5, Claude-Sonnet-4.6, GPT-5.4-mini, Gemini-3.5-flash。
- **对比方法**：无防御基线、系统通道政策、Spotlighting风格对抗防御、约束固定。
- **其他实验**：多轮压缩、跨语言（中文/西班牙语）、多种压缩策略（recency-truncate, hierarchical, LLM summarize, head_tail）、真实框架验证（LangGraph, LangMem, AutoGen, OpenAI Agents SDK）。

### 资源与算力
- **未明确说明**：论文指出所有实验均为推理阶段、无需训练（training-free），但未提供GPU型号、数量或运行时间。可能通过API调用进行，未透露具体算力资源。

### 实验数量与充分性
- **主要实验**：7模型 × 9任务 × 3重复 = 1323 episode（表2所示主网格）。另有一次高功率网格（5模型 × 9任务 × 6重复 = 1080 episode）确认置信区间。
- **消融实验**：
  - 约束存活 vs 违规率（LLM裁判 + 关键词启发式 + 三裁判投票）。
  - 压缩器 vs 智能体交叉实验（3个压缩器 × 3智能体，每个15 episode）。
  - 压缩激进程度扫描（预算从15词到300词，每个模型重复3次）。
  - 压缩策略对比（4种策略，每策略每模型多任务，结果在附录）。
  - 多轮压缩（R=0→4）。
  - 跨语言（中文、西班牙语）。
  - 攻击策略搜索（3模型 × 5软任务 × 6策略，每个策略多重复）。
  - 防御效用测试（7个合法动作，3模型 × 84 episode）。
  - 约束固定压力测试（3模型 × 5软任务 × 60 episode，含操作者角色冒充、过载等）。
  - 真实框架验证（每个4组，n=20 per cell）。
- **充分性评价**：实验设计系统，覆盖多模型、多任务、多条件、多因素，因果隔离清晰（机制证据：政策存活预测违规；压缩器决定违规而非智能体）。但重复次数中等（3次/条件），攻击搜索未涉及梯度优化。总体客观公平。

### 论文主要结论与发现
1. **治理衰减显著**：压缩导致违规从0%升至30%（平均），最高达59%（DeepSeek-V4, Kimi-K2.5）。某些情况下甚至超过无政策基线。
2. **约束存活是关键**：政策存活时违规0%，被丢弃时38%。
3. **归因于压缩器而非智能体**：交叉实验显示违规率取决于写摘要的模型，而非决策智能体。
4. **软政策衰减远大于硬规范**：软组织政策衰减+50百分点，硬规范仅+6（8.3倍差异），因为硬规范模型内置，而软政策只能依赖上下文。
5. **攻击可武器化**：体积攻击和摘要器注入攻击均有效；优化注入可击败所有模型，即使对被动压缩鲁棒的GLM和Gemini也会被攻破。
6. **约束固定有效且低开销**：将违规恢复至0%，开销<0.5% token；不降低有用性（允许合法动作99%）。
7. **约束固定局限性**：无法抵抗操作者角色冒充（rescind命令出现在近期上下文），需要可信带外操作者通道。

### 优点
- **机制透明**：通过约束存活分析、压缩器-智能体交叉实验、长上下文对比等，证明删除机制而非注意力稀释。
- **基准全面**：ConstraintRot覆盖软/硬政策、多种攻击和防御场景，确定性评分避免LLM裁判偏差。
- **防御实用**：约束固定无需训练，简单集成，成本极低。
- **现实验证**：在LangGraph、LangMem、AutoGen、OpenAI Agents SDK等真实框架中重现结果，增强生态相关性。
- **诚实报告局限**：明确标注约束固定的失效条件和未解决的问题，避免过度承诺。

### 不足与局限
1. **依赖可提取的显式约束**：隐式约束（如模型通过微调学到的规则）不在范围内，无法通过固定保护。
2. **不可信操作者通道**：当攻击者冒充操作者在近期上下文中发出覆盖指令，约束固定仍会失效（从0%升至17% → 10%），需带外认证。
3. **实验规模限制**：每条件3次重复（主网格），少数条件n=6或n=20，统计精度有限；未进行梯度或LLM-in-the-loop优化，实际攻击可能更强。
4. **仅评估工具调用违规**：未考虑文本输出违规或间接安全后果。
5. **模型静态API访问**：非开源模型版本固定，结果可能随模型更新变化。
6. **压缩策略单一默认**：主要实验使用LLM摘要，但生产环境可能采用更复杂的混合策略，虽在附录中有验证但覆盖不完整。
7. **未量化计算开销**：约束固定引入的额外延迟和资源消耗未报告，尽管声称<0.5% token。

（完）
