---
title: "The Geometry of Refusal: Linear Instability in Safety-Aligned LLMs"
title_zh: 拒绝的几何学：安全对齐大语言模型中的线性不稳定性
authors: "Shivam Ratnakar, Kartikeya Vats"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22686v1"
tags: ["query:ai-safety"]
score: 9.0
evidence: 通过对比对数导向揭示安全对齐的脆弱性和越狱方法
tldr: "73% vs 22.6%），反向使用可加固模型。揭示了安全对齐的线性脆弱性及双向控制的防御潜力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22686v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 797, \"height\": 642, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22686v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1662, \"height\": 864, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22686v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1632, \"height\": 1407, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22686v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1631, \"height\": 803, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22686v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 808, \"height\": 319, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22686v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 605, \"height\": 178, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22686v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 792, \"height\": 451, \"label\": \"Table\"}]"
motivation: 探究安全对齐LLM中拒绝机制的线性可操纵性，揭示其脆弱本质。
method: 提出对比对数几率引导（CLS），通过对比隐藏状态提取拒绝方向并直接操作输出分布，无需优化。
result: "CLS对晚期决策模型（Llama-3.1）攻击成功率95%，对早期发散模型（Qwen-2.5）鲁棒性高；反向控制可加固模型。"
conclusion: "安全对齐技术创造了线性可操纵的'安全轴'，既是关键漏洞也是防御基元。"
---

## 摘要
现代大型语言模型（LLM）依赖于广泛的安全对齐，但拒绝机制的基础仍然不透明。在这项工作中，我们研究安全合规是深层的语义决策还是可操作的线性特征。我们引入了对比对数导向（CLS），这是一个零优化框架，通过对比来自安全和不受限制的系统提示的隐藏状态来隔离“拒绝方向”。与作用于内部激活的表征工程方法不同，CLS直接操作输出分布，作为对齐脆弱性的诊断探针。当与前缀注入结合以绕过初始拒绝反射时，该方法引发了一个相位转变，其中防护栏崩溃。我们在7个模型家族上的实验表明，安全实现是架构确定性的。虽然Llama-3.1等模型表现出“晚期决策”拓扑，容易被CLS绕过（约一秒钟内达到95%的攻击成功率），但其他模型如Qwen-2.5通过将安全整合到计算中期表现出“早期分歧”。与已建立的激活级导向方法的直接比较显示，CLS在Llama 2（73% vs. 22.6%）和Qwen 7B（91% vs. 79.2%）上实现了显著更高的攻击成功率，表明对数级干预暴露了隐藏状态方法低估的对齐漏洞。除了攻击，我们表明这种线性性实现了双向控制：反转导向向量可以在不重新训练的情况下“强化”模型对抗越狱。我们的发现表明，当前的对齐技术创建了一个可操作的“安全轴”，这既是关键脆弱性，也是防御的精确原语。

## Abstract
Modern Large Language Models (LLMs) rely on extensive safety alignment, yet the mechanistic basis of refusal remains opaque. In this work, we investigate whether safety compliance is a deep semantic decision or a manipulable linear feature. We introduce Contrastive Logit Steering (CLS), a zero-optimization framework that isolates the "refusal direction" by contrasting hidden states derived from safe and unrestricted system prompts. Unlike representation engineering methods that intervene on internal activations, CLS operates directly on the output distribution, serving as a diagnostic probe for alignment fragility. When coupled with prefix injection to bypass initial refusal reflexes, this method induces a phase transition where guardrails collapse. Our experiments on 7 model families reveal that safety implementation is architecturally deterministic. While models like Llama-3.1 exhibit a "Late Decision" topology that is easily bypassed by CLS (reaching 95% ASR in approximately one second), others like Qwen-2.5 demonstrate "Early Divergence" by integrating safety mid-computation. Direct comparison with established activation-level steering methods shows that CLS achieves substantially higher attack success rates on Llama 2 (73% vs. 22.6%) and Qwen 7B (91% vs. 79.2%), demonstrating that logit-level intervention exposes alignment vulnerabilities that hidden-state methods underestimate. Beyond attacks, we show that this linearity enables bidirectional control: inverting the steering vector "hardens" models against jailbreaks without retraining. Our findings suggest that current alignment techniques create a steerable "safety axis" that serves as both a critical vulnerability and a precise primitive for defense.

---

## 论文详细总结（自动生成）

# 中文详细总结

## 1. 论文的核心问题与整体含义
- **研究动机**：现代LLM通过RLHF等方法实现了安全对齐，但安全合规的底层机制（拒绝行为）是否是一种深层语义决策还是可操纵的线性特征，仍然不透明。这导致安全对齐的脆弱性可能被攻击者利用。
- **整体含义**：论文通过机制性证据表明，安全对齐实际上是一个浅层的线性几何特征（“拒绝向量”），而非深层语义约束。这一特征使得模型可以被轻松地通过简单的算术操作绕过（越狱）或强化（防御）。

## 2. 论文提出的方法论
- **核心思想**：提出**对比对数导向（Contrastive Logit Steering, CLS）**，通过对比“安全”和“无限制”系统提示下的模型输出logit，直接计算并减去“拒绝向量”，从而在零优化、零搜索的情况下操纵输出分布。
- **关键技术细节**：
  - 需要白盒访问logit层，不修改内部激活。
  - 采用三个并行前向传播：基座（base）、无限制（positive/s+）、安全（negative/s-）提示。
  - 在每个解码步，计算瞬时导向向量 `v = z+ - z-`，然后加到基座logit上：`z̃ = z_base + α·v`。
  - α>0用于越狱（去除安全），α<0用于强化安全。
  - 结合**前缀注入**（强制前k个token为“Sure”），绕过模型的初始拒绝反射。
- **公式/算法流程**（文字说明）：
  1. 对每个查询，用三个不同的系统提示分别得到logit向量。
  2. 计算导向向量（无限制提示logit减去安全提示logit）。
  3. 将导向向量乘以超参数α并加到基座logit上。
  4. 从调整后的logit中采样下一个token；前k个token被强制设为“Sure”。
- **零样本检测**：通过平均有害/良性查询的最终层隐藏状态计算全局拒绝向量，然后利用余弦相似度（阈值τ）判断新查询是否恶意，无需训练分类器。

## 3. 实验设计
- **数据集/场景**：
  - **AdvBench**：用于α扫描的细粒度敏感性分析。
  - **JailbreakBench**：用于CLS vs GCG比较以及零样本检测。
  - **HarmBench**（“标准行为”子集，159个提示）：用于与Arditi等人激活级导向方法的对比。
- **基准（Benchmark）**：攻击成功率（ASR）和连贯性评分（1–5 Likert量表）。
- **对比方法**：
  - **GCG**：基于梯度搜索的对抗后缀越狱方法（100步）。
  - **Arditi et al. (2024)**：通过中间隐藏状态进行激活级导向方法。
- **模型家族**：7个开源模型：Gemma-3（4B,12B）、Llama-3.1（8B）、Llama-3.2（3B）、Llama-3.3（70B）、Qwen-2.5（1.5B, 7B），额外包括Llama-2和Qwen-7B用于与Arditi对比。

## 4. 资源与算力
- 论文中提到GCG每个查询在A100上约需**15分钟**，而CLS仅需**约1秒**（接近900倍加速）。
- 未明确说明使用的GPU数量、训练时长或具体算力配置（如A100型号、数量等）。但暗示实验在单GPU上完成（推理开销较小）。

## 5. 实验数量与充分性
- **实验数量与类型**：
  - α扫描（-5到5，步长1），分别测试α>0和α<0，覆盖7个模型。
  - CLS vs GCG对比在两个模型（Llama-3.1-8B, Qwen-2.5-7B）上。
  - CLS vs Arditi对比在Llama-2和Qwen-7B上（使用HarmBench标准子集）。
  - KL散度分析（内部表示深度变化）在两个典型模型上。
  - 零样本检测实验（F1评分在JailbreakBench上）。
- **充分性与公平性**：
  - 实验覆盖了多个模型家族、不同规模，直接与两个最相关的方法比较（GCG和激活层导向）。
  - 使用人类验证挑选最佳评判模型（Mistral-Small-24B）以降低自动评估噪声。
  - 报告了ASR和连贯性两个指标，考虑了质量退化。
  - 但缺乏与RepE、Activation Addition等其他方法的直接定量比较（仅定性提及），也未在更大规模模型（如70B以上）上进行全量对比（Llama-3.3-70B仅部分实验）。

## 6. 论文的主要结论与发现
1. **安全对齐是浅层线性特征**：在Llama等模型中，拒绝决策仅在最后几层出现（“Late Decision”拓扑），模型处理有害和良性查询在95%的层中完全一致。这使得CLS能在约1秒内达到95% ASR。
2. **模型架构决定脆弱性**：Qwen-2.5表现出“Early Divergence”（安全整合在~40%深度），因此抵抗线性导向更强（α=5时ASR仅68.5%）。
3. **CLS优于激活级导向**：在Llama-2上ASR 73%（vs 22.6%），在Qwen-7B上91%（vs 79.2%），证明logit层干预更能暴露浅层安全漏洞。
4. **双向控制**：负α可“强化”安全，并将越狱成功率降至个位数；同时提升连贯性（Qwen-2.5连贯性从3.84升至4.14）。
5. **零样本检测**：利用拒绝向量的余弦相似度可实现0.92 F1，无需额外训练。

## 7. 优点
- **方法创新**：CLS是零优化、零搜索的几何干预，直接揭示安全对齐的线性本质，计算成本极低（秒级），可作为诊断探针。
- **诊断性贡献**：通过KL散度分析给出“Late Decision” vs “Early Divergence”拓扑分类，对理解不同模型的安全机制具有解释意义。
- **实验对比严谨**：直接复现并比较了当前最相关的激活级方法，并控制评估标准（人类验证判官），结果可靠。
- **防御价值**：负向导向可实时强化模型，无需重训练，且不牺牲质量（甚至改善连贯性）。
- **可解释性**：PCA可视化清晰展示拒绝向量的线性分离性，增强了方法论的可理解性。

## 8. 不足与局限
- **白盒限制**：CLS需要logit访问，仅适用于开源权重模型；无法直接扩展到黑盒API模型。
- **超参数敏感**：系数α需要手动调整，不同模型最佳α不同；论文中扫描步长较大（1.0），可能遗漏更细粒度的转折点。
- **评估范围有限**：仅关注单轮交互，未考虑多轮对话或上下文依赖；未在更大模型（如70B以上）的多个基准上系统评估。
- **对比方法不全**：仅与GCG和Arditi方法严格比较，与RepE、Activation Addition等仅定性提及，缺乏直接定量对比。
- **因果归因未完全分离**：Late Decision vs Early Divergence的差异可能由架构和训练共同导致，论文未能解耦两者贡献。
- **潜在危害**：披露的越狱技术可能被滥用，但论文已通过强调防御应用（负α）和机制分析来缓解风险。

（完）
