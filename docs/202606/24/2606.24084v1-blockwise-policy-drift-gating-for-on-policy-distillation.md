---
title: Blockwise Policy-Drift Gating for On-Policy Distillation
title_zh: 用于在线策略蒸馏的块状策略漂移门控
authors: "Liwen Zheng, Haiyun Jiang"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24084v1"
tags: ["query:opd"]
score: 10.0
evidence: 提出块级策略漂移门控用于在策略蒸馏OPD直接解决OPD在长期任务中的脆弱性问题
tldr: "在线策略蒸馏（OPD）在长程推理任务中因策略漂移导致性能下降。本文提出分块策略漂移门控，通过计算行为学生与当前学生在采样路径上的log概率偏移，在固定64 token块上聚合后均值归一化，作为门控权重重新加权OPD位置损失。在六个Qwen3数学推理变体上，该方法将平均pass@8从0.4978提升至0.5160。实验表明，局部策略漂移是重用OPD rollout的有效控制信号，块级门控可作为提高求解率鲁棒性的简单默认方法。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24084v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1353, \"height\": 413, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24084v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1272, \"height\": 456, \"label\": \"Table\"}]"
motivation: 针对在线策略蒸馏中策略漂移导致长程推理性能下降的问题，提出轻量级门控机制以提升求解率。
method: 计算行为学生与当前学生在采样路径上的log概率偏移，按固定块聚合后均值归一化，作为门控权重重新加权位置损失。
result: "在六个Qwen3数学推理变体上，64 token块门控将平均pass@8从0.4978提升至0.5160，优于基线。"
conclusion: 识别局部策略漂移为有效控制信号，块级门控作为简单默认方法可提高求解率鲁棒性。
---

## 摘要
在线策略蒸馏（OPD）利用由学生策略自身采样的轨迹上计算的教师信号来训练学生策略。最近的研究表明，采样令牌在线策略蒸馏（Sampled-token OPD）在长程推理任务上可能脆弱，而局部教师支持匹配是一种简单有效的修复方法。本文引入了块状策略漂移门控，一种轻量级的仅学生端的新旧策略漂移控制器，用于轨迹重用下的在线策略蒸馏。该方法计算行为学生与当前学生在采样令牌路径上的对数概率偏移，将这些偏移在固定块或跨度上聚合，并利用得到的分离的、均值归一化的门控来重新加权OPD位置损失。它不改变教师目标、教师Top-K支持或轨迹采样策略。在一个六变种Qwen3数学推理基准测试中，所有训练变种均采用统一的200步训练预算，我们使用pass@8作为主要的问题级解决率指标。固定64令牌块门控将采样令牌OPD在AIME24、AIME25、MATH500和AMC23上的平均pass@8从0.4978提升至0.5160。在教师TopK/LSM上，Block64在训练学生中获得了最佳的四基准平均pass@8。结果识别出局部新旧策略漂移作为重用OPD轨迹的实用控制信号，并促使块级门控成为提高解决率鲁棒性的简单默认方案。

## Abstract
On-policy distillation (OPD) trains a student policy using teacher signals computed on trajectories sampled by the student itself. Recent work shows that sampled-token OPD can be fragile on long-horizon reasoning tasks and that local teacher-support matching is a simple and effective repair. This paper introduces blockwise policy-drift gating, a lightweight student-only old-current drift controller for OPD under rollout reuse. The method computes log-probability shifts between the behavior student and the current student on the sampled token path, aggregates these shifts over fixed blocks or spans, and uses the resulting detached, mean-normalized gates to reweight OPD position losses. It does not change teacher targets, teacher top-K supports, or the rollout policy. In a six-variant Qwen3 math reasoning benchmark with a uniform 200-step training budget for all trained variants, we use pass@8 as the primary problem-level solve-rate metric. Fixed 64-token block gating improves sampled-token OPD mean pass@8 from 0.4978 to 0.5160 across AIME24, AIME25, MATH500, and AMC23. On Teacher-TopK/LSM, Block64 gives the best four-benchmark mean pass@8 among trained students. The results identify local old-current policy drift as a practical control signal for reused OPD rollouts and motivate block-level gating as a simple default for improving solve-rate robustness.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在线策略蒸馏（OPD）在长程推理任务中容易因策略漂移而性能下降。具体而言，在PPO风格的训练中，同一学生的rollout可能会在多个优化周期中被重用，导致当前学生策略与行为学生策略（生成该rollout时的策略）产生对数概率偏移，使得过时的rollout数据对当前更新的监督信号不可靠。
- **研究动机**：现有工作如Teacher-TopK局部支持匹配（LSM）通过改进教师信号来解决OPD的脆弱性，但尚未关注学生侧策略漂移对损失权重的影响。本文旨在探索是否应利用这种新旧策略偏移来调制OPD损失，以及在何种粒度上聚合。
- **整体含义**：提出一种轻量级的仅学生端策略漂移门控机制，作为现有OPD损失的插入式权重，在不改变教师目标、教师支持或rollout策略的前提下，通过调整不同位置的损失贡献来提高解决率鲁棒性。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：在OPD训练中，对每个采样令牌计算行为学生与当前学生之间的对数概率偏移 \(d_{i,t} = \log \pi_{\theta}(y_{i,t}) - \log \pi_{\text{old}}(y_{i,t})\)，然后以固定块（如64令牌）为单位聚合这些偏移，生成一个分离的、均值归一化的门控权重，用于重新加权每个位置的基础OPD损失。门控权重仅控制哪些位置主导优化，不改变教师监督或rollout分布。
- **关键技术细节**：
  - **偏移计算**：仅使用学生模型的log-prob，不涉及教师模型。
  - **门控形状**：对聚合后的分数 \(s\)，采用软漂移门控 \(g = \exp(-\tau |s|)\)，\(\tau=1\)。
  - **归一化**：对所有有效响应令牌的 \(g\) 进行均值归一化，得到 \(\tilde{g}_{i,t}\)，使其平均值为1，以保持平均损失尺度不变。
  - **聚合粒度**：主要评估两种：
    - Block64-Gate：将响应分割为固定64令牌块，块内平均偏移并广播门控。
    - NewlineSpan-Gate：按换行符分割，合并短跨度，分割长跨度，无换行符时回退到固定块。
- **最终目标**：\(L_{\text{gate}} = \frac{\sum_{i,t} m_{i,t} \tilde{g}_{i,t} \ell^{\text{base}}_{i,t}}{\sum_{i,t} m_{i,t} + \epsilon}\)，分母保持原始响应掩码分母。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：
  - 训练数据：来自公开的DAPO-Math-17k数学语料库，预处理后约1,791,700行数学题目（行数，非唯一提示词数量）。
  - 评估基准：四个数学基准：MATH500、AIME 2024、MathArena AIME 2025、AMC23（来自2023年AMC 12A/12B的40题）。
- **基准**：主要指标为pass@8（每个问题生成8个样本，任一正确即算解决），辅助指标avg@8（平均样本准确率）。
- **对比方法**：共训练6个学生变种，每个均为Qwen3-1.7B-Base，教师为Qwen3-4B-Base-GRPO。
  - **基线**：Sampled-token OPD（无门控）、Teacher-TopK/LSM（无门控）。
  - **门控变种**：OPD + Block64、OPD + NewlineSpan、LSM + Block64、LSM + NewlineSpan。
  - 同时报告base student（未训练）和teacher reference（4B模型）作为参照。

## 4. 资源与算力

- 论文明确说明：每个单独训练运行使用A800 80GB GPU，训练批次大小为4，以平衡内存和墙钟时间，同时保留rollout组大小（8）和响应长度。未报告GPU数量、总训练时长或并行度。论文提到“fixed 200-step training budget”，但未说明每个步骤的耗时。
- **不足**：算力信息不完整，未给出具体GPU数量、训练总时长或能耗。

## 5. 实验数量与充分性

- **实验数量**：共6个训练变种 × 4个评估基准，报告了主要pass@8和辅助avg@8。还包括base student和teacher reference的评估。但未进行重复随机种子实验，未包含粒度控制（如Token-Gate、Sequence-Gate、Token-IS）的对比。
- **充分性**：
  - **优点**：在统一的200步训练预算下比较，避免早期停止偏差；使用固定模型和教师，控制变量；报告了四个不同难度和风格的数学基准。
  - **不足与风险**：
    - 仅使用一个学生模型（1.7B）和一个教师模型（4B），结论的泛化性受限。
    - AIME25等小样本集（约15-20题）导致差异统计意义有限。
    - 未做消融实验（如不同块大小、绝对值 vs 符号均值、门控归一化策略）。
    - 未提供漂移分布诊断（如对数比率百分位数、有效令牌比、梯度范数分布）。
    - 仅一次运行，无随机种子重复，无法评估方差。

## 6. 论文的主要结论与发现

- **主要结论**：
  - Block64门控改进了采样令牌OPD的pass@8，从0.4978提升至0.5160（平均四基准），且在LSM上获得最佳聚合pass@8（53.3%）。
  - NewlineSpan门控接近Block64，但不如Block64一致（尤其在OPD基线上）。
  - 块级门控比令牌级更平滑（避免噪声），比序列级更局部（保留区域信息）。
  - 局部新旧策略漂移是重用的OPD rollout的实用控制信号，块级门控可作为简单默认方案提高解决率鲁棒性。
- **辅助发现**：avg@8与pass@8趋势一致，支持OPD侧增益主要来自样本质量或解决率鲁棒性，而非均匀扩展稀有样本覆盖。

## 7. 优点

- **方法创新性**：提出一种正交于教师支持匹配的轻量级学生侧权重机制，直接利用策略漂移信号，不依赖教师、验证器或结果标签。
- **简洁有效**：仅需计算学生log-prob偏移，块级聚合后归一化，实现简单且计算开销小。
- **实验设计干净**：统一200步训练预算、固定模型、控制变量（仅改变门控粒度），避免超参数调优引入偏差。
- **评估指标合理**：以pass@8作为主要指标（问题级解决率），更符合实际推理任务需求；同时报告avg@8作为辅助诊断。
- **放弃复杂处理**：明确不改变教师目标、支持或rollout策略，保持与现有OPD框架兼容。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅一个学生模型（1.7B）、一个教师（4B）、一个数学领域，未验证在其他任务（如代码生成、通用推理）或更大模型上的效果。
  - 未包含完整的粒度消融：缺失Token-Gate、Token-IS（重要性采样）、Sequence-Gate、不同块大小（32、64、128）、绝对值/RMS/非对称聚合等控制实验。
  - 未进行重复随机种子实验，无法评估统计显著性。
- **潜在偏差风险**：
  - 训练数据来自单一语料库（DAPO-Math-17k），可能存在领域特异性。
  - 门控使用符号平均可能使正负偏移抵消，低估实际漂移幅度（作者已承认此局限）。
  - 固定200步预算可能对某些方法不公平（某些方法可能需更多步收敛，而另一些可能过拟合）。
- **应用限制**：
  - 仅适用于同步rollout重用场景，不适用异步或延迟更新（与f-OPD正交）。
  - 门控权重通过exp(-|s|)软衰减，可能会过度抑制罕见但有用的样本（作者建议未来研究保留多样性）。
  - 未评估门控对梯度范数或训练稳定性的影响，缺乏诊断分析。
- **方法局限**：该方法并非无偏离线策略估计器，只是经验性加权规则。

（完）
