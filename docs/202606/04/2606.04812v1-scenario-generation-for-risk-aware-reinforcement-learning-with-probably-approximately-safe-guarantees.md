---
title: Scenario Generation for Risk-Aware Reinforcement Learning with Probably Approximately Safe Guarantees
title_zh: 具有近似安全保证的风险感知强化学习的场景生成
authors: "Mohit Prashant, Arvind Easwaran"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04812v1"
tags: ["query:ai-safety"]
score: 9.0
evidence: 直接解决强化学习中的安全问题，提供概率安全保证
tldr: 确保安全是强化学习真实部署的关键，但策略易受转换扰动导致未知或不安全行为。现有概率屏障证书方法可能边界不紧。本文利用变分自编码器近似状态分布，构建上下界屏障证书并定义为对偶优化问题，通过采样上下界之间的非鲁棒区域状态动态收紧边界，提供更尖锐的近乎肯定安全保证。实验结果证明了边界紧致性，为风险感知强化学习提供了可证明安全的场景生成方法，推动了安全RL的实用化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04812v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 874, \"height\": 607, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.04812v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 582, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04812v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1093, \"height\": 757, \"label\": \"Table\"}]"
motivation: 强化学习策略在转换扰动下可能进入未充分探索状态，导致不安全行为，迫切需要紧致的安全概率保证。
method: 使用VAE近似状态分布，构建上下界概率屏障证书，通过采样两类证书之差集（非鲁棒区域）优化边界紧致性。
result: 实验基于多个连续控制任务，证明了所提方法能够显著收紧安全概率的上下界，提供更精确的安全保证。
conclusion: 本文为风险感知强化学习提供了一种可证明安全的场景生成方法，通过动态采样非鲁棒区域提升安全保证紧致性。
---

## 摘要
保证安全性对于强化学习（RL）智能体在现实世界中的部署至关重要，尤其是当使用深度强化学习学到的策略可能表现出对转移扰动的敏感性，从而导致未知或不安全的行为时。一种策略验证方法是构建概率屏障证书，通过采样关于安全约束的策略轨迹，从而将已知安全行为与未知行为区分开来。如果策略容易受到转移不确定性或扰动的影响，导致智能体进入未充分探索的状态，则获取违反这些约束的概率的严格上下界可能很困难。为了解决这个问题，我们使用变分自编码器（VAE）近似所遇到的状态空间的分布，并利用状态的潜在特征构建上下界屏障证书，以高置信度优化已知安全行为的区域。在我们的工作中，这被构建为一个对偶优化问题，其中下界屏障证书比上界屏障证书提供了更保守的安全区域估计。在训练过程中，采样位于两者集合差（即非鲁棒区域）内的状态，使我们能够收紧上下界，从而提供更尖锐的安全性概率保证。在我们的研究中，我们描述了所给出的保证，并通过实验证明了我们边界的紧致性。

## Abstract
Guaranteeing safety is critical to the deployment of reinforcement learning (RL) agents in the real-world, especially as policies learned using deep RL may demonstrate susceptibility to transition perturbations that result in unknown or unsafe behaviour. A method of policy verification is to construct probabilistic barrier-certificates by sampling policy trajectories with respect to safety constraints, thereby demarcating known safe behaviour from unknown behaviour. Obtaining tight upper and lower bounds on the probability of violation of these constraints may be difficult if the policy is susceptible to transition uncertainty or perturbation that places the agent in insufficiently explored states. To address this, we approximate the distribution of the encountered state-space using a variational autoencoder (VAE) and construct upper and lower-bound barrier-certificates using latent characteristics of states to optimize for regions of known, safe behaviour with high confidence. We frame this in our work as a dual optimization problem where the lower-bound barrier-certificate presents a more conservative estimate of the safe region than the upper-bound barrier-certificate. Sampling states that lie within the set difference of the two during training, i.e. the non-robust region, allows us to tighten the upper and lower bounds to provide sharper probabilistic guarantees on safety. Within our study, we describe the guarantees placed and demonstrate the tightness of our bounds experimentally.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：深度强化学习（RL）策略在真实世界部署时容易受到**转移扰动（transition perturbations）** 的影响，导致进入未充分探索的状态，产生不安全行为。现有概率屏障证书（probabilistic barrier-certificates）方法虽然能给出安全概率的上下界，但**上下界之间的差距（gap）可能很大**，导致安全保证不够尖锐。
- **背景**：传统安全验证依赖模型检查或已知动力学，难以应用于复杂模型无关环境。近期研究采用**近似正确（PAC）保证**来量化违反安全约束的概率，但边界紧致性受限于探索不足或状态分布偏移。
- **本文目标**：通过生成位于“非鲁棒区域”（上下界屏障证书之间的状态）的场景，**收紧安全概率的上下界**，提供更尖锐的近乎肯定安全（Probably Approximately Safe）保证，从而提升RL策略的鲁棒性。

## 2. 方法论：核心思想、关键技术细节与算法流程
- **核心思想**：将安全保证建模为**对偶机会约束规划（dual CCP）**，分别构造**上界屏障证书（upper-bound barrier certificate, B₂）** 和**下界屏障证书（lower-bound barrier certificate, B₁）**。下界证书对应更保守的安全区（违反概率 ≤ ε₁），上界证书对应更宽松的安全区（违反概率 ≤ ε₂，ε₂ ≥ ε₁）。两者在潜在空间（latent space）中的集合差即为“非鲁棒区域”。通过生成并训练该区域内的状态，可以收紧 ε₁ 与 ε₂ 的差距。
- **关键技术细节**：
  - **变分自编码器（VAE）**：将观测状态编码为潜在变量 z，重构状态分布。利用VAE的潜在高斯分布特性，便于插值和生成。
  - **线性屏障函数**：B(𝑧) = 𝑤·𝑧 + 𝑏，在潜在空间中定义安全/不安全边界。
  - **场景优化（Scenario Optimization）**：基于[N]个采样轨迹，利用Camp等人定理（Theorem 5）和推论（Corollary 6）给出ε的PAC界：  
    \[
    \epsilon \ge \frac{1}{N}\left(k + |\phi| - 1 + \sqrt{2k\ln\frac{1}{\delta} + 2(|\phi|-1)\ln\frac{1}{\delta}}\right)
    \]
    其中 k 为观察到的违反约束的轨迹数，|φ| 为优化参数数量，δ 为置信度。
  - **算法流程**（Algorithm 1 & 2）：
    1. **Phase 1**：训练基础RL策略（PPO）和VAE。
    2. **构建屏障证书**（Algorithm 1）：同时优化上界和下界线性屏障函数，利用安全/不安全轨迹更新参数。
    3. **Phase 2**（Algorithm 2）：采样位于“非鲁棒区域”（B₁(z)<0 且 B₂(z)>0）的潜在状态，通过VAE解码生成新状态，并加入初始状态集继续训练，重复收紧边界。
- **两种实现方法**：① 利用VAE解码生成状态后由专家初始化（需可配置模拟器）；② 模型无关方法：通过潜在空间邻域内采样不安全状态的相邻状态来生成新初始状态。

## 3. 实验设计
- **环境/场景**：
  - **CartPole**（经典控制，低维，可达性问题）
  - **Ant**（复杂高维机器人环境）
  两者均来自 OpenAI Gym（Gymnasium）库。
- **基准方法**（Phase 2对比）：
  - Vanilla PPO（基线）
  - Epsilon-Greedy PPO（ε=0.2）
  - Perturbed Exploration（对转移函数加高斯噪声）
  - CoDE（环境生成方法）
  - Genetic Curriculum（遗传课程学习）
  - Curriculum Adversarial Training（对抗课程训练）
- **策略学习**：使用 PPO（stable-baselines3 实现），所有方法在Phase 1均用PPO训练 1e5 个episode，然后在Phase 2继续训练 1e5 episodes。
- **VAE**：标准变分自编码器，ELBO损失，KL散度加权系数0.6，每Episode后批量更新。
- **参数设置**：N=1000（采样轨迹数），δ=0.001，β=0.05（邻域半径），α=0.01（学习率）。取多种随机种子（seeds）的平均结果。

## 4. 资源与算力
- 论文**未明确说明**使用的GPU型号、数量、训练时长或总算力。仅提及实验基于默认stable-baselines3实现，未提供CPU/GPU规格。因此无法量化算力消耗。

## 5. 实验数量与充分性
- **实验数量**：
  - 在**两种环境**（CartPole, Ant）下进行。
  - 对比了**6种Phase 2方法**加上本文方法，共7种，每种方法在Phase 2训练后报告(ε₁, ε₂)和平均奖励。
  - 还提供了**图1**展示边界紧致性随采样数N的变化（仅CartPole）。
  - 未列出消融实验（如是否用VAE、不同β值、不同初始种子数的统计方差等）。
- **充分性与客观性**：
  - 对比方法较全面，覆盖了扰动、基于课程、对抗等多种策略。
  - 使用**归一化平均奖励**（[0,1]）便于比较，报告了(ε₁, ε₂)值。
  - **不足**：缺乏错误条/标准差，未报告跨种子方差；未做统计显著性检验。仅两个环境，泛化性有限。缺少对VAE潜在空间表征质量的直接评估。

## 6. 主要结论与发现
- **边界紧致性提升显著**：本文方法在Phase 2后，两个环境的(ε₁, ε₂)差距均明显缩小（Ant: 0.11,0.41 → 0.09,0.18；CartPole: 0.21,0.48 → 0.10,0.15），优于所有对比方法。
- **性能（平均奖励）可比或接近最优**：Ant上本文奖励0.94，仅次于Curriculum Adversarial Training（0.96）；CartPole上本文0.96，仅次于CoDE（0.97）。
- **对比其他方法**：简单的探索策略（epsilon-greedy、加噪声）对收紧边界帮助不大，而直接生成非鲁棒区域状态效果更好。Curriculum Adversarial Training在CartPole表现接近，但在Ant上差距较大，说明本文方法在复杂环境下鲁棒性更强。
- **图1**：随着采样数N增加，边界差距减小，且本文方法收敛更快。

## 7. 优点（方法与实验亮点）
- **方法创新**：首次将**VAE潜在空间**与**对偶屏障证书**结合用于安全RL场景生成，直接针对“非鲁棒区域”进行采样，而非随机探索。
- **理论保证**：提供了PAC概率界（基于场景优化），可量化安全置信度。
- **模型无关**：Algorithm 2不依赖环境动力学，仅需生成初始状态，降低了应用门槛。
- **实验公平性**：统一使用PPO作为基础算法，Phase 1训练量一致，对比方法代表性强。
- **可视化边界收敛**：图1直观展示了边界随采样数改善的趋势。

## 8. 不足与局限
- **实验覆盖有限**：仅两个环境（CartPole简单，Ant中等），未在更复杂（如Atari、自动驾驶模拟器）或高维图像任务上验证。
- **缺乏统计细节**：未报告多次运行的方差、置信区间、t检验，结果可能受随机性影响。
- **VAE训练无特殊优化**：未讨论VAE的重建质量对结果的影响，潜在空间线性可分性未验证。
- **计算开销**：Phase 2需要重复采样和训练VAE，但未与对比方法比较计算时间或sample效率。
- **环境限制**：Algorithm 2依赖“潜在空间距离反映状态相似性”的假设，并非所有任务都成立。
- **未讨论状态分布偏移（distribution shift）**：假设训练轨迹分布与部署分布一致，实际中可能不成立。

（完）
