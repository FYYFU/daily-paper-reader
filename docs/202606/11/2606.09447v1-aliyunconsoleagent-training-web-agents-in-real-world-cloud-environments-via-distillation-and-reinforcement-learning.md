---
title: "AliyunConsoleAgent: Training Web Agents in Real-World Cloud Environments via Distillation and Reinforcement Learning"
title_zh: AliyunConsoleAgent：通过蒸馏与强化学习在真实云环境中训练网页代理
authors: "Bojie Rong, Zheyu Shen, Qiaoping Wang, Pengfei Kang, Yang Xu, Yawen Wei, Hanyu Wu, Zhi Zhao, Leihao Pei, Linquan Jiang"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09447v1"
tags: ["query:opd"]
score: 9.0
evidence: 通过蒸馏和强化学习训练Web智能体
tldr: "云平台控制台UI与文档常不一致，人工验证覆盖率不足1%。提出AliyunConsoleAgent，采用蒸馏前沿模型轨迹进行监督微调，再通过GRPO和双通道奖励模型在真实云环境中强化学习。构建基于Terraform和LLM的高确定性rollout系统，结合后端审计日志实现抗奖励黑客的评估。在278任务基准上，32B模型达63.52%成功率，仅落后最佳前沿模型1.82个百分点，推理成本降低92%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09447v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 842, \"height\": 489, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09447v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 637, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09447v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1094, \"height\": 459, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09447v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1147, \"height\": 625, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09447v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 761, \"height\": 278, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09447v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 615, \"height\": 525, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09447v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1104, \"height\": 366, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09447v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 837, \"height\": 240, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09447v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 557, \"height\": 206, \"label\": \"Table\"}]"
motivation: 云平台产品迭代快，控制台UI与文档频繁偏离，人工验证覆盖率极低，前沿模型成本高且存在数据隐私限制。
method: 两阶段训练：先蒸馏前沿模型轨迹进行SFT，再通过GRPO和双通道奖励模型在真实云环境中强化学习，配合高确定性rollout和审计日志评估。
result: "在278任务基准上，模型成功率从基础模型提升20.24个百分点至63.52%，与最佳前沿模型差距仅1.82pp，推理成本降低92%。"
conclusion: 证明了结合蒸馏与强化学习可让开源模型在真实云环境中接近前沿模型性能，同时大幅降低成本，实现自动化文档验证的可行方案。
---

## 摘要
我们提出AliyunConsoleAgent，一个用于真实云控制台自动化文档验证的网页代理框架。主流云平台包含数百个产品，功能迭代迅速，导致控制台用户界面频繁与对应文档产生偏差。验证文档流程是否准确反映当前控制台且可端到端执行，预计每年需进行400万次重复检查，但人工覆盖率仍低于1%。尽管基于前沿专有模型的代理系统成功率较高，但其高昂成本和数据隐私限制阻碍了大规模部署。我们提出两阶段训练范式：首先在蒸馏的前沿模型轨迹上进行监督微调（SFT），随后在真实云环境中使用组相对策略优化（GRPO）和双通道结果奖励模型进行强化学习。为支持大规模强化学习训练，我们构建了一个高确定性推理系统，具备基于Terraform的资源预置和LLM驱动的按需置备，有效隔离环境噪声与训练信号。我们进一步引入基于后端审计日志的规则化奖励评估协议，提供客观、抗奖励攻击的结果判断。我们的模型从机械式指令跟随演变为具备云控制台和产品特定理解的自主决策。在最具挑战性的278任务基准测试中（最佳前沿模型仅达65.34%），AliyunConsoleAgent-32B实现了63.52%的平均成功率——相比基础模型提升20.24个百分点，将差距缩小至最佳前沿专有模型的1.82个百分点（bootstrap 95% CI [-1.27, 7.39]），同时推理成本降低92%。

## Abstract
We present AliyunConsoleAgent, a web agent framework for automated documentation verification in real-world cloud consoles. Major cloud platforms encompass hundreds of products with rapid feature iteration, causing console UIs to frequently diverge from their corresponding documentation. Verifying that documented procedures accurately reflect the current console and can be executed end-to-end demands an estimated 4 million recurring inspections annually, yet manual coverage remains below 1%. While agent systems built on frontier proprietary models achieve high success rates, their prohibitive cost and data privacy constraints preclude large-scale deployment. We propose a two-stage training paradigm: supervised fine-tuning (SFT) on distilled frontier-model trajectories, followed by reinforcement learning using Group Relative Policy Optimization (GRPO) and a dual-channel outcome reward model in real cloud environments. To support large-scale RL training, we construct a high-determinism rollout system featuring Terraform-based resource pre-provisioning and LLM-driven on-demand provisioning, which effectively isolates environment noise from the training signal. We further introduce a rule-based reward evaluation protocol grounded in backend audit logs, providing objective, reward-hacking-resistant outcome judgment. Our model evolves from mechanical instruction following to autonomous decision-making with cloud console and product-specific understanding. Experiments on a challenging 278-task benchmark where the best frontier model achieves only 65.34% demonstrate that AliyunConsoleAgent-32B achieves a 63.52% mean success rate -- a 20.24 percentage-point improvement over the base model, narrowing the gap to the best frontier proprietary model to 1.82 pp (bootstrap 95% CI [-1.27, 7.39]) -- at 92% lower inference cost.

---

## 论文详细总结（自动生成）

# AliyunConsoleAgent: 通过蒸馏与强化学习训练真实云环境中的网页代理

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**: 云平台产品迭代速度快，控制台UI频繁更新，而文档由人工维护，两者之间逐渐产生偏差（文档漂移）。检测这种不一致需要大量人工检查——估计每年需400万次重复检查，每次约1.6小时，但当前人工覆盖率不足1%。
- **现有方案困境**: 使用前沿专有模型（如Gemini、GPT）构建的代理虽能实现高成功率，但成本高昂（每任务约7–15.5 CNY），且传输敏感云元数据到外部服务存在数据隐私合规风险。
- **需求**: 训练一个轻量级、可私有部署的模型（32B参数），在真实生产云环境中实现自动化文档验证，兼顾性能与成本。

## 2. 方法论

### 核心思想
采用“监督微调（SFT）→ 强化学习（GRPO）”两阶段训练范式，将模型从机械式指令跟随提升为具备云控制台领域知识的自主决策体。关键创新在于构建高确定性Rollout环境以隔离环境噪声，并使用基于后端审计日志的规则化奖励实现客观、抗奖励攻击的评估。

### 关键技术细节
1. **POMDP形式化建模**: 将代理与控制台交互建模为部分可观测马尔可夫决策过程，状态包含可见UI和隐藏后端资源配置。动作空间包括GUI交互、按需环境置备动作、任务完成/不可完成信号。
2. **WebAgent框架**: 遵循ReAct范式，每步获取带Set-of-Mark标注的截图和DOM文本，输出Thought推理链后选择动作。引入ResourceCoder（LLM驱动的编码代理）处理环境依赖缺失，通过Terraform脚本动态置备资源。每个子步骤分配40步交互预算。
3. **Rollout环境（核心创新）**: 
   - 四层架构：账户池管理、沙箱执行（ACK容器化）、资源META离线置备（Terraform模板）、运行时按需置备（ResourceCoder）。
   - 资源META五元组M = ⟨D, K, C_create, C_verify, C_destroy⟩，保证跨账户可移植和可销毁恢复。
   - 离线META覆盖显式依赖，运行时覆盖隐式依赖，形成“置备-执行-恢复-销毁”闭环。
   - 效果：在ECS上将空账户并行执行的33.81%成功率提升至84.39%（+50.58pp），同时大幅缩短审计时间。
4. **两阶段训练**:
   - **SFT阶段**: 使用约16万单步样本，包括两类数据：蒸馏的前沿模型成功轨迹（经任务级和步骤级过滤）+ 自主探索轨迹（模型自由交互产生的成功路径）。全参数微调Qwen3-VL-32B-Instruct，64 GPU，DeepSpeed ZeRO-2，学习率1e-5，余弦调度，BF16，1 epoch。
   - **GRPO阶段**: 
     - 奖励模型：双通道ORM。通道1基于ActionTrail审计日志，对确定性成功标准给出二值奖励（0假阳性）；通道2使用两个强通用LLM作为集成裁判（仅当一致时采纳，否则排除）。验证集合上达96.7%准确率（vs单一模型91.9%）。
     - 两层优势归一化：先按任务组内标准化，再跨任务全局标准化，解决任务难度差异和同质rollout无效问题。
     - 策略更新：GRPO裁剪替代目标 + per-token MSE形式KL惩罚（代替k3估计器），稳定梯度。
     - 超参数：裁剪范围ε=0.2，KL系数β=0.1，组大小G=16（1.25倍过采样），学习率5e-7。

## 3. 实验设计

### 数据集/场景
- **单步评估**: 从各产品轨迹中随机采样400个状态-动作对，每样本标注多个有效动作，评估每模型3次取平均。
- **端到端评估**: 278个真实云产品文档验证任务，覆盖12个阿里云产品，包括76个标准任务（与线上分布一致）和202个难任务（历史执行失败率高）。所有任务仅使用规则化ActionTrail审计奖励，且评估集与训练集完全隔离（文档级别）。

### Benchmark指标
- **pass@1**: 每任务独立运行3次，报告均值和标准差。
- **pass@3**: 至少一次成功即计为成功。

### 对比方法
- 基础模型：Qwen3-VL-32B-Instruct
- 开源/自有变体：AliyunConsoleAgent-32B (SFT), AliyunConsoleAgent-32B (SFT+GRPO)
- 前沿API模型：Qwen3.6-Plus, Kimi K2.6, Gemini 3 Pro Preview, GPT-5.5

### 对比公平性
- 所有模型共享相同的Rollout环境（Terraform预置 + ResourceCoder动态置备）。
- 私有部署的AliyunConsoleAgent使用4×L20 GPU（0.4 QPS），每任务约0.56 CNY；API模型按实际调用计费。
- 评估使用规则化ActionTrail奖励，避免LLM-as-Judge的主观偏差。

## 4. 资源与算力

- **SFT阶段**: 64 GPU（未具体说明型号，推测为A100或类似），DeepSpeed ZeRO-2，BF16精度，1 epoch。
- **GRPO阶段**: 算力说明较模糊。训练集群仅用于参数更新，Rollout由远程服务（专用推理端点）执行。未明确给出GRPO所用GPU数量或训练时间。
- **推理部署**: 私有部署AliyunConsoleAgent-32B使用4×L20 GPU，QPS约0.4，每任务推理成本约0.56 CNY。
- **综合评估**: 论文未披露完整训练的总GPU小时数，仅给出了各阶段的硬件配置概览。

## 5. 实验数量与充分性

- **单步评估**: 400个样本，3次重复平均，对比了8个模型（含基础、SFT、前沿模型）。
- **端到端评估**: 278任务，3次独立运行，覆盖12个产品，区分标准/难任务，呈现pass@1和pass@3。
- **消融实验**: 比较了Base、SFT、SFT→GRPO三种训练范式，报告了95%置信区间和统计显著性。
- **定性分析**: 分析了7.2节中的9个从失败转为成功的任务案例，以及7.3节中的错误分类（资源置备、UI交互、代理决策三类）。
- **充分性评价**: 实验设计总体充分且客观：
  - 使用规则化ActionTrail奖励替代LLM判断，避免主观偏差；
  - 评估集与训练集文档级别隔离；
  - 报告了置信区间和p值（GRPO相对SFT提升显著，与Gemini差距不显著）；
  - 进行了单步和端到端两层次评估。
  - 不足：缺少在更多云平台（如AWS、Azure）上的跨平台泛化实验；GRPO消融未探索不同KL惩罚形式、组大小等超参数影响。

## 6. 主要结论与发现

- **性能**: AliyunConsoleAgent-32B (SFT+GRPO) 在278任务上达到63.52% pass@1，相比基础模型提升20.24个百分点，相比仅SFT提升6.63个百分点（95% CI [2.97, 9.64]，显著）。与最佳前沿模型Gemini 3 Pro Preview的差距仅1.82个百分点（95% CI [-1.27, 7.39]，不显著）。
- **成本效益**: 推理成本降低92%（从7.0 CNY降至0.56 CNY），使全量覆盖的周期性验证变得可行。
- **能力进化**: SFT提供基础控制台交互能力；GRPO进一步赋予自主决策能力，如前置条件构建（先启用再禁用以验证）、自适应路径调整（绕过不存在的UI按钮）。
- **生产部署**: 基于前沿模型的框架已审计54,000+流程，确认4,399个缺陷（91%确认率，76%端到端成功率）。训练后的32B模型可作为即时替代，预计年节省约35万CNY。

## 7. 优点

1. **创新性的Rollout环境设计**: 通过Terraform预置+ResourceCoder动态置备+账户隔离，彻底消除了训练中环境噪声对策略梯度的污染，是RL在真实生产环境成功应用的关键。
2. **抗奖励攻击的评估机制**: 采用ActionTrail审计日志的规则化奖励，而非LLM-as-Judge，避免模型刻意讨好裁判产生的虚假成功。双通道ORM进一步通过集成共识提高可靠性。
3. **两阶段优势归一化**: 解决了任务难度差异大时梯度偏斜的问题，并避免了同质rollout（所有组内结果相同）产生的无效优势。
4. **开源与可复现**: 公开了评估benchmark、筛选训练数据、Rollout环境基础设施和代码，利于社区复现和改进。
5. **实际部署验证**: 不仅有离线实验，还给出了生产环境部署的规模、缺陷发现率和成本节约预测，具有较强的实用价值。

## 8. 不足与局限

1. **实验覆盖偏窄**: 仅针对阿里云产品，未在其他云平台（AWS、Azure、GCP）验证，结论的泛化性存疑。
2. **算力与训练细节模糊**: GRPO阶段的GPU数量、训练时间、总计算成本未明确披露，不利于复现和成本估算。
3. **Rollout环境维护成本**: 静态Terraform模板随云API更新而失效，需定期用前沿模型重新生成，产生持续成本。论文承认这是一个局限性。
4. **视觉标注依赖**: 依赖Set-of-Mark（基于DOM解析）进行元素识别，对复杂前端（日期选择器、文件上传器等）兼容性差，且DOM解析逻辑维护成本高。
5. **错误分析中的资源置备失败**: 最大失败类别是资源置备不足（缺失前置、状态错误等），虽然提出了扩展模板策略，但未实际评估该方案的有效性。
6. **RL超参数敏感性**: 仅采用一组默认超参数，未进行消融分析（如组大小、KL系数、学习率等），难以判断当前设置是最优还是次优。
7. **潜在的评估偏差**: 尽管用了规则化奖励，但任务成功标准由专家设计的验证规则定义，若规则覆盖率不全，可能漏掉某些正确执行但未被捕获的路径。另外，仅使用ActionTrail事件可能无法反映所有操作（如配置页面内文本修改）。

（完）
