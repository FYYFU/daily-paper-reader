---
title: Weight-Space Geometry of Offline Reasoning Training
title_zh: 离线推理训练的权重空间几何
authors: "Aleksandr Nikolich, Igor Kiselev, Vladimir Platonov, Karina Romanova"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.23740v1"
tags: ["query:opd"]
score: 9.0
evidence: 使用离线强化学习损失进行推理蒸馏
tldr: "本研究探究多种离线推理训练损失函数（SFT, RFT, DFT, RIFT, Offline GRPO, DPO）在权重空间中的几何差异。通过在Qwen3-4B模型上进行注意力LoRA微调，分析权重更新的方向相似性。结果显示SFT、RFT和RIFT的更新几乎共线，而DPO位于近正交子空间且存在模式连接障碍。该工作揭示了不同损失函数在机制上的本质区别，为选择训练策略提供了几何视角。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23740v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 760, \"height\": 653}, {\"url\": \"assets/figures/arxiv/2606.23740v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1752, \"height\": 418}, {\"url\": \"assets/figures/arxiv/2606.23740v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 853, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2606.23740v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 852, \"height\": 343}, {\"url\": \"assets/figures/arxiv/2606.23740v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 857, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2606.23740v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 854, \"height\": 340}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23740v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 865, \"height\": 422}, {\"url\": \"assets/tables/arxiv/2606.23740v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 691, \"height\": 275}]"
motivation: 现有离线推理训练损失在准确率上被比较，但缺乏对其权重更新机制差异的深入理解。
method: 在Qwen3-4B上使用注意力LoRA，训练六种方法，通过余弦相似度、主角度、线性模式连接和CKA分析权重更新几何。
result: "SFT/RFT/RIFT更新几乎共线（余弦≥0.97），DPO更新近正交（CKA降至0.46），且DPO准确率最高（GSM8K 93.5%），但其学习率小10倍。"
conclusion: 离线推理训练损失在权重空间几何上存在显著差异，DPO学习机制独特，但性能优势与超参数有关，未来需统一学习率比较。
---

## 摘要
离线强化学习损失（RFT、RIFT、DFT、Offline GRPO、DPO）被广泛用于将推理能力从大教师模型蒸馏到小学生模型中，并且通常仅通过下游准确率进行比较。我们探究它们在机制上是否不同，还是收敛到相似的权重更新。针对同一基础模型（Qwen3-4B）的相同数学数据，使用仅注意力LoRA训练六种方法（SFT、RFT、DFT、RIFT、Offline GRPO、DPO），我们通过余弦相似度、主角度子空间分析、线性模式连通性和CKA分析所得的增量。我们观察到：(i) SFT、RFT和RIFT的权重增量几乎共线（余弦≥0.97，144个模块上主角度中位数约7度），GSM8K准确率相当（87-88%，n=1319；成对McNemar p≥0.15）；(ii) DFT的方向偏离程度大于任何奖励加权方法，尽管使用相同数据；(iii) Offline GRPO在SFT损失盆地内增加了一个与SFT方向正交的显著分量（全局约67%，后期层高达约86%）；(iv) DPO位于几乎正交的子空间中，显示模式连通性障碍，并将后期层CKA降至约0.46。DPO在我们的协议下同时达到了GSM8K（93.5%，相对于其他每种方法的McNemar p<10^-9）和AIME26（30.0%对比3.3-10.0%）的最高准确率；其训练使用比其他方法小10倍的学习率（标准惯例），因此更新范数和准确率差距共同反映了损失函数和优化器选择，学习率匹配的DPO比较留待未来工作。

## Abstract
Offline reinforcement-learning losses (RFT, RIFT, DFT, Offline GRPO, DPO) are widely used to distill reasoning from large teachers into smaller students, and are typically compared on downstream accuracy alone. We ask whether they are mechanistically distinct or converge to a similar weight update. Training six methods (SFT, RFT, DFT, RIFT, Offline GRPO, DPO) on identical math rollouts from a single base model (Qwen3-4B) with attention-only LoRA, we analyze the resulting deltas via cosine similarity, principal-angle subspace analysis, linear mode connectivity, and CKA. We observe: (i) SFT, RFT, and RIFT have nearly colinear weight deltas (cosine >= 0.97, top-1 principal angle ~7 deg median over 144 modules) and comparable GSM8K accuracy (87-88%, n=1319; pairwise McNemar p >= 0.15); (ii) DFT diverges further in direction than any reward-weighted method despite using the same data; (iii) Offline GRPO adds a substantial component orthogonal to the SFT direction (~67% globally, up to ~86% in late layers) while staying in the SFT loss basin; (iv) DPO sits in a near-orthogonal subspace, shows a mode-connectivity barrier, and collapses late-layer CKA to ~0.46. DPO also reaches the highest accuracy in our protocol on both GSM8K (93.5%, McNemar p < 10^-9 vs. each other method) and AIME26 (30.0% vs. 3.3-10.0%); its training uses a 10x smaller learning rate than the others (the standard convention), so the update-norm and accuracy gaps reflect loss-function and optimizer choices jointly, and a learning-rate-matched DPO comparison is left for future work.

---

## 论文详细总结（自动生成）

# 离线推理训练的权重空间几何 —— 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：当前大量离线强化学习损失函数（如 RFT、RIFT、DFT、Offline GRPO、DPO）被用于将大教师模型的推理能力蒸馏到小模型，但这些方法通常仅通过下游准确率进行比较，**不清楚它们是否在机制上存在本质区别，抑或收敛到相似的权重更新**。这种区分的缺失对实践者（该实现哪种损失？）和可解释性研究者（“离线 RL” 是单一机制还是家族？）都构成困扰。
- **整体含义**：论文首次从**权重空间几何**的角度系统比较了六种主流离线推理训练损失，揭示了它们在更新方向、子空间结构、线性模式连通性和表示相似性上的显著差异，为理解不同损失函数的机理提供了基础性洞察。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：在完全受控的实验条件下（相同基础模型、相同数据集、相同 LoRA 初始化、相同训练超参数），仅改变损失函数，通过分析各方法 LoRA 权重增量 ΔW 的几何特性（方向、子空间、盆地结构）来理解损失函数之间的本质关系。
- **关键技术细节**：
  - **基础模型**：Qwen3-4B-Instruct（主要实验）和 Llama-3.2-3B-Instruct（复现验证）。
  - **微调方式**：仅对注意力投影层（q, k, v, o proj）施加 LoRA（rank=32, α=64, dropout=0），共 144 个模块（36 层 × 4 个投影），其余参数冻结。
  - **共享数据**：所有离线方法使用同一组 rollouts：DeepScaleR 提示（~40k 数学题），教师模型 DeepSeek-V4-Flash，每个提示生成 4 条 CoT 回答，二元数学验证奖励。
  - **损失函数**（简化数学表示）：
    - SFT：对所有序列求负对数似然 ℓᵢ = -log πθ(yᵢ|x) 之和。
    - RFT：仅对正样本（奖励=1）求 ℓᵢ 之和。
    - DFT：对每个 token 的损失乘以 sg(πθ(yt|y<t,x))，即根据当前模型置信度下加权。
    - RIFT：对每个序列乘以 (1 - λ·rᵢ) 加权（线性奖励加权）。
    - Offline GRPO：对每个序列乘以优势 Âᵢ = rᵢ - mean(r) + bias，其中 bias=0.1。
    - DPO：在偏好对（chosen, rejected）上使用对比损失：-log σ(β [log(πθ(yw)/πref(yw)) - log(πθ(yl)/πref(yl))])，β=0.1。
  - **分析指标**：
    - 全局/逐层余弦相似度
    - 主角度（principal angles）分析 top-k 奇异子空间
    - 线性模式连通性（linear mode connectivity）：沿 ΔW 线性插值，评估损失盆地
    - CKA（Centered Kernel Alignment）：比较合并模型隐层表示
- **算法流程**（文字说明）：
  1. 教师模型生成固定 rollout 数据集。
  2. 对同一基础模型（Qwen3-4B）施加 LoRA，随机初始化 A、B 矩阵。
  3. 分别用六种损失函数训练 1500 步（有效 batch 32，cosine 学习率调度，5% warmup，权重衰减 0.01，梯度裁剪 1.0，随机种子 42，bf16 混合精度）。
  4. 提取每个方法训练后的 ΔW = BA。
  5. 计算 ΔW 之间的余弦相似度、SVD 分解、主角度等。
  6. 对 ΔW 进行线性插值，评估模式连通性障碍。
  7. 将 ΔW 合并到基础模型，计算 CKA。

## 3. 实验设计

- **数据集**：
  - 训练数据：DeepScaleR 提示（~40k 数学题），来自 DeepSeek-V4-Flash 教师模型生成的 4 条 CoT 完成。
  - 评估基准：
    - GSM8K（full set, n=1319，数学问题）。
    - AIME26（n=30，高难度数学竞赛）。
- **对比方法**：
  - 离线方法：SFT、RFT、DFT、RIFT、Offline GRPO、DPO。
  - 线上方法（额外实验）：Online GRPO、Online DAPO（用于对比方向差异来源）。
  - 基础模型本身（Qwen3-4B-Instruct）作为基线。
- **评价指标**：
  - 主要：贪婪解码 pass@1 准确率。
  - 几何指标：全局余弦相似度、逐层余弦、主角度、正交分量比例、线性模式连通性（CE on masked answer）、CKA。
  - 统计检验：McNemar 精确检验（p 值）。
- **控制变量**：
  - 所有离线方法使用完全相同的 rollout 数据。
  - 相同 LoRA 随机初始化种子（种子 42 为主要，种子 123 用于敏感性）。
  - DPO 使用标准惯例的学习率（5e-7），比其他方法（5e-6）小 10 倍，这是已知的不匹配，作者将其明确列为局限。
  - 所有方法训练步数相同（1500 步），报告步 1000 的结果。

## 4. 资源与算力

- **文中未明确说明** GPU 型号、数量或训练总时长。文中仅提及：
  - 使用 bf16 混合精度训练。
  - 有效 batch size 32，cosine 学习率调度，5% warmup。
  - 每个实验训练 1500 步。
  - 此外，进行种子敏感性实验（2 个种子 × 3 个学习率 × 6 种损失？实际有限）、在线方法训练（Online GRPO 600 步）。
  - 推测算力需求中等（单/双 GPU 即可在较短时间内完成，但未量化说明）。

## 5. 实验数量与充分性

- **主要实验**：6 种损失函数 × 2 种评估集（GSM8K, AIME26）= 12 个主要准确率结果；几何指标覆盖 144 个模块，统计可靠。
- **消融与敏感性分析**：
  - 种子敏感性：2 个种子（42, 123）。
  - 学习率敏感性：3 个学习率（5e-7, 5e-6, 5e-5），但仅对 SFT 进行。
  - 跨架构复现：在 Llama-3.2-3B 上重复几何分析（图 4-6）。
  - 在线训练比较：增加 Online GRPO 和 Online DAPO 的对比（表 2）。
- **充分性评估**：
  - **优点**：控制严格、分析深入（多维度几何指标）、跨架构确认。
  - **不足**：
    - AIME26 样本量仅 30，统计效力低（SFT vs DPO p=0.07）。
    - DPO 学习率不匹配导致无法公平比较准确率。
    - 仅覆盖单一领域（数学推理）和单一模型规模（4B/3B），未扩展到更大模型或其他任务。
    - 未包含更广泛的对比族（如 KTO、IPO、SimPO、Cal-DPO），仅提及留作未来工作。
    - 仅使用注意力 LoRA，MLP 层冻结，可能限制泛化性。
  - 总体而言，实验设计在受控比较上充分，但因 DPO 学习率问题和高难度 AIME 统计力不足而存在一定偏差风险。

## 6. 主要结论与发现

1. **SFT、RFT、RIFT 几乎共线**：余弦相似度 ≥ 0.97，主角度中位数 ~7°，逐层 CKA > 0.99，GSM8K 准确率无统计显著差异（87-88%，McNemar p ≥ 0.15）。更新方向完全相同，仅步长略有差异。这些方法本质上是同一种更新方向。
2. **DFT 的方向偏移比任何奖励加权方法更大**：尽管 DFT 未使用奖励信息，其 ΔW 与 SFT 的余弦仅为 0.57，表明其基于模型置信度的自加权机制对示例选择的影响比显式奖励更强。
3. **Offline GRPO 在 SFT 盆地内增加正交分量**：全局约 67% 的更新正交于 SFT 方向，后期层达 80-86%，但线性插值无障碍（仍在同一损失盆地）。其代表性是 SFT 的细化而非完全不同的方向。
4. **DPO 位于几乎正交的子空间**：余弦 ≤ 0.13，有效秩更高（24.5 vs ~16），晚期层 CKA 降至 0.46，显示明显的线性模式障碍。DPO 达到了最高准确率（GSM8K 93.5%，AIME26 30.0%），但由于学习率小 10 倍，其几何与性能差异不能完全归因于损失函数。
5. **在线采样是方向偏离的主要原因**：Online GRPO 的更新几乎完全正交于 SFT（正交分量 0.998），而 Offline GRPO 的正交分量仅 0.67，表明固定 rollout 使离线方法向 SFT 方向收敛，而在线采样打破了这种收敛。

## 7. 优点

- **严格对照**：使用完全相同的 rollout 数据、相同基础模型、相同 LoRA 配置、相同训练超参数，仅改变损失函数，因果归因清晰。
- **多维度几何分析**：不仅比较准确率，还从余弦、主角度、线性模式连通性、CKA 等角度全面刻画更新空间结构，提供了比简单性能排名更深刻的机制理解。
- **跨架构验证**：在 Qwen3-4B 和 Llama-3.2-3B 上得到一致结论，增加结果可靠性。
- **揭示出人意料的模式**：如 DFT 的方向偏移比奖励加权方法更大，以及固定 rollout 导致的方法趋同性，这些发现对实践者选择损失函数具有指导价值。
- **公开代码和适配器**：提高可复现性。

## 8. 不足与局限

- **DPO 学习率不匹配**：DPO 使用 5e-7（其他方法 5e-6），这是其代码库惯例，导致无法区分性能和几何差异是来自损失函数还是优化器设置。作者坦言需要学习率匹配的比较。
- **覆盖范围有限**：仅针对数学推理、4B/3B 规模模型、注意力 LoRA。未测试更大模型（如 7B/8B）或通用任务（如代码、对话），也未测试其他 LoRA 变体（如全参数微调）。
- **AIME26 统计力不足**：n=30，p 值边缘显著，结论需谨慎。
- **未包含完整的偏好学习家族**：仅比较了 DPO，未评估 KTO、IPO、SimPO、Cal-DPO 等。
- **单一种子种子分析**：虽然做了两个种子的敏感性，但主要几何结论基于一个种子；种子之间方向差异较大（因 LoRA A 初始随机），但证明在同一盆地内。
- **仅评估贪婪解码**：未报告 pass@k 或波束搜索结果，可能忽略多样性。
- **教师模型与 rollout 固定**：未测试不同教师或不同 rollout 质量的影响。

（完）
