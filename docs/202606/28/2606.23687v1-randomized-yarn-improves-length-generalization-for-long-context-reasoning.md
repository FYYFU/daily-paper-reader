---
title: Randomized YaRN Improves Length Generalization for Long-Context Reasoning
title_zh: 随机化YaRN改善长上下文推理的长度泛化能力
authors: "Manas Mehta, Fangcong Yin, Greg Durrett"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23687v1"
tags: ["query:l-context-rl"]
score: 9.0
evidence: 用于长上下文推理长度泛化的随机化YaRN
tldr: 大型语言模型在短序列上预训练后扩展到长序列时，难以泛化到极长上下文。本文提出Randomized YaRN方法，在短上下文训练中为token分配从更大范围采样的YaRN位置编码，结合随机位置编码和长度课程，使模型提前暴露于分布外位置表示。在BABILong和MRCR基准上，仅用8K以内数据训练即可在16K至128K上下文长度上持续提升推理性能，尤其远超分布外长度时效果显著。该方法证明了逐步暴露于分布外位置分布是实现可泛化长上下文推理的有效途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23687v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 798, \"height\": 541, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23687v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 790, \"height\": 322, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23687v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1438, \"height\": 631, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23687v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 820, \"height\": 651, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23687v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 818, \"height\": 320, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23687v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1200, \"height\": 867, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23687v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 991, \"height\": 249, \"label\": \"Table\"}]"
motivation: 现有LLM在短序列上训练后难以泛化到极长序列，需要更有效的长度泛化方法。
method: 结合YaRN位置外推、随机位置编码和长度课程，在短上下文训练中为token分配从更大范围采样的位置编码。
result: 在BABILong和MRCR上，使用8K以内数据训练即可在16K至128K上下文上持续提升推理性能，远超标准微调。
conclusion: 逐步暴露于分布外位置分布是提升长上下文推理泛化能力的有效策略。
---

## 摘要
大型语言模型（LLMs）通常在短序列上进行预训练，然后通过额外训练扩展到处理更长序列。然而，这类LLMs在进一步泛化到非常长的序列时仍然存在困难。我们提出随机化YaRN，一种通过将基于YaRN的位置外推与随机位置编码和长度课程相结合来改善长度泛化能力的训练方法。在短上下文数据的训练过程中，标记被赋予从更大位置范围中采样的YaRN位置编码，即使是在短上下文输入下，模型也会暴露于分布外的位置表示。我们在两个具有挑战性的长上下文推理基准测试——BABILong和多轮指代消解（MRCR）上评估随机化YaRN。当在上下文长度小于8K的数据上训练时，随机化YaRN在16K到128K的上下文长度上持续改善了推理性能，并优于标准微调，最大的改进出现在远超出分布的长度上。我们的结果表明，逐步将模型暴露于分布外的位置分布为可泛化的长上下文推理提供了一种有效的方法。

## Abstract
Large language models (LLMs) are typically pretrained on short sequences and then extended to work on longer sequences with additional training. However, such LLMs still struggle to further generalize to very long sequences. We propose Randomized YaRN, a training method that improves length generalization by combining YaRN-based positional extrapolation with randomized positional encoding and a length curriculum. During training on short context data, tokens are assigned YaRN positional encodings sampled from a larger position range, exposing the model to out-of-distribution positional representations even on short-context inputs. We evaluate Randomized YaRN on two challenging long-context reasoning benchmarks, BABILong and Multi-Round Coreference Resolution (MRCR). When training on data with <8K context, Randomized YaRN consistently improves reasoning performance on context lengths from 16K to 128K and outperforms standard fine-tuning, with the largest gains appearing at far out-of-distribution lengths. Our results suggest that progressively exposing models to OOD positional distributions provides an effective recipe for generalizable long-context reasoning.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLMs）通常在短序列上预训练，之后通过额外训练扩展到长序列。然而，现有方法在处理远超训练长度的上下文时，**长度泛化能力仍然不足**，尤其是在需要复杂推理的长上下文任务上表现不佳。
- **研究动机**：常见的上下文扩展方法（如继续预训练或微调大量长上下文数据）主要使长序列“分布内化”，并未赋予模型真正的泛化能力；而基于位置编码的扩展方法（如YaRN）在简单检索任务上有效，但在推理任务上对超出训练分布的长度表现参差不齐。
- **整体含义**：本文旨在通过一种**结合随机位置编码和课程学习的训练方法**，让模型即使在短上下文训练数据上也能学会泛化到极长上下文（16K–128K），从而**提升长上下文推理能力**。

## 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

- **核心思想**：在短上下文训练期间，为每个token分配从**更大长度范围随机采样**的YaRN位置编码，模拟分布外（OOD）位置，并通过**长度课程**逐步增大采样范围，使模型逐步适应越来越长的位置。
- **关键技术细节**：
  - **YaRN**：一种针对RoPE的位置外推方法，通过NTK-by-parts插值和注意力温度缩放扩展上下文长度。超参数 `s` 可缩放至 `s·L_pre`。
  - **随机位置编码（RPE）**：在训练时，从 `{1,...,L_t}` 中无放回地均匀采样 `L` 个位置（排序后），将第 `j` 个token的编码设为 `YaRN(i_j; s)`，其中 `L_t` 是当前epoch的最大采样长度（远大于训练长度 `L`）。这使模型提前看到OOD旋转角度。
  - **长度课程**：在 `T` 个训练epoch中，逐步增大采样长度 `L_t`，例如 `L_t ∈ {8K, 16K, 24K, ...}`。帮助模型先适应中等外推，再逐步推广到更大范围。
- **算法流程**（文字描述）：
  1. 初始化模型参数（使用LoRA微调所有线性层）。
  2. 对于每个epoch `t`，设置最大采样长度 `L_t`。
  3. 对每个训练batch，从 `{1,...,L_t}` 中随机采样 `L` 个不同位置，排序后得到排列 `{i_1,...,i_L}`。
  4. 将序列中第 `j` 个token的原始RoPE编码替换为 `YaRN(i_j; s)`（ `s` 为训练时YaRN缩放因子）。
  5. 使用标准语言建模损失进行LoRA微调。
  6. 推理时，直接使用标准YaRN编码（缩放因子为 `s'`），通常 `s'=s`，但也可更大以解锁额外泛化。

## 3. 实验设计：数据集、Benchmark、对比方法

- **数据集和Benchmark**：
  - **BABILong**：多跳推理任务，本文使用其中最困难的**三跳推理子集**（QA3），需要从长段落中定位三个支持事实并推理。训练数据长度 0–8K，共4K个样本；评估涵盖0K–128K共9个长度段，每段约305个样本。
  - **多轮指代消解（MRCR）**：基于多轮对话的检索+推理任务，需从长对话中定位特定助手回复并执行操作。训练用60个4–8K样本，评估共147个样本，分为5个长度段（4–8K, 8–16K, 16–32K, 32–64K, 64–128K）。
- **Baseline方法**：
  - 0-shot（无训练）
  - 0-shot + YaRN（推理时使用YaRN）
  - LoRA（标准微调，不使用YaRN）
  - Trained YaRN（使用YaRN编码进行训练和推理）
  - RPE（随机位置编码但不使用YaRN训练，推理时用YaRN）
  - **Randomized YaRN**（本文方法）
- **模型**：Qwen2.5-7B-Instruct 和 Olmo3-7B-Instruct（后者预训练已内置YaRN，因此部分baseline不适用）。

## 4. 资源与算力

- 论文中明确提到：
  - **Olmo3-7B-Instruct** 训练实验在**单张 NVIDIA A100-SXM4 GPU（80GB内存）**上运行，评估使用2张GPU。
  - **Qwen2.5-7B-Instruct** 训练实验在**单张 NVIDIA H200 GPU（80GB内存）**上运行。
  - 所有训练和评估总计约**250 GPU小时**。

## 5. 实验数量与充分性

- **实验数量**：
  - 在两个数据集（BABILong、MRCR）上各测试了两个模型（Qwen2.5、Olmo3），共4组主要对比实验。
  - 包含6种baseline（部分因模型限制未全部适用）。
  - 进行了**消融实验**（表3）：在MRCR上对比了去掉长度课程的RPE和Randomized YaRN。
  - 还有**学习率敏感性实验**（附录B.3图2）：在Olmo3+BABILong上对4个学习率进行扫描。
- **充分性与公平性**：
  - 覆盖了主流长上下文推理基准和不同架构模型。
  - 所有训练方法均使用相同的LoRA设置（r=16, α=32, dropout=0.1），超参数分别调优，确保公平。
  - 消融实验明确证明了课程机制的重要性。
  - 但仅测试了7B规模模型，未涉及更大或更小模型，泛化性有一定局限。

## 6. 论文的主要结论与发现

1. **仅推理时使用YaRN效果有限**：0-shot + YaRN仅比0-shot平均提升~1%，训练是必要的。
2. **Randomized YaRN显著优于所有baseline**：在BABILong上，Qwen2.5平均OOD准确率达90.3%（对比LoRA的83.6%）；MRCR上Qwen2.5平均72.7%（对比LoRA的64.4%），尤其在远OOD长度（64K–128K）上优势更大。
3. **长度课程至关重要**：去掉课程后，Randomized YaRN在MRCR上的OOD平均准确率从77.3%降至69.4%，RPE从75.1%降至56.8%。
4. **逐步暴露于OOD位置分布是有效的泛化策略**。

## 7. 优点

- **方法创新性**：将随机位置编码与YaNR外推相结合，并引入课程学习，思路清晰且实用。
- **高效性**：仅需少量短上下文（<8K）训练数据和低秩适应（LoRA），即可实现到128K的泛化，计算成本低。
- **实验设计严谨**：含多个baseline、两种模型、两种任务、消融实验、超参数扫描，结论可靠。
- **代码和数据开源**：可复现。

## 8. 不足与局限

- **方法适用范围有限**：主要针对推理上下文长度远大于训练长度的场景，对“训练长度内”的性能提升未涉及。
- **模型规模单一**：仅评估了7B参数模型，未测试更大模型（如13B、70B）或更小模型，泛化性未知。
- **数据限制**：仅在英文长上下文推理任务上测试，未覆盖多语言或其他任务（如生成、分类）。
- **评估规模小**：MRCR数据集很小（训练60样本，评估147样本），BABILong虽稍大但子集有限，统计显著性可能不足。
- **计算资源限制**：未进行大规模超参数搜索，最优设置可能因模型/任务不同而变化。
- **风险偏差**：训练数据均为特定基准，可能过度拟合基准分布，真实世界长上下文数据多样性不足。

（完）
