---
title: A hardware-safety-gated system for LLM-written native ARTIQ control code on a trapped-ion platform
title_zh: 用于离子阱平台上LLM编写的原生ARTIQ控制代码的硬件安全门控系统
authors: "Duanyang Wang, Lu Qi, Yuanheng Xie, Norbert M. Linke, Kenneth R. Brown"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27231v1"
tags: ["query:ai-safety"]
score: 9.0
evidence: 针对LLM智能体控制的硬件安全门控系统
tldr: LLM代理编写实验控制代码缺乏安全边界，可能损坏精密设备。本文设计硬件安全门控系统，通过MCP服务器桥接LLM与ARTIQ栈，所有工具调用需携带绑定内容的令牌，令牌通过硬件模拟或人工签发。在钙离子阱上代理自主构建校准栈并实现磁场闭环控制，在镱离子平台验证可移植性。对抗测试表明安全边界有效，发现LLM局限在于元认知而非领域知识。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27231v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1455, \"height\": 492, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27231v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1827, \"height\": 657, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27231v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1840, \"height\": 626, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27231v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1825, \"height\": 1254, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27231v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1776, \"height\": 598, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27231v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1646, \"height\": 236, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27231v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1458, \"height\": 809, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27231v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1753, \"height\": 1391, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27231v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1340, \"height\": 737, \"label\": \"Table\"}]"
motivation: LLM编写实验控制代码缺乏安全边界，可能损坏精密设备，需在无监督与人工授权间建立形式化界限。
method: 构建MCP服务器桥接LLM与ARTIQ栈，所有工具调用需携带绑定内容的令牌；令牌通过硬件模拟自动签发或人工签发。
result: 在钙离子阱实现自主校准与磁场闭环控制，在镱离子平台验证跨平台兼容，对抗攻击测试揭示安全边界有效性。
conclusion: 安全门控保证自主实验安全性，发现LLM局限在于元认知而非领域知识，为未来增强自我反思能力指明方向。
---

## 摘要
大语言模型（LLM）智能体可以编写并运行实验控制代码，这使得实验室工作能够自主进行。然而，这种自主性带来了先前工作未解决的安全问题。未经检查的代码可能损坏设备，且在人机授权/监督与智能体决策之间缺乏正式的每操作边界。我们提出了一种控制系统，在离子阱实验中引入LLM智能体，同时强制执行这样的边界。智能体通过模型上下文协议（MCP）服务器提供的工具控制现有的高级实时量子物理基础设施（ARTIQ）栈。任何工具调用必须携带与其确切内容绑定的授权令牌才能到达硬件。令牌通过两种方式之一发放：自动方式——在隔离的硬件仿真（dax.sim）中运行智能体提议的脚本，并对照预设的每设备边界检查每个操作；手动方式——由人类操作员对敏感操作进行授权。在此边界内，智能体自主开发实验，而非仅调用预置例程。我们在共陷的$^{40}$Ca$^{+}$/$^{40}$CaOH$^{+}$晶体上部署该系统，智能体自主构建完整的校准栈，并在有针对性的操作员引导下闭合跨仪器磁场稳定环路。在另一个独立的$^{171}$Yb$^{+}$平台上，我们确认了接口级别的可移植性。我们使用试图绕过令牌授权机制的对抗性脚本系统测试该机制，绘制其保护的确切边界，并优先确定下一步需要加强的位置。通过分析智能体仍需人类引导的领域，我们发现其限制在于元认知控制，即识别何时需要重新定义问题，而非领域知识本身。

## Abstract
Large-language-model (LLM) agents can write and run experimental control code. This allows laboratory work to be conducted autonomously. However, this autonomy raises a safety problem that prior work has not addressed. Unchecked code can damage the apparatus, and there is no formal, per-operation boundary between human authorization/supervision, and agent decisions. We present a control system that places an LLM agent in the loop of a trapped-ion experiment while enforcing such a boundary. The agent controls the existing Advanced Real-Time Infrastructure for Quantum physics (ARTIQ) stack through tools provided by a Model Context Protocol (MCP) server. No tool call reaches the hardware unless it carries an authorization token bound to its exact contents. Tokens are issued in one of two ways: automatically, by running the agent's proposed script in an isolated hardware simulation (dax.sim) and checking every operation against preset per-device bounds, or manually by a human operator for sensitive actions. Within this boundary the agent develops its own experiments, rather than only calling pre-built routines. We deploy the system on a co-trapped $^{40}$Ca$^{+}$/$^{40}$CaOH$^{+}$ crystal, where the agent autonomously builds a full calibration stack and, with targeted operator guidance, closes a cross-instrument magnetic-field-stabilization loop. On a separate, independent $^{171}$Yb$^{+}$ platform, we confirm interface-level portability. We systematically test token-authorization mechanism with adversarial scripts that attempt to bypass it, mapping the precise boundary of its protection and prioritizing where to strengthen it next. Analyzing where the agent still requires human guidance, we find that its limits lie in metacognitive control, namely recognizing when a problem must be re-framed, rather than in domain knowledge.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- 量子物理实验（如离子阱）需要持续校准、数据采集和参数调整，现有控制框架（如ARTIQ/Sinara）提供了精确实时控制，但实验循环本身仍高度依赖人工专家反复判断和调整。
- 大语言模型（LLM）智能体能够从自然语言描述生成可执行代码，并解释错误消息和日志，具备自动化实验循环的潜力。已有工作将LLM应用于化学、材料科学以及超导量子比特校准，但存在两个关键空白：
  - 缺乏形式化的硬件保护层：任何由LLM生成的未经检查的代码都可能损坏昂贵实验装置。
  - 缺乏可机器验证的每操作审查标准：已有工作仅定性描述人类监督，未提供确切的授权边界。
- 本文在离子阱平台上部署了基于LLM的闭环自动化系统，核心目标是：**在允许LLM智能体自主编写并执行控制代码的同时，强制执行一个严格的安全授权门控，确保没有工具调用能绕过授权到达硬件。**

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将智能体的代码编写自由与其执行权限解耦。智能体可以自由编写任意ARTIQ Python脚本，但所有工具调用必须通过安全过滤器（safety-filter）获得授权令牌后才能执行。
- **系统架构**（图1）：
  - 人类操作员提供高层目标（自然语言）。
  - LLM智能体设计实验、生成ARTIQ Python脚本，通过MCP工具调用提交给安全过滤器。
  - 安全过滤器作为MCP代理，负责授权：仅当持有绑定到确切调用内容的有效期一次令牌时，才将调用转发给`artiq-mcp`。
  - `artiq-mcp`是宿主MCP服务器，通过ARTIQ原生sipyco接口与ARTIQ master通信，执行实验并返回数据/日志。
  - 智能体观察结果、诊断失败、修订脚本，直至达成目标。
- **安全过滤器的关键机制**：
  - **自动授权管道**：
    1. **AST检查**：扫描脚本的抽象语法树，匹配对危险函数（如`os.system`, `subprocess`, `eval`）的调用，阻止已知误用模式。
    2. **硬件仿真检查**：通过`dax.sim`仿真器运行脚本，生成每步硬件操作的跟踪记录，检查所有参数（DDS频率/幅度、DAC电压、TTL脉冲等）是否在操作员预配置的每设备界限内。
      - 若任何参数超限 → 阻止调用。
      - 若调用未映射的设备/方法 → 阻止并升级至人工审查（拒绝优先策略）。
      - 如果所有操作均在界限内 → 存储脚本内容哈希（避免重复仿真），并签发令牌。
  - **人工授权**：对可能造成不可逆硬件损坏的敏感操作（如关闭阱RF），操作员单独签发可重复使用的令牌，支持审计和撤销。
  - **单次使用令牌**：每个令牌仅可用于一次特定工具调用，不可重用、不可交换、不可并发使用。
  - **审计日志**：所有调用和授权决策写入只追加日志，保证事后可重构完整因果链。
- **安全假设**：智能体并非恶意，但可能因推理错误或处理对抗性内容（如损坏的实验日志）而生成不安全的命令。保护机制不依赖智能体的合作。

## 3. 实验设计：使用了哪些数据集/场景、benchmark、对比方法

- **硬件平台**：
  - 主平台：杜克大学的共陷$^{40}\text{Ca}^+ / ^{40}\text{CaOH}^+$晶体，线性保罗阱，激光冷却、光谱、边带冷却、Rabi/Ramsey操作。
  - 第二平台：马里兰大学的独立$^{171}\text{Yb}^+$系统，仅验证接口级兼容性（无闭环物理校准）。
- **实验场景**：
  - **自主校准栈构建**（5个阶段）：Doppler冷却 → 729nm光谱 → 边带冷却 → Rabi振荡 → Ramsey干涉测量。每个阶段由简短自然语言提示启动，智能体自主生成代码、迭代优化。
  - **跨仪器磁场闭环补偿**：智能体通过单个控制循环同时控制ARTIQ（进行Ramsey测量）和外部任意波形发生器（AWG）产生60Hz抵消波形。迭代优化$T_2^*$，最终从约180μs提升至~1.5ms。
  - **对抗性边界表征**：在第二平台上对安全过滤器进行红队测试，使用1932个生成脚本和约250个内部接口测试，系统评估过滤器是否按设计工作。
- **对比方法**：论文未直接与现有LLM自动化方法进行定量对比（如覆盖率、成功率），而是以自身控制集（631个真实校准脚本）作为基准，验证零假拒绝。
- **benchmark**：对抗测试对脚本分类为“已阻止”“已批准”“故意责任划分”“残余缺陷”，并给出按原因的统计（图5、表II）。控制集脚本全部被批准。

## 4. 资源与算力

- 论文未明确提及使用的GPU型号、数量或训练时长。LLM智能体基于Anthropic的Claude Opus 4.8（运行于云端API），使用“额外努力”设置和百万token上下文窗口。
- 智能体运行在多个会话中，通过外部Markdown文件持久化目标、工作记忆和进展。
- 未在本地进行大规模模型训练，算力开销主要用于仿真运行和API调用。本地实验控制硬件为两台独立的离子阱系统。

## 5. 实验数量与充分性

- **校准栈实验**：5个阶段，每个阶段经过多次迭代（如边带冷却经历初始脉冲方案→操作员建议后改为连续方案）。实验覆盖了整个典型量子控制流程，从冷却到相干操作，最终获得合理的物理结果（如侧带热力学、Rabi振荡、Ramsey相干时间）。
- **磁场补偿**：在4种条件（补偿开/关 × 线触发/自由运行）下对比，每个条件采集16个自由演化时间的相扫描条纹，共数千次测量。操作员介入指导，但最终结果可靠。
- **对抗测试**：1932个脚本 + ~250个内部接口测试。脚本按预测结果与过滤器响应对比，控制集631个合法脚本无假拒绝。充分性较高，覆盖了直接命令、循环、寄存器写入等各类模式。但对抗测试仅在一个平台上进行，且红队假设智能体恶意（超出设计威胁模型）。
- **跨平台验证**：仅有接口级验证，无闭环物理校准，因此可移植性声明局限于基于ARTIQ的架构。
- **总体评价**：实验设计合理，数量充足，但可进一步加强：1）进行正式的A/B对比测试（与手工编写校准栈的端点比较）；2）长期无人值守运行记录；3）多模型（不同LLM）对比。

## 6. 论文的主要结论与发现

- **安全系统有效**：安全过滤器按设计工作，确定性硬件保护（无LLM在决策路径中）保证零假拒绝，所有超过配置界限的操作均被阻止。令牌单次使用、绑定内容等机制通过对抗测试验证。
- **LLM的自主能力**：智能体能够自主构建完整校准栈（从Doppler冷却到Ramsey干涉），并完成跨仪器磁场补偿，承担了参数扫描、波形合成、拟合分析等日常耗时的任务。
- **LLM的元认知局限**：在磁场补偿中，操作员多次介入引导，发现瓶颈并非缺乏领域知识（孤立测试中模型能正确回答），而是无法识别当前方法已经饱和、需要重新定义问题框架。这归因于上下文饱和效应：任务框架在对话历史中自我强化，模型无法自发突破。
- **安全边界的残留缺陷**：包括：静态代码审查无法对抗故意绕过（可通过间接构造实现）；仿真未覆盖所有设备驱动特性；存在绕过审查的独立执行路径。计划通过切换到白名单、改进仿真、关闭旁路路径来加强。
- **未来方向**：设计多智能体架构：元认知知识型智能体（不执行硬件操作）负责识别停滞并重新定义问题；工作型智能体（通过安全过滤器）执行具体任务。保持安全保证不变。

## 7. 优点：方法或实验设计上的亮点

- **严格形式化的安全边界**：首次以每调用级、绑定内容的授权令牌实现自动与人工相结合的安全门控，为LLM代理控制真实物理硬件建立了可验证的安全模型。
- **仿真驱动的自动授权**：利用成熟的DAX仿真器（`dax.sim`）在隔离环境中运行脚本，检查硬件操作参数，避免接触真实设备。策略设计巧妙：不要求完美仿真，只要求输出硬件跟踪可判断是否越界。
- **跨仪器闭环能力**：通过MCP服务器统一抽象，智能体可同时控制ARTIQ实时内核和非ARTIQ的外部仪器（如AWG），扩展性强。
- **全面的对抗性测试**：系统性地对自身安全过滤器进行红队测试，绘制保护边界并分类残余缺陷，这种自我攻击的实践在物理实验室控制系统中罕见。
- **明确的长处与局限**：不仅展示成功，还深入分析了LLM的元认知缺陷，并据此提出多智能体解决方案，具有前瞻性。
- **代码开源**：安全过滤器和`artiq-mcp`代码在GitLab公开，促进社区改进。

## 8. 不足与局限

- **实验覆盖有限**：
  - 跨平台验证仅在接口级进行，未在$^{171}\text{Yb}^+$上实现闭环物理校准，可移植性声明的实证强度不足。
  - 仅使用单一LLM模型（Claude Opus 4.8），未对比不同模型的表现，难以评估方法对模型选择的依赖性。
  - 长期无人值守实验数据缺失，无法评估现实环境中对抗性代码出现的频率。
  - 缺少与手工编写校准栈的正式A/B对比实验（作者计划未来进行）。
- **安全过滤器固有局限**：
  - **仿真完整性不足**：无法仿真所有设备驱动的高级特性（如底层寄存器写入），导致某些路径的跟踪可能不准确。
  - **静态审查不完整**：在通用编程语言中，禁止操作总能通过间接构造绕过（如别名、包装器），白名单策略虽可缓解但需维护成本。
  - **绕过审查的路径**：存在独立的文件审查/扫描路径，可执行已编写但未经授权的脚本。
  - **测量条件分支问题**：仿真仅沿一条分支执行，对于依赖实时测量结果的动态控制流无法保证安全。当前将这类脚本阻止，但限制了智能体编写更复杂实时反馈的能力。
- **应用限制**：
  - 系统紧密绑定到ARTIQ生态，扩展到其他控制栈需要重新构建等效仿真器。
  - 物理建模错误（如荒谬但参数合规的序列）不在过滤器防御范围内，依赖智能体自身闭环优化或人工监督。
  - 无法处理光束漂移等超出电子控制范围的故障，需增加自动光束控制硬件。

（完）
