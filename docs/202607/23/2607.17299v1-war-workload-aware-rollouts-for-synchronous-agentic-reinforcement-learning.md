---
title: "WAR: Workload-Aware Rollouts for Synchronous Agentic Reinforcement Learning"
title_zh: WAR：面向同步智能体强化学习的工作负载感知回滚生成
authors: "Ryan Xu, Atlas Zhao, David Bao, Frank Du"
date: 2026-07-19
pdf: "https://arxiv.org/pdf/2607.17299v1"
tags: ["query:l-context-rl"]
score: 9.0
evidence: 长视界卷展加速的agentic强化学习
tldr: 长视野rollout生成是同步agentic强化学习的系统瓶颈。WAR提出负载感知rollout系统，低负载时采用无模型投机解码SuffixDecoding复用已有轨迹后缀；高负载时转向缓存感知调度，减少KV-cache重计算和负载不均。实验表明低负载提升1.4倍，高负载提升1.6倍。WAR无需改变RL算法即可显著消除rollout瓶颈，为可扩展长上下文智能体训练提供实用方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17299v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 752, \"height\": 757}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 879, \"height\": 828}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 795, \"height\": 604}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 872, \"height\": 794}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 872, \"height\": 813}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 870, \"height\": 824}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 867, \"height\": 572}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 847, \"height\": 1294}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 864, \"height\": 582}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 865, \"height\": 571}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17299v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 883, \"height\": 224}, {\"url\": \"assets/tables/arxiv/2607.17299v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 656, \"height\": 265}, {\"url\": \"assets/tables/arxiv/2607.17299v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 399, \"height\": 244}]"
motivation: 同步agentic RL中长视野rollout生成成为主要系统瓶颈，现有方法无法适应动态负载变化。
method: WAR根据运行时负载选择优化策略：低负载下用SuffixDecoding复用后缀，高负载下用全局缓存感知调度。
result: 低负载下吞吐量提升1.4倍，高负载下提升1.6倍。
conclusion: WAR通过解码级和系统级协同优化，有效消除rollout瓶颈，支持可扩展长上下文智能体训练。
---

## 摘要
长回合回滚生成已成为智能体强化学习（RL）中的主要系统瓶颈。随着智能体与环境在多轮交互中，轨迹迅速增长至数万token，同步RL训练日益受到回滚生成的约束。我们提出WAR，一种工作负载感知的回滚生成系统，通过联合优化解码与调度，显著加速同步智能体RL。WAR基于一个关键观察：最优回滚优化策略取决于运行时负载：（1）在低负载下，WAR通过SuffixDecoding实现无模型推测解码，该技术重用先前完成轨迹的后缀模式作为未来回滚的推测草稿。与基于模型的草稿器不同，SuffixDecoding不引入额外草稿模型，避免了与回滚生成的GPU竞争。（2）在高负载下，当饱和的批处理解码限制了推测加速的余地时，WAR将优化重心转向缓存感知调度。全局调度器基于缓存局部性、轨迹进度和服务器负载，将请求分布到回滚副本之间，减少冗余的KV-cache重计算并缓解负载不均。通过将解码层的后缀重用与系统层的回滚调度相结合，WAR在不改变底层RL算法的情况下，在不同工作负载场景下实现了稳健的吞吐量提升。在低负载下，WAR将长上下文智能体回滚吞吐量提升1.4倍，在高负载下提升高达1.6倍。这些结果表明，WAR消除了同步智能体RL中的主要回滚瓶颈，并为可扩展的长上下文智能体训练提供了实用路径。

## Abstract
Long-horizon rollout generation has become the dominant systems bottleneck in agentic reinforcement learning (RL). As agents interact with environments over many turns, trajectories rapidly grow to tens of thousands of tokens, making synchronous RL training increasingly constrained by rollout. We propose WAR, a workload-aware rollout system that substantially accelerates synchronous agentic RL by jointly optimizing decoding and scheduling. WAR is built on a key observation: the optimal rollout optimization strategy depends on runtime load: (1) Under low load, WAR enables model-free speculative decoding with SuffixDecoding, which reuses suffix patterns from previously completed trajectories as speculative drafts for future rollouts. Unlike model-based drafters, SuffixDecoding introduces no additional draft model and avoids GPU contention with rollout generation. (2) Under high load, where saturated batched decoding leaves limited room for speculative speedup, WAR shifts the optimization focus to cache-aware scheduling. A global scheduler places requests across rollout replicas based on cache locality, trajectory progress and server load, reducing redundant KV-cache recomputation and mitigating load imbalance. By combining decoding-level suffix reuse with system-level rollout scheduling, WAR delivers robust throughput improvements across workload regimes without changing the underlying RL algorithm. WAR improves long-context agentic rollout throughput by 1.4x under low load and up to 1.6x under high load. These results show that WAR removes a major rollout bottleneck in synchronous agentic RL and provides a practical path toward scalable long-context agent training.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心瓶颈**：在同步智能体强化学习（RL）中，长视界（long-horizon）rollout 生成成为主要系统瓶颈。智能体与环境多轮交互导致轨迹可达数万 token，且存在长尾现象（少数极长轨迹拖慢整体进度）。
- **现有不足**：简单负载调度策略（如 least-request + sticky session）在低负载尚可，但高负载下忽略 KV-cache 局部性、轨迹进度和服务器负载，导致重复重计算和负载不均。异步 rollout 虽能缓解但引入策略陈旧和分布偏移问题。
- **整体意义**：WAR 提出**工作负载感知（Workload-Aware）** 的优化思路，将运行时负载作为第一类因素，自适应选择解码级或系统级优化，在不改变 RL 算法的前提下显著消除 rollout 瓶颈，为可扩展的长上下文智能体训练提供实用路径。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：根据运行时有效批大小（反映 GPU 利用率）切换优化策略：
  - **低负载**（批大小小）→ 启用**无模型推测解码 SuffixDecoding**，重用已完成轨迹的后缀模式作为草稿，无需额外草稿模型，避免 GPU 竞争。
  - **高负载**（批大小大）→ 关闭推测解码，启用**缓存感知调度**，减少 KV-cache 重计算和负载失衡。
- **关键技术细节**：
  - **SuffixDecoding**：基于后缀树缓存 token 序列模式，跨 prompt 和同 prompt 多 rollout 均可复用。引入跨工作器后缀树同步（每 RL step 广播轨迹）。
  - **缓存感知调度**：全局调度器为每个请求计算优先级分数 \( S = S_{\text{waiting}} + S_{\text{turn}} + S_{\text{cache}} + S_{\text{inflight}} \)，其中：
    - \( S_{\text{waiting}} \)：防止 sandbox TTL 超时（权重最大）。
    - \( S_{\text{turn}} \)：偏向估计助手轮次 \( \hat{T}_g \) 较小的短轨迹（负权重，其次大）。
    - \( S_{\text{cache}} \)：偏向缓存命中率高的副本（正权重，第三）。
    - \( S_{\text{inflight}} \)：偏向负载较低的副本（负权重，最小）。
  - **AIMD 式并发控制**：根据缓存命中率动态调整每副本最大请求数 \( W_t \)，确保至少可处理 1 个请求，上限为平均排队请求数。
  - **自适应切换**：以运行时有效批大小作为负载指标，经验确定阈值（实验中发现批大小 16 为切换点）。

## 3. 实验设计
- **数据集/场景**：SWE（软件工程）任务数据集，包含 500 个 prompt，智能体需多轮交互（文件检查、命令执行等），最大助手轮次 120。
- **基准（Baseline）**：veRL v0.8.0 默认调度策略（least-request + sticky session），禁用 WAR 的所有优化。
- **对比方法**：四种配置：
  - `veRL base`（无优化）
  - `sched only`（仅缓存感知调度）
  - `spec only`（仅 SuffixDecoding）
  - `sched spec`（两者结合）
- **主要指标**：端到端 rollout 时间、prefill 吞吐量、decode 吞吐量；辅助指标：缓存命中率、平均接受长度。
- **超参数**：温度=1.0，top_p=1.0，top_k=200，每个 prompt rollout 8 次，训练 5 步，批大小从 4 到 64 变化。

## 4. 资源与算力
- **GPU**：16 块 NVIDIA H100（2 节点，每节点 8 块，InfiniBand 互联）。
- **模型**：Qwen3-32B，使用 YaRN 扩展至 96K 上下文长度。
- **训练设置**：训练侧 TP=4, PP=2, CP=2；rollout 侧 TP=4。
- **训练时长**：未明确报告总时长，但每实验运行 5 个训练步（步数较少）。

## 5. 实验数量与充分性
- **实验组数量**：
  - 5 个批大小（4,8,16,32,64）× 4 种配置 = 20 组主实验。
  - 消融实验：调度组件消融（仅等待因子、仅转弯、仅缓存、仅负载因子各一种，批大小 16）、等待因子影响（批大小 32、64）、SuffixDecoding 独立测试（批大小 4~64，固定输入输出长度）、推测解码与无推测解码对比。
  - 额外分析：接受长度来源（跨 prompt 与 intra-prompt 对比）。
- **充分性评估**：
  - 覆盖了低负载到高负载全范围，并明确标出负载边界（批大小 16）。
  - 所有实验经过 workload 一致性筛选（prefill/decode token 和助手轮次相对差异<5%），确保公平性。
  - 报告了均值与标准差（5 个训练步），具有统计意义。
  - 消融实验充分验证了各组件的贡献及互补性。
- **客观性**：直接对比 veRL 默认调度，未引入额外不公平优势；公开了代码实现细节（7K 行 Python 代码）。

## 6. 论文的主要结论与发现
- **WAR 在全负载范围内显著加速**：低负载（批大小 4）吞吐提升约 1.4 倍（35%~53% improvement），高负载（批大小 64）提升高达 1.6 倍（63% improvement）。
- **SuffixDecoding 在低负载有效，高负载无效**：批大小≤16 时提升 48%~53%；批大小≥16 时优势消失甚至变负（原因：GPU 已饱和，验证开销抵消收益）。
- **缓存感知调度在高负载下效果突出**：批大小 64 时 prefill 吞吐提升 47%，decode 吞吐提升 47%，缓存命中率从约 0.1 提升至约 0.5。
- **两技术互补**：`sched spec` 在全部批大小上均超越单一组件，缓存调度在高负载主导，SuffixDecoding 在低负载主导。
- **接受长度可稳定达到 2.8~6.0**，表明 SWE 任务中存在重复代码片段，SuffixDecoding 适用。

## 7. 优点
- **负载感知自适应**：首次将运行时负载作为优化策略切换的首要因素，实用且有效。
- **无额外模型开销**：SuffixDecoding 为无模型方案，避免传统推测解码的草稿模型训练、同步及 GPU 竞争。
- **系统级与解码级协同**：联合优化了调度和推测解码，而非单一维度。
- **兼容性强**：不改变底层 RL 算法，可嵌入现有同步 RL 框架（基于 veRL 实现）。
- **考虑实际约束**：调度中包含 sandbox TTL 时间，防止因等待导致轨迹中断。
- **跨工作器后缀共享**：通过 broadcast 机制突破单机后缀树局限，增加历史覆盖率。
- **实验设计严谨**：进行了 workload 一致性筛选，保证对比公平。

## 8. 不足与局限
- **模型与任务单一**：仅在 Qwen3-32B 和 SWE 数据集上验证，未评估其他模型（如 Llama）或其他智能体任务（如代码生成、网页导航）。
- **训练步数少**：仅 5 步，未验证长期训练（如数百步）下的稳定性和 suffix 树累积效果。
- **阈值依赖经验**：低/高负载切换阈值（批大小 16）为经验值，未给出自动调优方法。
- **未与异步 rollout 对比**：论文强调异步会引入策略陈旧，但未进行直接性能对比（端到端吞吐 vs. 训练收敛性）。
- **SuffixDecoding 的局限性**：高负载下可能降速；且依赖轨迹中重复模式，若任务随机性大则收益降低。
- **调度复杂度增加**：全局调度器增加通信和计算开销，尤其请求数量极大时可能成为新瓶颈。
- **未讨论多节点网络延迟影响**：跨工作器后缀树同步可能引入额外延迟。

（完）
