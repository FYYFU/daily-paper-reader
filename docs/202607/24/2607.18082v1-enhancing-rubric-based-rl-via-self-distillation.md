---
title: Enhancing Rubric-based RL via Self-Distillation
title_zh: 通过自蒸馏增强基于评分标准的强化学习
authors: "Mingxuan Xia, Yuhang Yang, Chao Ye, Shuai Zhu, Shenzhi Yang, Guangcheng Zhu, Yuhang Zhang, Cheng Peng, Haobo Wang, Siqing Wang"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18082v1"
tags: ["query:opd"]
score: 9.0
evidence: 基于规则的RL中的自蒸馏
tldr: 基于评分规则的RL在开放任务中有效，但面临探索不足（未探索标准UC）和优化信号丢失（抑制标准SC）问题。现有方法引入外部指导导致训练-推理不匹配。本文提出CriPO，通过在线自蒸馏：针对UC用标准注入自教师做前向KL损失，针对SC在负优势样本中反转相关令牌优势。在医学和科学基准上，CriPO一致优于基线，优化步骤减少约一半，展现了显著性能提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18082v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1454, \"height\": 423, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18082v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 573, \"height\": 425, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18082v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1447, \"height\": 689, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18082v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 629, \"height\": 409, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18082v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1437, \"height\": 418, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18082v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1441, \"height\": 396, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18082v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1387, \"height\": 405, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18082v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1396, \"height\": 433, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18082v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 716, \"height\": 445, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18082v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1414, \"height\": 1260, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18082v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1451, \"height\": 742, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18082v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 761, \"height\": 406, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18082v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 702, \"height\": 400, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18082v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1451, \"height\": 742, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18082v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1378, \"height\": 703, \"label\": \"Table\"}]"
motivation: 现有方法解决未探索标准时引入训练-推理不匹配，且忽视了抑制标准这一更普遍的失败模式。
method: 针对未探索标准构建标准注入自教师计算前向KL损失；针对抑制标准在负优势样本中反转相关令牌优势。
result: 在医学和科学基准上CriPO一致优于现有方法，以约一半优化步骤达到更强性能。
conclusion: 自蒸馏能有效增强基于评分规则的RL，避免训练-推理不匹配并同时处理两种失败模式。
---

## 摘要
基于评分标准的强化学习最近在改进大语言模型解决开放式任务方面展现出潜力。一个广泛认可的局限性是探索不足：没有任何生成样本满足的标准（未探索标准，UC）得不到优化信号。最近的方法通过在生成样本期间引入评分标准信息作为外部指引来解决这一问题，但它们引入了训练-推理不匹配：策略在外部指引下生成的样本上被优化，而推理时却缺少这种指引，导致自回归解码中的误差累积。此外，这些注重探索的方法忽略了一个根本不同的失败模式，我们称之为被抑制标准（SC）——一些生成样本满足的标准，但由于标量奖励聚合分配了非正的整体优势，其学习信号在优化过程中丢失。我们的分析显示，SC 非常普遍：超过 57% 的样本在整个训练过程中表现出这种失败模式，每个样本平均有 1.8 个 SC。为了在不引入训练-推理不匹配的情况下同时解决 UC 和 SC，我们提出了基于标准蒸馏的策略优化（CriPO），通过在线策略自蒸馏来增强基于评分标准的强化学习。对于 UC，CriPO 构建了一个标准注入的自教师模型，并计算局部前向 KL 散度损失，将缺失的行为注入策略。对于 SC，CriPO 使用反事实自教师模型来定位负优势生成样本中与标准相关的 token，并将其 token 级优势翻转为正值，从而保留原本会被抑制的有用模式。在医学和科学基准上的实验表明，CriPO 始终优于基于评分标准的强化学习，在大约减少 2 倍优化步骤的情况下实现了更强的最终性能。

## Abstract
Rubric-based RL has recently shown promise in improving LLMs on open-ended tasks. A widely recognized limitation of rubric-based RL is limited exploration: criteria that no rollout manages to satisfy (Unexplored Criteria, UC) receive no optimization signal. Recent methods address this by incorporating rubric information as external guidance during rollout, yet they introduce a train-inference mismatch: the policy is optimized on rollouts produced under external guidance while this guidance is absent at inference time, causing error accumulation through autoregressive decoding. Moreover, these exploration-focused approaches overlook a fundamentally different failure mode that we term Suppressed Criteria (SC) -- criteria that are satisfied by some rollouts yet whose learning signals are lost during optimization because scalar reward aggregation assigns them non-positive aggregate advantages. Our analysis reveals that SC are remarkably prevalent: over 57% of samples exhibit this failure mode throughout training, with an average of 1.8 SC per sample. To simultaneously address both UC and SC without introducing training-inference mismatch, we propose Criterion-Distilled Policy Optimization (CriPO), which enhances rubric-based RL via on-policy self-distillation. For UC, CriPO constructs a criterion-injection self-teacher and computes a localized forward-KL loss to inject missing behaviors into the policy. For SC, CriPO employs a counterfactual self-teacher to locate criterion-relevant tokens in negative-advantage rollouts and flips their token-level advantages to positive values, preserving useful patterns that would otherwise be suppressed. Experiments on medicine and science benchmarks demonstrate that CriPO consistently outperforms rubric-based RL, achieving stronger final performance with approximately $2\times$ fewer optimization steps.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：基于评分标准（rubric）的强化学习（RL）已在开放性任务（如医疗咨询、长文写作）中显示出改进大语言模型（LLM）的潜力。它将响应质量分解为多个可解释的标准，通过LLM-as-a-Judge给出标准分，再聚合为标量奖励用于RL训练（如GRPO）。
- **核心问题**：论文识别了基于评分标准的RL中两种关键的失败模式：
  - **未探索标准（Unexplored Criteria, UC）**：当前采样组中没有任何rollout满足某个标准，导致该标准得不到优化信号。这是由于GRPO仅优化已出现的行为，探索受限。
  - **抑制标准（Suppressed Criteria, SC）**：某些rollout虽然满足某个标准，但由于标量奖励聚合，该rollout的整体优势为负或零，从而标准满足行为被抑制或忽略。论文统计发现，训练中超过57%的样本存在SC，平均每样本1.8个。
- **现有方法的不足**：近期方法通过将标准信息作为外部指导来生成rollout（如RuscaRL、HeRL）以缓解UC，但引入了训练-推理不匹配（exposure bias），且完全忽略了SC问题。
- **研究动机**：在不引入训练-推理不匹配的前提下，同时解决UC和SC，提升基于评分标准的RL的有效性和效率。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：保留GRPO作为稳定的奖励引导优化骨干，同时采用在线策略自蒸馏（On-Policy Self-Distillation, OPSD）提供密集的token级监督，专门针对UC和SC进行局部校正。OPSD利用当前策略本身在特权信息下构建自教师，避免分布偏移。
- **关键技术细节**：
  - **针对UC（行为注入）**：
    - 选择组内优势最高的rollout（最佳rollout），构建标准注入自教师：在prompt中加入未满足的UC，让教师模型生成修订版响应。
    - 计算学生分布（原始条件）与教师分布（额外条件）之间的逐token前向KL散度（forward-KL）。
    - 只选择累计KL贡献达到阈值γ（0.95）的top token（贡献导向过滤），仅在这些token上施加OPSD损失，避免噪声。
    - 损失：\( \mathcal{L}_{\text{OPSD}} = \frac{1}{|T^u|} \sum_{t \in T^u} D_{KL}\left( \text{sg}(\pi_\theta(\cdot|x, C^u, y^*_{<t})) \parallel \pi_\theta(\cdot|x, y^*_{<t}) \right) \)。
  - **针对SC（优势翻转）**：
    - 对于每个负优势rollout，找出其满足的SC集合。
    - 构建反事实自教师：提示教师修改或删除满足这些SC的部分，产生对比版本。
    - 通过比较学生和教师的log概率，定位与SC相关的token：满足条件 \(\Delta_{i,t} > 0\) 且教师对原始token的置信度低（\( p_{i,t}^T(y_{i,t}) < \alpha \cdot \max_v p_{i,t}^T(v) \)）。
    - 将这些相关token的原始优势替换为正值 \(\tau_{\text{flip}}\)，其他保持原优势。然后使用修正后的token级优势计算GRPO损失。
    - 损失形式与标准GRPO一致，但优势为逐token的调整值。
  - **总训练目标**：\( \mathcal{L}_{\text{CriPO}} = \mathcal{L}_{\text{GRPO}} + \beta \mathcal{L}_{\text{OPSD}} \)，其中β控制OPSD权重。
  - **算法流程**（Algorithm 1）：每步采样rollout → 计算奖励和优势 → 识别UC和SC → 对UC执行行为注入（OPSD损失）→ 对SC执行优势翻转（修改GRPO损失）→ 组合更新策略。

### 3. 实验设计：使用的数据集/场景、Benchmark、对比方法

- **数据集与场景**：
  - 训练集：RaR-Medicine（医学QA）、RaR-Science（科学QA），过滤掉易样本后分别得到15,658和10,874条训练样本。
  - 测试集（同领域）：各自对应的测试集（1,936/1,365条）。
  - 跨领域测试：HealthBench（医学）、LLMEval-Med（医学）、ResearchQA（科学），各抽取500或全部样本。
- **Benchmark**：使用GPT-4o-mini作为主要评估裁判，也使用Qwen3-32B作为裁判进行鲁棒性验证。评估指标为准确率（Accuracy@4或判断分数）。
- **对比方法**：
  - 基线模型：Qwen3-1.7B、Qwen3-4B（原始模型）。
  - 强化学习方法：GRPO（标准）、HeRL（基于后见反馈的探索增强）、OPSD（仅使用所有标准作为特权信息进行自蒸馏）。
  - 消融变体：CriPO-U（仅处理UC）、CriPO-S（仅处理SC）、完整CriPO。
- **实验设置**：所有方法在相同数据上训练200步，使用相同的超参数（附录表5提供细节，包括rollout_n=8, lr=1e-6等）。

### 4. 资源与算力

- 训练硬件：使用8× A800-40G GPUs，训练基于verl框架。
- 裁判模型：Qwen3-32B作为训练时的裁判，部署在64×910B2 NPUs。
- 训练时长：未明确给出每步绝对时间，但图5右侧展示了不同模型/数据集上GRPO和CriPO的wall-clock时间对比，例如在RaR-Medicine上，CriPO在约6-7小时内达到超过GRPO的表现，GRPO完整200步约需11-14小时。论文称CriPO的额外每步开销较小，整体效率更高。

### 5. 实验数量与充分性

- **实验数量**：
  - 主实验：2种模型（Qwen3-1.7B、Qwen3-4B）× 5个数据集（RaR-Medicine、HealthBench、LLMEval-Med、RaR-Science、ResearchQA），报告平均分。
  - 两种裁判（GPT-4o-mini和Qwen3-32B）下的结果均在正文或附录中呈现。
  - 跨领域指令跟随评估（IFEval、IFBench、MulDimIF，附录表3）。
  - 消融实验：对行为注入（移除过滤、移除最佳选择）和优势翻转（随机定位）进行对比。
  - 训练动态分析：奖励、熵、响应长度随步数变化。
  - 标准统计：UC和SC在训练过程中的比例变化。
  - 案例分析：具体的token选择示例和推理案例。
- **充分性与公平性**：
  - 对比基线包括标准GRPO、HeRL和OPSD，覆盖了当前最相关的方法。
  - 超参数固定，训练步数一致。
  - 使用两种不同裁判（GPT-4o-mini和Qwen3-32B）验证结果稳健性。
  - 消融实验系统，验证每个组件的必要性。
  - 实验设计比较充分，结果有统计意义，但所有实验仅在医学和科学QA领域进行，通用性需更多验证。

### 6. 论文的主要结论与发现

- **主要结论**：CriPO能够同时缓解UC和SC，在几乎所有评测设定下一致超过GRPO和HeRL，且训练效率更高（约2倍减少到GRPO的收敛步数）。
- **具体发现**：
  - 完整CriPO在Qwen3-1.7B上平均得分+3.2（超过GRPO），在Qwen3-4B上+1.4。
  - CriPO-U和CriPO-S分别针对UC和SC有效，组合后更优。
  - 训练动态显示CriPO维持更高熵和更长响应，促进探索。
  - 标准统计显示CriPO降低了UC和SC的出现比例。
  - 局部token选择机制有效，反事实教师能准确识别标准相关位置。
- **局限性**：OPSD单独使用不稳定（导致性能下降），结合GRPO是必要的。

### 7. 优点：方法或实验设计上的亮点

- **方法创新**：
  - 首次系统定义并同时处理UC和SC两种失败模式。
  - 利用自蒸馏在不引入训练-推理不匹配的情况下注入缺失行为。
  - 通过贡献导向的token过滤和反事实教师定位实现局部、精准的校正，避免噪声。
  - 保留GRPO骨干，稳定性好，仅增加少量计算负担。
- **实验设计亮点**：
  - 深入分析SC的普遍性（平均1.8/样本，>57%样本）。
  - 可视化token级别的KL分布，证明局部性原理。
  - 消融实验拆解每个设计组件的贡献。
  - 跨领域评估（医学→科学，以及通用指令跟随）验证泛化性。
  - 使用不同裁判模型验证鲁棒性。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

- **实验覆盖**：仅在医学和科学QA任务上验证，未涉及其他开放任务（如写作、对话、代码生成）。通用指令跟随性能略有波动，但基本保持。
- **偏差风险**：依赖LLM-as-Judge（Qwen3-32B和GPT-4o-mini）的质量，裁判偏差可能影响标准定义和奖励。论文未充分讨论裁判偏差的影响。
- **应用限制**：
  - 需要预定义的评分标准（rubric），构建高质量rubric成本高。
  - 超参数较多（γ, α, τ_flip, β等），调优可能较敏感。
  - 行为注入需要构建教师prompt，可能产生过长的输入。
  - 仅在较小模型（1.7B和4B）上测试，大规模模型（如7B/14B/72B）上的效果未知。
  - 方法依赖组采样（G=8），对于超大模型采样成本可能更高。
- **其他**：未与RLHF方法对比，也未探索在更复杂多步任务上的表现。论文未公开代码（预印本阶段），可复现性待验证。

（完）
