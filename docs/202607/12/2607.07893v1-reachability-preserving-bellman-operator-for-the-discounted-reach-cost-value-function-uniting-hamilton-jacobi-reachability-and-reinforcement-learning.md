---
title: "Reachability-Preserving Bellman Operator for the Discounted Reach-Cost Value Function: Uniting Hamilton-Jacobi Reachability and Reinforcement Learning"
title_zh: 折扣到达代价价值函数的保持可达性的Bellman算子：统一Hamilton-Jacobi可达性与强化学习
authors: "Isabelle El-Hajj, Prashant Solanki, Jasper van Beers, Coen de Visser, Erik-Jan van Kampen"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07893v1"
tags: ["query:ai-safety"]
score: 9.0
evidence: 统一了哈密顿-雅可比可达性（安全）与强化学习，提供安全保证
tldr: Hamilton-Jacobi可达性理论提供严格的安全保证，但受维数灾难制约；深度强化学习可扩展但目标函数非累加，难以直接融合。本文提出一种保持语义的折扣Bellman算子，通过非累加价值函数建立与HJ公式的等价性，并证明该算子是压缩映射，保证值迭代收敛于唯一不动点。该算子使强化学习成为求解同一不动点方程的样本近似方法，从而在保持安全关键含义的前提下实现数据驱动的可达性近似。数值实验表明，零水平集与HJ解高度一致，验证了语义保持性，为高维系统安全证书的计算开辟了新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07893v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 722, \"height\": 581, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07893v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 734, \"height\": 1198, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07893v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 725, \"height\": 590, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07893v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 716, \"height\": 571, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07893v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 587, \"height\": 577, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07893v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 886, \"height\": 831, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07893v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 936, \"height\": 969, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07893v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 824, \"height\": 539, \"label\": \"Table\"}]"
motivation: 消除HJ可达性与强化学习之间的语义鸿沟，使RL能够原值保持安全目标地近似可达集，避免传统折扣破坏安全性。
method: 提出一种保持原语义的折扣可达代价价值函数，并推导出非累加Bellman算子，通过压缩映射证明其不动点与HJ解等价。
result: 新算子的唯一不动点精确匹配HJ价值函数，RL的时序差分更新等价于其样本近似，数值实验验证零水平集对齐。
conclusion: 建立了HJ可达性与RL之间严格且保持语义的桥梁，为高维系统数据驱动安全分析提供理论基础。
---

## 摘要
Hamilton-Jacobi (HJ) 可达性为连续时间动力系统提供严格的安全和可达性保证，但其数值求解存在维度诅咒。相比之下，深度强化学习 (DRL) 提供可扩展的基于样本的方法。然而，RL通常围绕加性累积奖励构建，而可达性目标本质上是非加性的。这种不匹配使得HJ可达性与RL之间的直接桥梁变得不平凡。最近的折扣公式要么通过改变原始可达性语义引入压缩，要么在HJ侧保留精确语义而没有相应的Bellman不动点刻画。本文通过构建在保持语义的折扣基于可达的价值函数，并推导出一个非加性Bellman算子，其唯一不动点与HJ公式中的价值函数精确匹配，从而弥合了这一差距。我们证明了折扣使该算子具有压缩性，从而得到价值迭代的存在性、唯一性和收敛性。此外，我们建立了HJ和Bellman刻画之间的等价性，并表明RL可以解释为同一不动点方程的基于样本的近似方案。这产生了HJ可达性与RL之间原则性的、语义精确的连接，使得基于学习的方法能够近似可达性价值函数，同时保留其安全关键含义。因此，所提出的框架为高维系统中可达集和安全证书的可扩展、数据驱动计算打开了大门。数值实验表明与HJ解高度一致，通过零水平集的对齐确认了可达性语义的保留，并支持将强化学习解释为所提出的Bellman算子的基于样本求解器。

## Abstract
Hamilton-Jacobi (HJ) reachability provides rigorous safety and reachability guarantees for continuous-time dynamical systems, but its numerical solution suffers from the curse of dimensionality. Deep reinforcement learning (DRL), by contrast, offers scalable sample-based methods. However, RL is typically built around additive cumulative rewards; whereas, reachability objectives are inherently non-additive. This mismatch makes a direct bridge between HJ reachability and RL nontrivial. Recent discounted formulations have either introduced contraction by altering the original reachability semantics, or preserved exact semantics on the HJ side without a corresponding Bellman fixed-point characterization. In this paper, we close this gap by building on a semantics-preserving discounted reach-based value function and deriving a non-additive Bellman operator whose unique fixed point exactly matches the value function in the HJ formulation. We prove that discounting makes this operator contractive, yielding existence, uniqueness, and convergence of value iteration. Furthermore, we establish the equivalence between the HJ and Bellman characterizations, and show that RL can be interpreted as a sample-based approximation scheme for the same fixed-point equation. This yields a principled and semantically exact connection between HJ reachability and RL, enabling learning-based methods to approximate reachability value functions while preserving their safety-critical meaning. As a result, the proposed framework opens the door to scalable, data-driven computation of reachable sets and safety certificates in high-dimensional systems. Numerical experiments demonstrate close agreement with HJ solutions, confirm preservation of reachability semantics via alignment of zero level sets, and support the interpretation of reinforcement learning as a sample-based solver of the proposed Bellman operator.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：Hamilton-Jacobi（HJ）可达性理论能为连续时间动力系统提供严谨的安全与可达性保证，但其数值求解受“维度诅咒”（curse of dimensionality）制约；而深度强化学习（DRL）虽具有可扩展的样本高效学习能力，但其目标函数通常是加性累积奖励，与可达性目标（非加性、依赖是否达到目标集）之间存在本质语义鸿沟。如何建立两者之间既保持可达性语义又兼容动态规划与学习的桥梁，是本文要解决的核心问题。
- **研究动机**：已有工作要么在RL侧引入折扣但不保留原始可达性语义（如最小折扣奖励MDR），要么在HJ侧保持语义但缺少对应的Bellman不动点刻画。本文旨在填补这一空白，推导出一个既保持语义又具备压缩性的Bellman算子，使得RL可被解释为同一不动点方程的样本近似。
- **整体含义**：该工作建立了HJ可达性与RL之间“原则性且语义精确”的连接，为将强化学习用于高维系统安全关键控制提供了理论基础，开启了数据驱动计算可达集和安全证书的新方向。

## 2. 方法论

### 2.1 核心思想
- 采用Choi等人提出的“保持语义的折扣可达代价价值函数”作为起点，定义：
  \[
  V(t,x) = \inf_{u(\cdot)} \inf_{s\in[t,T]} g(\xi_{t,x}^u(s)) e^{-\lambda (s-t)}
  \]
  其中 \(g(x)<0\) 表示在目标集内，折扣因子 \(e^{-\lambda(s-t)}\) 不改变符号，因此可达性由 \(V(t,x)\le 0\) 决定，语义保持不变。
- 将该价值函数转化为时间到终点（time-to-go）形式 \(W(\tau,x)\)，并推导其动态规划原理（DPP）。
- 基于DPP定义非加性Bellman算子 \(T_{\sigma,\lambda}\)（有限期）和 \(T_{\sigma,\lambda}^{\infty}\)（无限期），算子包含“停止分支”和“延续分支”，通过min操作实现非加性结构。
- 证明折扣（\(\lambda>0\)）使得该算子在无穷范数下是压缩映射（收缩因子 \(e^{-\lambda\sigma}<1\)），从而在完整函数空间上存在唯一不动点。
- 通过Banach不动点定理，证明价值迭代收敛到唯一不动点，且该不动点即为HJ价值函数本身。
- 进而证明HJ变换不等式的粘性解与Bellman不动点是等价的（Theorem 7 & 8）。
- 最终将RL算法（如拟合值迭代）解释为同一算子的样本近似：使用离散时间步长 \(\Delta t\) 近似轨迹、有限控制集近似连续控制，并通过最小化Bellman残差训练参数化模型。

### 2.2 关键技术细节
- **折扣率** \(\lambda>0\)，保证压缩；**Bellman步长** \(\sigma>0\)，控制每次更新的时间跨度。
- 算子的非加性结构：\(\min\{ \text{停止分支},\ \text{延续分支} \}\)，其中停止分支是\([0,\sigma]\)内的折扣最小代价，延续分支是折扣后未来价值 \(e^{-\lambda\sigma} W(\tau-\sigma, \phi_x^u(\sigma))\)。
- 有限期情形：需满足边界条件 \(W(0,x)=g(x)\)，且当剩余时间 \(\tau<\sigma\) 时只使用停止分支。
- 离散实现：将连续Bellman算子离散化为 \(\widehat{T}_{\sigma,\lambda}\)，通过数值积分（如Runge-Kutta）和有限控制集离散化，证明在 \(\Delta t\to0\) 时收敛到真算子。
- 学习算法：使用正弦表示网络（SIREN）作为函数近似器，通过拟合值迭代更新参数，本质上是在最小化Bellman残差。

## 3. 实验设计

- **场景一：双积分器（Double Integrator）可达问题**
  - 系统：\(\dot{x}_1=x_2,\ \dot{x}_2=u,\ u\in[-1,1]\)
  - 目标集：以 \((0,0)\) 为中心、半径0.5的圆盘，代价函数为缩放后的符号距离函数 \(\tanh((\|x\|^2 - r^2)/\alpha)\)
  - 对比基准：通过求解定常HJ变分不等式（半拉格朗日格式）得到的参考解（类似有限差分/半拉格朗日数值解）
  - 对比方法：本论文提出的FVI（算法2）学习的价值函数，以及由参考HJ解得到的价值函数
- **场景二：Dubins车规避问题（Avoid formulation）**
  - 系统：\((\dot{x}_1,\dot{x}_2,\dot{\theta}) = (\nu\cos\theta,\ \nu\sin\theta,\ u)\)，控制为偏航率 \(u\in[-\dot{\theta}_{max},\dot{\theta}_{max}]\)
  - 目标：避开以 \((x_{1,\text{ref}},x_{2,\text{ref}})\) 为中心的区域，是不安全集
  - 对比基准：使用helperOC工具箱（基于HJ）计算的无折扣无限期近似解（采用足够长有限期近似）
  - 对比方法：同样使用FVI学习得到的价值函数，重点比对零水平集
- **关键评估指标**：
  - 零水平集对齐程度（sign agreement → 语义保持）
  - 点对点数值误差（图1(c)和2(c)）
- **消融实验**：无，只展示了两个标准场景。

## 4. 资源与算力

- 论文**未明确说明**使用了多少GPU（型号、数量）或训练总时长。
- 仅提及使用SIREN网络（隐层64×64或128×128），训练步数200,000步（表II），学习率约1e-4或2.87e-4，优化器为Adam。
- 未给出收敛时间或计算资源消耗的量化数据。

## 5. 实验数量与充分性

- **实验组数**：仅包含两个场景（双积分器reach、Dubins车avoid），且每个场景只报告了单一对比结果（HJ参考 vs FVI学习）和一个零水平集叠加图。
- **充分性评估**：
  - 优点：两个场景分别覆盖了“可控导致无限可达集”和“规避导致有界安全集”两种典型情形，验证了语义保持（零水平集对齐）和数值近似程度。
  - 不足：
    - 未进行**消融研究**（例如改变折扣率\(\lambda\)、Bellman步长\(\sigma\)、网络架构、训练样本数等）。
    - 未与其他现有方法（如MDR、DeepReach等）进行定量对比。
    - 没有报告统计量（多次运行均值/方差、收敛曲线等）。
    - 缺乏更高维场景（如>3维）的验证，以证明方法的可扩展性。
    - 未在真实机器人或更复杂动力学上测试。
- **客观性**：实验目标明确（验证语义保持和HJ-Bellman等价），结果展示清晰，但对比不够系统和深入。

## 6. 主要结论与发现

1. **建立了等价的HJ-Bellman刻画**：所提非加性Bellman算子的唯一不动点精确等于HJ价值函数（Theorem 7 & 8）。
2. **折扣保证压缩性**：尽管算子非加性，折扣因子\(e^{-\lambda\sigma}\)使得该算子在无穷范数下是压缩映射，确保价值迭代收敛到唯一不动点。
3. **语义保持**：折扣不改变价值函数符号，零水平集与原始HJ可达集一致（图2(c)），通过Dubins车规避问题证实。
4. **RL视为样本近似**：拟合值迭代（FVI）可解释为对同一Bellman不动点方程的样本求解，数值实验显示学习解与HJ参考解高度一致，误差较小。
5. **为高维安全学习提供理论基础**：所建立的理论框架为将RL应用于安全关键控制时保持形式化保证提供了依据。

## 7. 优点

- **理论严谨**：完整推导了从价值函数定义到DPP、Bellman算子、压缩性证明、不动点存在唯一性、与HJ等价的完整链条，数学基础扎实。
- **语义保持**：是第一个在Bellman算子层面同时实现折扣压缩与原始可达性语义保持的工作，突破了MDR等方法只能近似语义的限制。
- **框架通用性**：算子结构可自然推广到避障（avoid）及存在扰动的情况；时间不变动力学假设可松绑。
- **连接机器学习与经典控制**：清晰地将RL解释为Bellman不动点的样本近似，避免了人为设计奖励函数的做法，为安全RL提供了理论锚点。
- **实验验证清晰**：通过零水平集对齐直观展示了语义保持，且误差分析证明了FVI数值精度。

## 8. 不足与局限

- **函数近似收敛性未证明**：论文承认（Remark 8）所提出的FVI（含有参数近似和有限样本）并不自动继承底层离散算子的压缩性质；缺乏对投影复合算子的收敛保证，这是核心理论空白。
- **实验覆盖有限**：仅两个低维（2D、3D）场景，无高维实例；未与其他方法（如DeepReach、MDR）定量比较；无消融研究，参数依赖不够清晰。
- **资源开销未报告**：无法判断方法的计算效率及可扩展性。
- **控制集离散化假设**：实际中连续控制需离散化，可能影响最优性，但未分析离散化带来的性能损失。
- **应用局限**：实验仅针对确定性动力学；随机动力学或部分观测情况未涉及；提出的算法需要已知动力学模型（文中明确为模型已知的“近似动态规划”），真正RL需环境交互；扩展至model-free仍需额外工作。
- **数值实验仅展示无限期情形**：有限期公式虽在理论中完整给出，但未在实验中验证（如不同\(\tau\)下的值函数形态）。
- **代码未开源**：论文未提及代码或数据公开，影响可复现性。

（完）
