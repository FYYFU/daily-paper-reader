---
title: "CompactionRL: Reinforcement Learning with Context Compaction for Long-Horizon Agents"
title_zh: "CompactionRL: 面向长程智能体的上下文压缩强化学习"
authors: "Yujiang Li, Zhenyu Hou, Yi Jing, Jie Tang, Yuxiao Dong"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05378v1"
tags: ["query:l-context-rl"]
score: 9.0
evidence: 基于上下文压缩的强化学习用于长时程智能体
tldr: "长程LLM智能体因有限上下文窗口而常中断任务。CompactionRL通过上下文压缩将强化学习与总结生成结合，采用token级损失归一化和跨轨迹GAE联合优化。在SWE-bench Verified上，GLM-4.5-Air Pass@1达66.8%（+7.0），GLM-4.7-Flash达56.0%（+5.5）。这是首个有效集成上下文压缩到RL训练的方法，显著提升长程任务性能并已用于训练GLM-5.2。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05378v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1387, \"height\": 543, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05378v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 710, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05378v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1416, \"height\": 724, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05378v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1421, \"height\": 414, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05378v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1280, \"height\": 225, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05378v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1459, \"height\": 900, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05378v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1441, \"height\": 757, \"label\": \"Table\"}]"
motivation: 长程LLM智能体因有限上下文窗口难以完成交互任务，现有方法未探索上下文压缩与强化学习的结合。
method: 联合优化任务执行与摘要生成，采用token级损失归一化和跨轨迹广义优势估计（GAE）。
result: "SWE-bench Verified上GLM-4.5-Air提升7.0点至66.8%，GLM-4.7-Flash提升5.5点至56.0%。"
conclusion: CompactionRL首个将上下文压缩有效融入RL训练，提升长程智能体任务性能，并已用于训练更大模型GLM-5.2。
---

## 摘要
长程智能体大语言模型（LLM）正日益受到有限上下文窗口的限制，因为扩展的交互轨迹可能在任务完成前就超过最大上下文长度。上下文压缩通过总结之前的交互状态并在压缩上下文下继续生成轨迹提供了一种自然的解决方案，但将压缩纳入强化学习仍未被充分探索。我们提出CompactionRL，一种训练具有上下文压缩的长程智能体LLM的强化学习策略。我们的方法通过令牌级损失归一化和跨轨迹广义优势估计，联合优化任务执行和摘要生成。该设计使LLM智能体能够从压缩的长程轨迹中学习。我们在开源模型上训练CompactionRL，并在智能体编程任务上观察到一致的性能提升。CompactionRL使开源的GLM-4.5-Air模型（106B-A30B）在SWE-bench Verified上达到66.8%的Pass@1得分，在Terminal-Bench 2.0上达到24.5%，分别绝对提升了7.0和3.1个百分点。基于GLM-4.7-Flash（30B-A3B），CompactionRL将Pass@1提升了5.5和6.8个百分点，分别在SWE-bench Verified上达到56.0%，在Terminal-Bench 2.0上达到20.2%。因此，CompactionRL被部署在训练开源GLM-5.2模型（750B-A40B）的强化学习流水线中。

## Abstract
Long-horizon agentic LLMs are increasingly limited by finite context windows, as extended interaction trajectories can exceed the maximum context length before a task is completed. Context compaction offers a natural solution by summarizing previous interaction states and continuing the rollout under a compressed context, but incorporating compaction into reinforcement learning remains underexplored. We propose CompactionRL, a reinforcement learning strategy to train long-horizon agentic LLMs with context compaction. Our approach jointly optimizes task execution and summary generation with token-level loss normalization and cross-trajectory generalized advantage estimation. This design enables the LLM agents to learn from compacted long-horizon trajectories. We train CompactionRL on top of open models and observe consistent performance gains on agentic coding tasks. CompactionRL enables the open GLM-4.5-Air model (106B-A30B) to achieve Pass@1 scores of 66.8% on SWE-bench Verified and 24.5% on Terminal-Bench 2.0, with absolute gains of 7.0 and 3.1 points, respectively. Built upon GLM-4.7-Flash (30B-A3B), CompactionRL improves Pass@1 by 5.5 and 6.8 points, reaching 56.0% on SWE-bench Verified and 20.2% on Terminal-Bench 2.0, respectively. CompactionRL is thus deployed in the RL pipeline for training the open GLM-5.2 model (750B-A40B).

---

## 论文详细总结（自动生成）

# CompactionRL: 面向长程智能体的上下文压缩强化学习

## 1. 论文的核心问题与整体含义

- **研究动机**：长程LLM智能体（如软件工程、终端问题求解）在执行多步交互任务时，不断累积的交互历史（工具输出、中间推理、错误信息等）会很快耗尽模型的有限上下文窗口，导致任务中断。虽然增加上下文长度是直接方案，但成本高昂且长序列利用率下降。上下文压缩（将早期历史总结为简短摘要）是一种自然解决方案，但以往仅作为推理时启发式或外部记忆操作，未被纳入强化学习（RL）训练中。
- **整体含义**：本文提出CompactionRL，将上下文压缩作为RL训练的一环，使智能体在固定上下文预算下通过训练学习同时生成优质摘要并正确执行任务，从而有效扩展可训练的有效时域，提升长程任务性能。这是首个将上下文压缩有效集成到RL训练中的工作，并已用于训练更大的开源模型GLM-5.2。

## 2. 论文提出的方法论

- **核心思想**：在RL rollout收集过程中，当剩余上下文预算低于阈值时，策略模型生成一个摘要（与执行操作共享同一策略），然后基于摘要加最近几步历史重建上下文继续交互。所有执行段和摘要段在同一最终任务奖励下进行联合优化。
- **关键技术细节**：
  - **上下文压缩机制**：定义历史`h_t`，当`C - |h_t| < T_comp`时触发压缩。生成摘要`S_t`，重建上下文`\bar{h}_t = (system_prompt) + u_resume(S_t) + (最近k步)`。默认k=2。
  - **训练段划分**：一次rollout被划分为多个生成段（执行段和摘要段），每个rollout共享一个最终任务奖励`R(τ)`。摘要段由可训练策略生成并参与RL目标，不引入额外的摘要质量奖励。
  - **优化目标**：采用PPO框架。使用**令牌级损失归一化**（token-level loss）代替段级或序列级损失，以消除因变量段数和段长带来的权重偏差。损失公式：`L_π = -1/|M| * Σ min( ρ_{s,i} Â_{s,i}, clip(ρ_{s,i}, 1-ε, 1+ε)Â_{s,i} )`，其中`ρ_{s,i}`为重要性采样比率，`Â_{s,i}`为跨轨迹优势估计。
  - **跨轨迹广义优势估计（Cross-Trajectory GAE）**：为解决压缩导致的前期段距离最终结果人为变近的问题，对局部GAE施加折扣修正：`Â_{s,i} = (γλ)^{N_{>s}} * A^{loc}_{s,i}`，其中`N_{>s}`为该段之后所有后续段的优化令牌数，使折扣因子匹配实际时序距离。
  - **训练设置**：初始化critic与策略相同checkpoint，进行50步值预训练。使用slime异步RL框架，PPO更新中每个batch做2次critic更新、1次policy更新。采用长度自适应GAE（λ=1-1/(αl), α=1.5）。每个assistant响应限制10,240 tokens，压缩触发阈值为10,240 tokens，最多压缩3次。

## 3. 实验设计

- **数据集与场景**：
  - **SWE-bench Verified**：200个随机子集实例（完整benchmark用于公共基线参考）。
  - **Terminal-Bench 2.0**：完整测试集。
  - 训练数据来自SWE-Dev开源数据集。
- **基准与对比方法**：
  - 内部对比：基础模型（GLM-4.7-Flash 30B-A3B、GLM-4.5-Air-SFT 106B-A30B）、标准PPO（无压缩）在相同和扩展上下文预算下的训练、CompactionRL（无摘要训练变体）、CompactionRL完整方法。
  - 公共基线（仅参考，因scaffold可能不同）：GPT-5 mini、Qwen3-Coder系列、gpt-oss-120b等。
- **评估设置**：Pass@1准确率。评估环境为Harbor + Terminus-KIRA scaffold。top-p=1.0, temperature=1.0, 最多250交互轮、最多3次压缩。报告2次评估均值。

## 4. 资源与算力

文中未明确说明使用的GPU型号、数量及训练时长。训练使用slime开源异步RL框架，全局batch size=128，group size=1，上下文预算为64k（GLM-4.7-Flash）和80k（GLM-4.5-Air-SFT）。学习率2e-6（policy）、3e-6（critic）。具体硬件配置（如GPU类型、节点数）缺失。

## 5. 实验数量与充分性

- **主实验结果**（表2）：两个模型 × 两个基准 × 三种设置（基础、无压缩RL、CompactionRL） × 两种评估模式（单窗口/压缩后4倍），共12个主要对比单元。
- **消融实验**：
  - 表3：在30B和106B尺度上，对比无压缩RL（64k/128k、80k/160k）、CompactionRL（无摘要训练）、CompactionRL完整。共8个配置 × 3个评估设置（单/压缩/长窗口）。
  - 表4：在106B压缩机设置下，消融令牌级损失和跨轨迹GAE。共4个配置（基础、完整、去掉token-level loss、去掉cross-trajectory GAE）。
- **分析实验**：图3（压缩次数、工具调用、压缩任务准确率），图4（摘要长度、推理token、熵的训练动态）。
- **充分性评价**：实验设计较为全面，覆盖了不同模型规模、不同上下文预算、不同压缩相关变体，消融实验验证了关键组件的必要性。但主要聚焦于编程类长程任务（SWE-bench、Terminal-Bench），缺乏在网页交互、机器人控制等其他领域的验证；单窗口评估下CompactionRL提升不明显甚至略降，论文也指出训练-测试不匹配问题。总体而言，实验客观且内公平（使用相同scaffold、温度等），但领域覆盖有限。

## 6. 论文的主要结论与发现

1. CompactionRL在固定峰值上下文预算下，在SWE-bench Verified和Terminal-Bench 2.0上均取得最佳压缩机推理性能，显著超过基础模型和无压缩RL。
2. 将摘要响应包含在RL目标中（联合训练摘要）能持续提升压缩推理性能，且产生更详细、更长的摘要。
3. 令牌级损失归一化和跨轨迹GAE均对性能有正贡献，移除任一模块均导致下降，其中令牌级损失更为关键。
4. CompactionRL训练的模型在压缩后任务上准确率最高，且压缩次数和工具调用次数介于无压缩RL和基础模型之间，表明其更高效地利用了扩展上下文。
5. 训练动态显示，经过CompactionRL后，推理令牌数增加、策略熵增长更受控，说明学习到了更好的推理行为。
6. CompactionRL已成功部署到GLM-5.2（750B-A40B）的训练流水线中，表明其可扩展性。

## 7. 优点

- **创新性**：首个将上下文压缩作为可训练部分纳入RL训练的工作，而非仅作为推理时启发式，填补了该方向空白。
- **方法设计严谨**：针对压缩带来的段数可变、信用分配扭曲等问题，提出令牌级损失归一化和跨轨迹GAE，有效解决了优化困难。
- **实验充分且公开**：基于开源模型和开源框架（slime、SWE-Dev），结果可复现；消融实验覆盖关键组件。
- **实际价值高**：在具有挑战性的长程编程任务上获得一致且显著的提升，且已用于训练更大的模型，表明方法可扩展。
- **分析深入**：除了最终性能，还对压缩行为、训练动态、摘要质量进行了细致分析，揭示了方法工作原理。

## 8. 不足与局限

- **训练-测试不匹配**：CompactionRL在压缩禁用（单窗口）时性能提升不稳定甚至下降，限制其在无压缩机测试场景的直接应用。
- **近似信用分配**：跨轨迹GAE仍是对全轨迹GAE的近似，可能无法完全捕捉早期摘要跨越多个压缩边界的长程影响。
- **领域覆盖有限**：实验仅聚焦于代码与终端交互任务，在更广泛的智能体场景（如网页交互、机器人控制、多轮对话）效果未知。
- **算力与资源细节缺失**：未报告具体GPU型号、数量、训练时间，不利于成本评估和复现。
- **摘要生成未使用额外监督**：未设计摘要质量奖励，完全依赖最终任务奖励进行间接优化，可能不足以产生最优摘要。
- **潜在偏差**：训练数据来自SWE-Dev，可能对特定类型任务（如GitHub issue解决）有偏好，泛化性有待验证。

（完）
