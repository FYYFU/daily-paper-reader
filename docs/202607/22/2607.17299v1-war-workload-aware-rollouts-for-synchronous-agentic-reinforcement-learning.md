---
title: "WAR: Workload-Aware Rollouts for Synchronous Agentic Reinforcement Learning"
title_zh: "WAR: 面向同步智能体强化学习的工作负载感知的轨迹生成"
authors: "Ryan Xu, Atlas Zhao, David Bao, Frank Du"
date: 2026-07-19
pdf: "https://arxiv.org/pdf/2607.17299v1"
tags: ["query:l-context-rl"]
score: 9.0
evidence: 同步智能体强化学习的工作负载感知rollout生成
tldr: 同步智能体强化学习中，长轨迹生成成为系统瓶颈。WAR系统根据运行时负载动态优化：低负载时采用SuffixDecoding无模型投机解码，复用已完成轨迹后缀；高负载时采用缓存感知调度，减少KV-cache重计算。吞吐量在低负载下提升1.4倍，高负载下提升1.6倍，有效消除rollout瓶颈，支持可扩展长上下文智能体训练。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17299v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 752, \"height\": 757, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 879, \"height\": 828, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 795, \"height\": 604, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 872, \"height\": 794, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 872, \"height\": 813, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 870, \"height\": 824, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 867, \"height\": 572, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 847, \"height\": 1294, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 864, \"height\": 582, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 865, \"height\": 571, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17299v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 883, \"height\": 224, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17299v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 656, \"height\": 265, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17299v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 399, \"height\": 244, \"label\": \"Table\"}]"
motivation: 长轨迹生成是同步智能体RL的主要系统瓶颈，现有方法在负载变化时效率不足。
method: 低负载时使用SuffixDecoding复用后缀模式；高负载时使用缓存感知调度优化请求放置。
result: 长上下文智能体rollout吞吐量提升1.4倍（低负载）至1.6倍（高负载）。
conclusion: WAR消除了同步agentic RL的rollout瓶颈，为可扩展长上下文训练提供实用路径。
---

## 摘要
长程轨迹生成已成为智能体强化学习（RL）中的主要系统瓶颈。由于智能体与环境在多轮交互中交互，轨迹迅速增长至数万token，使得同步RL训练愈发受限于轨迹生成。我们提出WAR，一种工作负载感知的轨迹生成系统，通过联合优化解码与调度显著加速同步智能体RL。WAR基于一个关键观察：最优的轨迹生成优化策略取决于运行时负载：（1）在低负载下，WAR通过SuffixDecoding实现无模型推测解码，该策略重用之前完成的轨迹中的后缀模式作为未来轨迹生成的推测草稿。与基于模型的推测器不同，SuffixDecoding不引入额外的推测模型，避免了与轨迹生成任务的GPU争用。（2）在高负载下，由于批量解码饱和，推测加速空间有限，WAR将优化重点转向缓存感知调度。全局调度器根据缓存局部性、轨迹进度和服务器负载将请求分配到不同轨迹生成副本上，从而减少冗余的KV缓存重计算，并缓解负载不均。通过结合解码级别的后缀复用与系统级别的轨迹生成调度，WAR在不同工作负载下实现了稳健的吞吐量提升，且无需更改底层RL算法。WAR在低负载下将长上下文智能体轨迹生成吞吐量提升1.4倍，在高负载下提升高达1.6倍。这些结果表明，WAR消除了同步智能体RL中的主要轨迹生成瓶颈，并为可扩展的长上下文智能体训练提供了一条实用路径。

## Abstract
Long-horizon rollout generation has become the dominant systems bottleneck in agentic reinforcement learning (RL). As agents interact with environments over many turns, trajectories rapidly grow to tens of thousands of tokens, making synchronous RL training increasingly constrained by rollout. We propose WAR, a workload-aware rollout system that substantially accelerates synchronous agentic RL by jointly optimizing decoding and scheduling. WAR is built on a key observation: the optimal rollout optimization strategy depends on runtime load: (1) Under low load, WAR enables model-free speculative decoding with SuffixDecoding, which reuses suffix patterns from previously completed trajectories as speculative drafts for future rollouts. Unlike model-based drafters, SuffixDecoding introduces no additional draft model and avoids GPU contention with rollout generation. (2) Under high load, where saturated batched decoding leaves limited room for speculative speedup, WAR shifts the optimization focus to cache-aware scheduling. A global scheduler places requests across rollout replicas based on cache locality, trajectory progress and server load, reducing redundant KV-cache recomputation and mitigating load imbalance. By combining decoding-level suffix reuse with system-level rollout scheduling, WAR delivers robust throughput improvements across workload regimes without changing the underlying RL algorithm. WAR improves long-context agentic rollout throughput by 1.4x under low load and up to 1.6x under high load. These results show that WAR removes a major rollout bottleneck in synchronous agentic RL and provides a practical path toward scalable long-context agent training.

---

## 论文详细总结（自动生成）

# WAR: Workload-Aware Rollouts for Synchronous Agentic Reinforcement Learning 论文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：在同步智能体强化学习（RL）训练中，长上下文轨迹生成（rollout）已成为主要的系统瓶颈。智能体与环境多轮交互导致轨迹长度迅速增长至数万token，使得同步RL训练受限于rollout阶段。现有同步RL系统（如veRL）使用简单的请求调度策略（如最少请求+粘性会话），在低负载下尚可，但在高负载下因KV缓存压力、冗余重计算和负载不均导致效率急剧下降。
- **整体含义**：论文首次提出“工作负载”是rollout优化中的一阶因子，即不同负载下最优优化策略不同。通过设计工作负载感知的自适应系统WAR，可显著提升同步rollout吞吐量，为可扩展的长上下文智能体训练提供实用路径。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：根据运行时负载动态切换优化策略：
  - **低负载**（GPU利用率不足）→ 启用无模型推测解码（SuffixDecoding），利用历史轨迹中的后缀模式作为草稿，提升单次前向传播的并行度。
  - **高负载**（批量解码已饱和）→ 禁用推测解码，转向缓存感知调度，减少KV缓存重计算和负载不均。
- **关键技术细节**：
  - **SuffixDecoding**：基于后缀树的无模型推测解码，存储已生成轨迹的token序列模式，当前解码时匹配并生成多个草稿token，由目标模型并行验证。优势：无额外推测模型、无GPU争用、内存开销小、适用于重复代码模式（如SWE任务）。
  - **缓存感知调度**：全局调度器按优先级分数将请求分配到rollout副本，分数综合考虑四个因素：
    - 缓存命中率 \(H\)（越高越好）
    - 估计助手回合数 \(\hat{T}_g\)（越短越好，通过指数平滑更新）
    - 副本上正在执行的请求数 \(N_{\text{inflight}}\)（越少越好，负载均衡）
    - 请求在队列中的等待时间 \(t_{\text{waiting}}\)（优先级最高，避免sandbox超时）
    - 使用线性加权组合，权重通过实验确定量级关系。
  - 额外引入基于AIMD的并发控制机制（类似TCP拥塞控制），动态调整每个副本的最大请求数，以维持高缓存命中率。
- **公式与算法流程**（文字说明）：
  - 优先级分数 \(S = S_{\text{waiting}} + S_{\text{turn}} + S_{\text{cache}} + S_{\text{inflight}}\)，各项正负号及量级由实验确定。
  - 请求数调整：每处理δ个请求后更新最大请求数 \(W_t\)，基于平均缓存命中率是否低于或高于阈值进行AIMD调整，并限定边界。
  - 估计助手回合数 \(\hat{T}_g^{\text{new}} = \alpha T_g^{\text{obs}} + (1-\alpha)\hat{T}_g^{\text{old}}\)。

## 3. 实验设计
- **数据集/场景**：SWE任务数据集，包含500个提示（prompt），每个提示进行8次rollout。任务类型为软件工程（SWE），智能体需与定制沙箱环境多轮交互（最多120回合）。
- **基准（Baseline）**：veRL v0.8.0（默认同步RL框架），使用最少请求+粘性会话调度。
- **对比方法**：
  - **veRL base**：无WAR优化。
  - **sched only**：仅启用缓存感知调度。
  - **spec only**：仅启用SuffixDecoding。
  - **sched spec**：两者都启用（完整WAR）。
- **评估指标**：端到端rollout时间、预填（prefill）吞吐量、解码（decode）吞吐量、KV缓存命中率、接受长度（accepted length）等。
- **控制变量**：使用YaRN扩展Qwen3-32B支持96K上下文长度；保证不同配置间预填token数、解码token数、助手回合数差异在5%以内，以公平比较系统优化效果而非工作负载本身差异。

## 4. 资源与算力
- **GPU**：16块NVIDIA H100 GPU（2个节点，每节点8块），通过InfiniBand互联。
- **模型**：Qwen3-32B，训练侧并行配置为TP=4、PP=2、CP=2，rollout侧TP=4。
- **训练步数**：每个实验运行5个训练步（steps）。
- **训练批大小**：从4到64变化（4/8/16/32/64）。
- **说明**：论文未明确给出总训练时长或GPU小时数，但提供了端到端rollout时间相对减少的百分比。

## 5. 实验数量与充分性
- **实验组数**：
  - 主实验：4种配置 × 5个批大小 = 20组实验。
  - 消融实验：对调度因素（等待因子、三个分量）和SuffixDecoding接受长度来源进行了单独消融。
  - 此外，额外测量了固定输入/输出长度下SuffixDecoding的吞吐量影响（图10）。
- **充分性与公平性**：
  - 实验覆盖了从低负载到高负载的完整范围（批大小从4到64），展示了两种优化策略的互补效应，并统计了误差棒（5步均值±标准差）。
  - 保证了不同配置间生成的工作负载高度相似（差异<5%），避免了因生成轨迹不同导致的偏差。
  - 消融实验验证了每个调度分量的贡献，并揭示了SuffixDecoding在批大小超过16时反而降低性能，体现了工作负载感知的必要性。
  - 总体而言，实验设计较为充分、客观、公平。

## 6. 主要结论与发现
- **WAR显著提升rollout吞吐量**：完整配置（sched spec）在低负载（批大小4）下预填吞吐量提升35%~53%，解码吞吐量提升32%~48%；在高负载（批大小64）下预填吞吐量提升63%，解码吞吐量提升63%。端到端rollout时间减少相应幅度。
- **组件互补性**：SuffixDecoding在低负载下有效（批大小≤16），高负载下反而降低吞吐量；缓存感知调度在高负载下效果显著，低负载下影响较小。两者结合在全负载范围稳健。
- **KV缓存命中率**：缓存感知调度使高负载下缓存命中率从0.1提升至0.5。
- **接受长度**：SuffixDecoding接受长度可达2.8~6.0，但高负载下验证开销抵消了加速效果。

## 7. 优点
- **创新性**：首次将工作负载作为一阶因子引入rollout优化，揭示了推测解码与缓存调度在不同负载下的适用性差异。
- **实用性**：WAR不改变底层RL算法，兼容现有同步RL框架（基于veRL实现），引入的代码量约7K行Python，易于集成。
- **轻量高效**：无模型SuffixDecoding避免了额外推测模型的GPU资源竞争；缓存调度基于简单优先级公式，开销小。
- **实验严谨**：控制了工作负载一致性，进行了消融分析，展示了误差棒，验证了阈值设置合理性。

## 8. 不足与局限
- **实验覆盖范围有限**：仅在单一模型（Qwen3-32B）和单一数据集（SWE，500个提示）上评估，未验证在其他模型规模（如7B、70B）或其他任务（如数学推理、通用agent）上的泛化性。
- **潜在偏差**：SWE任务中代码重复模式可能有利于SuffixDecoding，其他任务可能后缀复用率较低，导致低负载下加速效果减弱。
- **未与最新异步方法比较**：虽然论文指出异步rollout存在策略过期等问题，但未在实验中直接对比WAR与异步方案（如AReaL、StreamRL）的性能和训练质量，使得实际优势评估不够全面。
- **阈值与权重经验确定**：切换阈值和调度权重依赖实验手动调整，未能给出自适应学习方法，可能在不同硬件或负载下需要重新调参。
- **未考虑端到端训练效率**：实验仅测量rollout阶段的吞吐量，未报告整体RL训练收敛速度或最终任务性能，尚不确定吞吐量提升能否直接转化为训练加速或模型质量提升。
- **未讨论跨步通信开销**：SuffixDecoding的跨工作器后缀树同步引入通信开销，论文未量化其对端到端训练的影响。

（完）
