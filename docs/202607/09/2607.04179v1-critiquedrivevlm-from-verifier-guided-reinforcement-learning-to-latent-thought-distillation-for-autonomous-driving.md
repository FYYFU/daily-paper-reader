---
title: "CritiqueDriveVLM: From Verifier-Guided Reinforcement Learning to Latent Thought Distillation for Autonomous Driving"
title_zh: CritiqueDriveVLM：从验证器引导的强化学习到潜在思维蒸馏的自动驾驶
authors: "Zhaohong Liu, Hao Ye, Xianlin Zhang, Mengshi Qi"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04179v1"
tags: ["query:opd"]
score: 9.0
evidence: 结合强化学习与蒸馏技术用于自动驾驶，核心为潜思蒸馏
tldr: "端到端视觉语言模型在自动驾驶中面临推理幻觉和保守偏差问题，传统链式思维方法虽能改善但引入高延迟。CritiqueDriveVLM提出三阶段框架：先通过多维验证器引导的多轮强化学习训练高准确率System-2教师，再通过潜在思维蒸馏将其推理能力压缩到System-1学生。教师模型在DriveLMM-01基准上将多项选择质量从55.54%提升至76.54%，学生模型平均仅生成28个token，推理延迟从3482毫秒降至416毫秒（降低88%）。该方法有效平衡了可靠性与效率，为低延迟自动驾驶部署提供了稳健路径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04179v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1235, \"height\": 687}, {\"url\": \"assets/figures/arxiv/2607.04179v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1260, \"height\": 650}, {\"url\": \"assets/figures/arxiv/2607.04179v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1202, \"height\": 739}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04179v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1281, \"height\": 1032}, {\"url\": \"assets/tables/arxiv/2607.04179v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1288, \"height\": 713}, {\"url\": \"assets/tables/arxiv/2607.04179v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1256, \"height\": 633}, {\"url\": \"assets/tables/arxiv/2607.04179v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1272, \"height\": 298}]"
motivation: 解决端到端VLM在自动驾驶中SFT的推理幻觉与高延迟矛盾，满足实时部署需求。
method: 提出三阶段框架：验证器引导多轮RL训练System-2教师，再通过潜在思维蒸馏得到快速System-1学生。
result: "教师模型MCQ达76.54%，学生模型平均生成28 token，推理延迟降低88%（从3482ms到416ms）。"
conclusion: 该方法解决了可靠性-效率权衡，显著提升精度并压缩推理成本，为低延迟自动驾驶提供稳健路径。
---

## 摘要
端到端视觉语言模型（VLM）在自动驾驶中展现出巨大潜力。然而，标准的监督微调（SFT）常遭受推理幻觉和保守偏见的困扰。虽然传统的工具增强框架和思维链（CoT）方法缓解了这些问题，但它们导致了过高的令牌消耗和不可接受的延迟，使得实时部署不切实际。为了解决这一可靠性-效率权衡问题，我们提出了CritiqueDriveVLM，一种新颖的统一三阶段框架，直接将推理内化到VLM中。首先，我们引入了由多维验证器引导的批判驱动多轮强化学习（RL）。通过提供细粒度的标量反馈和多轮惩罚，我们迫使策略内化逻辑推导，培养出一个稳健的系统2教师，无需脆弱的外部工具即可实现高精度。随后，我们提出了潜在思维蒸馏以克服延迟瓶颈。通过将学生的潜在表示与教师完全收敛的推理状态对齐，我们将深层逻辑能力压缩为一个快速、无CoT的系统1学生。在广泛使用的DriveLMM-01基准上的大量实验展现了显著改进。与基础模型相比，我们的无工具教师将多选题质量（MCQ）从55.54%提升至最先进的76.54%。关键的是，我们的蒸馏学生保持了具有竞争力的推理深度，同时极大缩短了生成长度，平均仅为28个令牌。这使得推理延迟降低了88%（从3482毫秒降至416毫秒），为低延迟自动驾驶铺平了一条高度鲁棒的路径。我们的源代码可在https://github.com/MICLAB-BUPT/CritiqueDriveVLM获取。

## Abstract
End-to-end Vision-Language Models (VLMs) show immense potential in autonomous driving. However, standard Supervised Fine-Tuning (SFT) often suffers from reasoning hallucinations and conservative biases. While traditional tool-augmented frameworks and Chain-of-Thought (CoT) approaches mitigate these issues, they incur exorbitant token consumption and unacceptable latency, rendering real-time deployment impractical. To resolve this reliability-efficiency trade-off, we propose CritiqueDriveVLM, a novel unified three-stage framework internalizing reasoning directly into the VLM. First, we introduce Critique-Driven Multi-Turn Reinforcement Learning (RL) guided by a multi-dimensional verifier. By providing granular scalar feedback and a multi-turn penalty, we force the policy to internalize logical deduction, cultivating a robust System-2 Teacher that achieves high accuracy without fragile external tools. Subsequently, we propose Latent Thought Distillation to overcome the latency bottleneck. By aligning the Student's latent representations with the Teacher's fully converged reasoning states, we compress deep logical capabilities into a fast, CoT-free System-1 Student. Extensive experiments on the widely-used DriveLMM-01 benchmark demonstrate remarkable improvements. Compared to the base model, our tool-free Teacher significantly boosts Multiple Choice Quality (MCQ) from 55.54% to a state-of-the-art 76.54%. Crucially, our distilled Student preserves competitive reasoning depth while drastically minimizing generation length to an average of merely 28 tokens. This slashes inference latency by 88% (from 3482 ms to 416 ms), paving a highly robust pathway for low-latency autonomous driving.Our source code is available at https://github.com/MICLAB-BUPT/CritiqueDriveVLM.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：端到端视觉语言模型（VLM）在自动驾驶中潜力巨大，但标准监督微调（SFT）常产生推理幻觉和保守偏见（如过度减速、忽略行人）。链式思维（CoT）和外部工具增强方法虽可缓解幻觉，但引入极高的令牌消耗和推理延迟（如AgentThink 5416毫秒），无法满足实时部署要求。
- **背景**：现有方法要么脆弱依赖外部API（如深度估计器），要么因显式CoT文本过长导致延迟不可接受。论文旨在解决**可靠性–效率权衡**，实现既能保持深度逻辑推理又不牺牲实时性的自动驾驶VLM。

## 2. 方法论：核心思想与关键技术细节
论文提出**CritiqueDriveVLM**三阶段框架，核心思路：先通过强化学习训练高精准度但慢速的System-2教师，再通过潜在空间蒸馏将其推理能力压缩为快速无CoT的System-1学生。

### 阶段1：热身SFT与多维验证器构建
- **Warm-up SFT**：对基础VLM（Qwen3-VL-8B）用DriveLMM-o1的CoT数据微调，强制输出严格格式：`<think>推理</think><answer>答案</answer>`，为后续RL奠定结构基础。
- **多维验证器训练**：使用更强的Qwen3-VL-235B为训练样本生成三个维度的离散评分（感知` sper`、逻辑` slog`、安全` ssaf`，取值{0,0.5,1.0}）及文本批评`c`。训练数据包括正样本（ground truth）和硬负样本（由基线GRPO模型生成）。最终验证器`V`接收图像、指令、响应，输出标量奖励和文本批评。

### 阶段2：批判驱动的多轮强化学习（Critique-Driven Multi-Turn RL）
- **多轮交互**：模型最多进行`K=2`轮交互。第1轮生成响应`y1`，验证器评估；若未满分或未达最大轮次，则将批评`c1`拼接到对话历史，模型生成修正响应`y2`。最终以终止轮`yT`计算奖励。
- **组合奖励**：
  - **格式奖励`Rfmt`**：确保输出符合`<think>`/`<answer>`标签。
  - **精度奖励`Racc`**：最终答案与真实标签是否一致。
  - **过程验证器奖励**：`α(sper + slog + ssaf)`，鼓励中间推理正确。
  - **基奖励**：`Rbase = Rfmt + Racc + α(sper + slog + ssaf)`。
  - **多轮惩罚`Pmt(T)`**：随轮次增加递减，迫使模型首轮即正确。最终奖励`Rfinal = Rbase - Pmt(T)`。
- **GRPO优化**：对每个问题采样`G=4`个终止响应，使用群组相对优势`Ai`及裁剪比值、KL散度惩罚进行策略更新。避免了PPO的Critic网络开销。

### 阶段3：潜在思维蒸馏（Latent Thought Distillation）
- **蒸馏数据集**：仅包含提示→答案对，无任何CoT文本，强制学生依赖内部隐表示。
- **对齐目标**：冻结教师，训练学生（初始化为原始VLM）。提取教师最后`</think>`令牌的隐藏状态`h_T^think`，和学生`<answer>`令牌的隐藏状态`h_S^answer`，用余弦相似度对齐：
  `Lalign = 1 - (h_S^answer·h_T^think)/(||h_S^answer|| ||h_T^think||)`
- **联合损失**：`Ltotal = LCE + λLalign`，其中`LCE`为标准自回归交叉熵，`λ=0.5`。

## 3. 实验设计
- **数据集**：唯一使用**DriveLMM-o1** benchmark（基于nuScenes），包含超过18,000个VQA训练对，每个样本含逐步推理。
- **基准与评估**：
  - 主要指标：Overall Reasoning得分、Multiple Choice Quality（MCQ）。
  - 细粒度指标：风险评估、交通规则遵循、场景感知、相关性、遗漏细节。
- **对比方法**：
  - 通用VLM：GPT-4o、Qwen2.5-VL-7B/72B、Qwen3-VL-8B、InternVL2.5-8B等。
  - 领域专用vlm：DriveLMM-o1、AgentThink（工具增强CoT）、OmniDrive-R1（交错多模态CoT）。
  - 消融：逐步加入精度奖励、验证器奖励、多轮惩罚。

## 4. 资源与算力
- **硬件**：单台服务器配备**8块NVIDIA A100 GPU**；推理延迟测试在单块A100上进行。
- **训练配置**：三个阶段均使用LoRA（低秩适应），学习率分别为1e-4（SFT）、2e-6（RL）、2e-5（蒸馏）。每个阶段训练**2个epoch**，全局批次大小128。
- **未明确说明**：具体训练时间（小时/天）未给出；教师模型推理时间约3482毫秒（含CoT生成）。

## 5. 实验数量与充分性
- **主要对比（表2）**：与18个SOTA模型对比，涵盖通用和领域专用VLM，凸显教师模型的领先地位。
- **效率与蒸馏实验（表3）**：对比并实现了“System-2有CoT”与“System-1无CoT”两类，验证蒸馏效果及延迟降低（88%）。
- **消融研究（表4）**：逐步添加奖励组件（SFT→GRPO仅Acc→+Ver→+MT），证明多轮批判机制带来质的飞跃（MCQ从67.46%升至76.54%）。
- **定性案例（图3）**：展示了教师通过验证器批评纠正幻觉、学生直接正确推理的示例。
- **评价**：实验设计完整，对比方法全面，消融清晰，结果一致且具有统计显著性。但仅在单一数据集上验证（DriveLMM-o1），缺乏多数据集或真实驾驶场景泛化实验。主要指标均为单轮多选题质量，未涉及连续规划或闭环评估。

## 6. 主要结论与发现
- **教师模型（Stage 2）达到SOTA**：MCQ从基线55.54%提升至**76.54%**，超越OmniDrive-R1（73.62%）和AgentThink（71.35%），且无需外部工具。
- **蒸馏学生（Stage 3）大幅降低延迟**：平均仅生成28个令牌，推理时间从3482毫秒降至**416毫秒**（降低88%），同时保持MCQ为68.59%（高于无蒸馏学生的61.73%）。
- **核心发现**：通过多维验证器引导的多轮RL可在不依赖脆弱外部API的情况下有效抑制推理幻觉；潜在思维蒸馏能够将“慢思考”的逻辑深度压缩到“快思考”的隐空间，实现精度与效率的最佳平衡。

## 7. 优点（方法/实验亮点）
- **创新性框架设计**：首次将验证器引导的多轮RL与隐空间蒸馏结合，同时解决推理可靠性和实时性问题。
- **无外部工具依赖**：教师模型完全端到端，避免工具级联失败，更具鲁棒性。
- **巧妙的多轮惩罚机制**：激励模型首轮正确，避免对批判的过度依赖。
- **潜在思维蒸馏策略**：不是机械模仿CoT文本，而是对齐深层语义表示，保留推理深度同时剪枝显式令牌。
- **实验公平且全面**：在统一基准下与多种SOTA方法对比，消融实验成系统分析每个组件贡献。

## 8. 不足与局限
- **数据集单一**：仅使用DriveLMM-o1（基于nuScenes），缺乏在Waymo、CARLA等更多样化场景上的验证，泛化性未知。
- **任务局限**：评价仅基于多项选择（MCQ）和场景理解指标，未涉及真正闭环的规划与控制任务（如轨迹生成、速度指令），与应用实际仍有距离。
- **延迟衡量条件**：推理时间在单卡A100上测试，但未讨论模型部署到车载边缘设备（如Orin、Jetson）的可行性。
- **训练资源未量化时间**：虽给出了GPU数量和epoch，但未报告实际训练时长，难以评估复现成本。
- **教师模型仍较慢**：3492毫秒的推理时间对于自动驾驶（通常要求<100毫秒）仍不可接受，需依赖蒸馏学生。
- **蒸馏精度损失**：学生模型MCQ较低（68.59% vs 教师76.54%），损失约8个百分点，某些高风险场景可能无法接受。
- **最大轮次固定**：K=2可能不足以处理高度歧义场景，若需更多轮则训练时惩罚设计和计算成本需调整。

（完）
