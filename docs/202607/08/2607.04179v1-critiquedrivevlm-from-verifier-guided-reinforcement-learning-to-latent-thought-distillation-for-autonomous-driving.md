---
title: "CritiqueDriveVLM: From Verifier-Guided Reinforcement Learning to Latent Thought Distillation for Autonomous Driving"
title_zh: CritiqueDriveVLM：从验证器引导的强化学习到潜在思维蒸馏的自动驾驶
authors: "Zhaohong Liu, Hao Ye, Xianlin Zhang, Mengshi Qi"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04179v1"
tags: ["query:opd"]
score: 9.0
evidence: 基于验证器的强化学习潜在思维蒸馏
tldr: "端到端视觉语言模型在自动驾驶中面临推理幻觉与高延迟问题。CritiqueDriveVLM通过多维验证器引导的多轮强化学习培养高精度System-2教师，再经潜在思维蒸馏将推理能力压缩至System-1学生。教师模型在DriveLMM-01基准上实现76.54%选择题准确率，学生模型仅需28个token，推理延迟从3482毫秒降至416毫秒。该方法首次在无外部工具下平衡了可靠性与实时性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04179v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1235, \"height\": 687}, {\"url\": \"assets/figures/arxiv/2607.04179v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1260, \"height\": 650}, {\"url\": \"assets/figures/arxiv/2607.04179v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1202, \"height\": 739}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04179v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1281, \"height\": 1032}, {\"url\": \"assets/tables/arxiv/2607.04179v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1288, \"height\": 713}, {\"url\": \"assets/tables/arxiv/2607.04179v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1256, \"height\": 633}, {\"url\": \"assets/tables/arxiv/2607.04179v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1272, \"height\": 298}]"
motivation: 标准监督微调导致推理幻觉和保守偏差，现有工具增强与思维链方法消耗过多token和延迟，无法满足自动驾驶的实时性要求。
method: 提出三阶段框架：先由多维验证器引导多轮强化学习训练System-2教师，再通过潜在表示对齐将教师推理状态蒸馏到System-1学生。
result: "教师模型在DriveLMM-01上MCQ从55.54%提升至76.54%；学生模型平均仅28词，推理延迟降低88%至416毫秒。"
conclusion: CritiqueDriveVLM通过内部化推理与蒸馏，在低延迟下保持高可靠性，为自动驾驶提供鲁棒的实时部署方案。
---

## 摘要
端到端视觉语言模型（VLM）在自动驾驶中展现出巨大潜力。然而，标准的监督微调（SFT）常受推理幻觉和保守偏差的困扰。尽管传统的工具增强框架和思维链（CoT）方法缓解了这些问题，但它们导致极高的令牌消耗和不可接受的延迟，使实时部署变得不切实际。为解决这种可靠性-效率的权衡，我们提出CritiqueDriveVLM，一种新颖的三阶段统一框架，将推理直接内化到VLM中。首先，我们引入了由多维验证器引导的批判驱动多轮强化学习（RL）。通过提供细粒度的标量反馈和多轮惩罚，我们迫使策略内化逻辑推理，培养出无需脆弱外部工具即可实现高准确率的鲁棒System-2教师。随后，我们提出潜在思维蒸馏以克服延迟瓶颈。通过将学生的潜在表示与教师完全收敛的推理状态对齐，我们将深层逻辑能力压缩为快速、无CoT的System-1学生。在广泛使用的DriveLMM-01基准上进行的大量实验展示了显著的改进。与基础模型相比，我们的无工具教师将多项选择质量（MCQ）从55.54%大幅提升至76.54%的最新水平。关键在于，我们的蒸馏学生保持有竞争力的推理深度，同时将生成长度大幅缩减至平均仅28个令牌。这使得推理延迟降低了88%（从3482毫秒降至416毫秒），为低延迟自动驾驶铺平了高度鲁棒的路径。我们的源代码可在https://github.com/MICLAB-BUPT/CritiqueDriveVLM获取。

## Abstract
End-to-end Vision-Language Models (VLMs) show immense potential in autonomous driving. However, standard Supervised Fine-Tuning (SFT) often suffers from reasoning hallucinations and conservative biases. While traditional tool-augmented frameworks and Chain-of-Thought (CoT) approaches mitigate these issues, they incur exorbitant token consumption and unacceptable latency, rendering real-time deployment impractical. To resolve this reliability-efficiency trade-off, we propose CritiqueDriveVLM, a novel unified three-stage framework internalizing reasoning directly into the VLM. First, we introduce Critique-Driven Multi-Turn Reinforcement Learning (RL) guided by a multi-dimensional verifier. By providing granular scalar feedback and a multi-turn penalty, we force the policy to internalize logical deduction, cultivating a robust System-2 Teacher that achieves high accuracy without fragile external tools. Subsequently, we propose Latent Thought Distillation to overcome the latency bottleneck. By aligning the Student's latent representations with the Teacher's fully converged reasoning states, we compress deep logical capabilities into a fast, CoT-free System-1 Student. Extensive experiments on the widely-used DriveLMM-01 benchmark demonstrate remarkable improvements. Compared to the base model, our tool-free Teacher significantly boosts Multiple Choice Quality (MCQ) from 55.54% to a state-of-the-art 76.54%. Crucially, our distilled Student preserves competitive reasoning depth while drastically minimizing generation length to an average of merely 28 tokens. This slashes inference latency by 88% (from 3482 ms to 416 ms), paving a highly robust pathway for low-latency autonomous driving.Our source code is available at https://github.com/MICLAB-BUPT/CritiqueDriveVLM.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：端到端视觉语言模型（VLM）在自动驾驶中因标准监督微调（SFT）产生**推理幻觉**和**保守偏差**，无法正确处理复杂交通场景。现有工具增强框架（如AgentThink、OmniDrive-R1）和思维链（CoT）方法虽能缓解上述问题，但引入**过度令牌消耗**和**不可接受延迟**，难以满足实时部署需求。
- **核心矛盾**：可靠性（深度逻辑推理）与效率（低延迟）之间的权衡。如何在不依赖脆弱外部工具和冗长CoT的前提下，实现安全、低延迟的自动驾驶决策。
- **目标**：提出一种新框架，将推理能力内化至VLM，同时实现高准确率和低延迟。

## 2. 方法论
### 核心思想
采用**三阶段统一框架**：
1. **Stage 1：Warm-up SFT & 多维验证器构建**  
   - 对基座VLM进行SFT，强制输出结构化格式：`<think>推理</think><answer>答案</answer>`。  
   - 训练一个独立的多维验证器V，对模型响应在**感知、逻辑、安全**三个维度输出标量分数（0/0.5/1.0）和文本批评。正样本来自标注，负样本来自GRPO模型推理出的错误输出，由Qwen3-VL-235B标注。

2. **Stage 2：Critique-Driven Multi-Turn RL（教师模型）**  
   - **多轮交互**：模型最多进行K轮（论文设K=2）与验证器的迭代。初始回复y1→验证器给出批评c1→模型基于历史生成修正回答y2。若完美或达最大轮数则终止。  
   - **复合奖励**：  
     - 基奖Rbase = 格式奖励Rfmt + 准确率奖励Racc + α(感知分sper + 逻辑分slog + 安全分ssaf)  
     - 多轮惩罚Pmt(T)：若需多轮则施加惩罚，鼓励首次正确。  
     - 最终奖励Rfinal = Rbase(yT) - Pmt(T)  
   - **优化**：采用GRPO算法（避免Critic网络），从旧策略采样G个终端响应，使用clip重要性采样比率和KL散度约束。

3. **Stage 3：Latent Thought Distillation（学生模型）**  
   - 初始化学生为基座VLM，蒸馏数据集仅含问题-答案对（无CoT文本）。  
   - 提取教师模型在最终`</think>`位置的隐藏状态h^think_T和学生模型在`<answer>`位置的隐藏状态h^answer_S。  
   - 通过余弦相似度对齐：L_align = 1 - cos(h^answer_S, h^think_T)  
   - 联合损失：L_total = L_CE + λ L_align（λ=0.5）。学生不生成CoT，直接预测答案。

### 关键公式（文字说明）
- 多维验证器输出：R_verif = {s_per, s_log, s_saf}，s∈{0,0.5,1.0}  
- 复合奖励：Rbase = Rfmt + Racc + α(s_per + s_log + s_saf)  
- GRPO目标：J(θ) = E[ 1/G Σ min(ρ_i A_i, clip(ρ_i,1-ε,1+ε)A_i) - β D_KL ]  
- 优势A_i = (R_final^i - mean(R_final)) / std(R_final)  
- 对齐损失：L_align = 1 - (h_S·h_T)/(||h_S|| ||h_T||)

## 3. 实验设计
- **数据集**：DriveLMM-01基准（基于nuScenes数据集），包含超过18,000个VQA对，每个样本提供逐步推理。训练集和测试集均来自该基准。
- **Benchmark**：官方评价协议，主要指标为**Overall Reasoning分数**和**Multiple Choice Quality (MCQ)**，以及细粒度指标（风险评估、规则遵守、场景意识、相关性、缺失细节）。
- **对比方法**：  
  - 通用模型：GPT-4o, Ovis1.5-Gemma2-9B, Mulberry-7B, LLaVA-CoT, LlamaV-O1, InternVL2.5-8B, Qwen2.5-VL-7B/72B, Qwen3-VL-8B  
  - 领域专用：DriveLMM-o1, AgentThink, OmniDrive-R1

## 4. 资源与算力
- **硬件**：所有实验在配备**8块NVIDIA A100 GPU**的单台服务器上进行；推理时间在**单个A100 GPU**上测试。  
- **训练配置**：Stage 1 LoRA学习率1e-4；Stage 2 GRPO学习率2e-6，G=4 rollouts，K=2轮；Stage 3 LoRA学习率2e-5，λ=0.5。**均训练2个epoch，全局batch size 128**。  
- **训练时长**：文中**未明确说明**具体训练时间。

## 5. 实验数量与充分性
- **主要对比**（Table 2）：与12个模型（含多种规模）比较，教师模型MCQ达76.54%最优。  
- **效率与蒸馏分析**（Table 3）：对比System-2（含CoT）和System-1（无CoT）两类模型，学生模型仅28.83 tokens，延迟416ms（降低88%）。  
- **消融实验**（Table 4）：逐步验证Accuracy Reward、Verifier Reward、Multi-Turn的作用，MCQ从62.86%→64.49%→67.46%→76.54%，逻辑清晰。  
- **定性分析**（Figure 3）：一个行人横穿场景案例，展示教师多轮修正和学生快速预测。  
- **充分性**：实验覆盖对比、效率、消融、定性，较全面；对比方法包括通用与专用模型；消融设计合理，逐步验证贡献。**公平性**：所有实验使用同一基准、同一基座（Qwen3-VL-8B），验证器独立于策略，蒸馏数据集统一。但仅使用单一数据集，缺乏更多场景验证。

## 6. 主要结论与发现
- 教师模型（Stage 2）在无外部工具条件下达到**MCQ 76.54%**，超越OmniDrive-R1（73.62%）和AgentThink（71.35%），证明多轮RL+多维验证器能有效抑制幻觉和保守偏差。  
- 学生模型（Stage 3）通过潜在表示对齐，在**仅28.83个令牌、416ms延迟**下达到MCQ 68.59%，相比教师推理延迟降低88%，同时保留竞争性推理能力，实现高效实时推理。  
- 多轮交互和惩罚机制显著提升性能（+9个百分点），验证了内部化逻辑推理的有效性。  
- 定性案例显示教师能通过验证器批评纠正初始错误，学生则直接输出正确动作。

## 7. 优点
- **创新性**：首次将**多轮验证器引导的强化学习**与**潜在空间思维蒸馏**结合，实现CoT-free高精度推理，无需外部工具。  
- **实用性**：推理延迟从秒级降至毫秒级（416ms），接近实时要求；学生无需显式CoT，适合车载部署。  
- **实验严谨**：消融分解清晰，对比基线丰富，验证器训练过程含人工检验，保证可靠性。  
- **理论基础**：基于System-2/System-1认知框架，蒸馏操作在隐藏状态层面而非文本层面，更符合逻辑内化需求。  
- **代码开源**：提供GitHub仓库，结果可复现。

## 8. 不足与局限
- **数据集单一**：仅使用DriveLMM-01（基于nuScenes），未在更多自动驾驶基准（如Waymo、CARLA）或真实路测中验证，泛化性存疑。  
- **模型规模局限**：仅实验于Qwen3-VL-8B，未探索更大或更小规模模型的效果，结论可能受限于单一基座。  
- **学生准确率差距**：学生MCQ 68.59%仍低于教师15.95个百分点，压缩过程可能丢失部分复杂推理能力，在安全关键场景中未必可靠。  
- **多轮交互限制**：K=2可能无法纠正所有深度错误，更大K虽能提升准确但会增加训练复杂度。  
- **实时部署挑战**：416ms虽低，但尚未在车载嵌入式平台测试；多轮推理仅在训练阶段使用，推理阶段学生单次前向，但教师仍用于蒸馏数据生成，实际部署算力需求仍高。  
- **验证器泛化性**：验证器基于Qwen3-VL-235B标注并经过人工验证，但训练集可能覆盖不全，对未见过的错误类型可能失效。  
- **偏向性风险**：奖励设计中安全维度权重可能仍不足以彻底消除保守偏差，实验中未专门分析误判类型。

（完）
