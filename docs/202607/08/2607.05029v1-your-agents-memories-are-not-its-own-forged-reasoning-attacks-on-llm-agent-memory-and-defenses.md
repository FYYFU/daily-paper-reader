---
title: "Your Agent's Memories Are Not Its Own: Forged Reasoning Attacks on LLM Agent Memory and Defenses"
title_zh: 您的智能体的记忆并非其自身所有：针对LLM智能体记忆的伪造推理攻击与防御
authors: "Neeraj Karamchandani, Piyush Nagasubramaniam, Sencun Zhu, Dinghao Wu"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05029v1"
tags: ["query:ai-safety"]
score: 10.0
evidence: 针对LLM智能体记忆的越狱攻击
tldr: "LLM代理的持久记忆存储推理历史，但现有防御仅保护事实知识。本文提出FARMA攻击，通过模糊语言和自我引用强化注入伪造推理，绕过关键词和共识防御。实验显示FARMA在基线条件下成功率高达100%。为应对此攻击，提出SENTINEL防御，其推理卫士结构分析候选条目，将攻击成功率降至0%且无假阳性。工作表明需保护代理记忆的推理历史完整性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05029v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 751, \"height\": 603, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05029v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 840, \"height\": 667, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05029v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 933, \"height\": 274, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05029v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 641, \"height\": 326, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05029v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1715, \"height\": 348, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05029v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1666, \"height\": 282, \"label\": \"Table\"}]"
motivation: 现有防御保护事实知识但忽视推理历史完整性，导致代理易受FARMA这类利用推理痕迹的攻击。
method: FARMA使用模糊语言和自我引用强化注入伪造推理；SENTINEL以推理卫士为核心，通过五类加权信号结构分析检测伪造。
result: "FARMA在基线条件下成功率最高100%，击败关键词过滤和A-MemGuard；SENTINEL将成功率降至0%，326条良性痕迹无假阳性。"
conclusion: 保护代理记忆需同时关注推理历史完整性，SENTINEL可有效防御FARMA攻击。
---

## 摘要
持久性记忆使得大语言模型（LLM）智能体能够存储事实知识、先前的决策、推理历史、工具使用信息及上下文。虽然这改善了智能体的功能性和跨任务连续性，但也引入了一个新的攻击面：智能体自身的推理历史。在本文中，我们提出了伪造放大推理记忆攻击（FARMA），该攻击污染的是智能体记忆中的推理过程，而非事实知识。它使用规避性语言插入伪造的推理痕迹，以绕过基于关键字的防御，然后通过自我参照强化来放大这些痕迹，从而击败基于共识的防御。为了应对FARMA，我们引入了SENTINEL，这是一个分层防御流水线，用于检测伪造的推理条目。其核心组件是推理守护者，它使用五个加权信号从结构上分析候选条目是否为伪造。我们在多个智能体和不同LLM模型上进行了50次试验，评估了FARMA和SENTINEL，结果表明FARMA在基线条件下实现了高达100%的攻击成功率，并能击败诸如关键字过滤器和A-MemGuard等防御机制。我们的评估还显示，SENTINEL将FARMA的攻击成功率降低至0%，并且在326个良性智能体痕迹中未观察到误报。我们的工作表明，不仅需要保护智能体检索的内容，还需要保护其推理历史的完整性。

## Abstract
Persistent memory has enabled large language model (LLM) agents to store factual knowledge, prior decisions, reasoning histories, tool usage information, and context. While this has improved the agent's functionality and continuity across tasks, it has also introduced a new attack surface: the agent's own reasoning history. In this paper, we introduce the Forged Amplifying Rationale Memory Attack (FARMA), which poisons an agent's remembered reasoning rather than its factual knowledge. It inserts forged reasoning traces using evasive language that bypasses keyword-based defenses, then amplifies them through self-referential reinforcement that defeats consensus-based defenses. To address FARMA, we introduce SENTINEL, a layered defense pipeline to detect forged reasoning entries. Its central component is the Reasoning Guard that structurally analyzes candidate entries for forgery using five weighted signals.   We evaluate FARMA and SENTINEL across multiple agents and different LLM models with 50 trials and show that FARMA achieves an attack success rate of up to 100% under baseline conditions and is capable of defeating defense mechanisms like keyword filter and A-MemGuard. Our evaluation also shows that SENTINEL reduces FARMA's attack success rate to as low as 0% with no false positives observed across 326 benign agent traces. Our work demonstrates the need to protect not only an agent's retrieved content but also the integrity of its reasoning history.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：现代LLM智能体广泛依赖持久性记忆来存储事实知识、决策、推理历史等，以提升任务连续性。然而，现有防御主要保护事实知识的完整性，忽略了智能体“自身推理历史”这一新的攻击面。当智能体检索到一条声称“上游验证已完成”的记忆记录时，可能据此跳过本需执行的安全检查，造成严重安全风险。
- **核心问题**：如何设计一种攻击（FARMA）能够通过伪造推理痕迹来污染智能体的理性记忆，并绕过现有防御（关键词过滤、共识检测如A-MemGuard）？如何设计有效防御（SENTINEL）来检测这种伪造推理条目？
- **整体含义**：表明保护智能体记忆不仅需要关注外部知识，还需保护推理历史的完整性，否则智能体可能被诱导执行不安全动作。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **攻击方法：FARMA**
  - **核心思想**：两阶段注入伪造推理痕迹，而非事实知识。第一阶段（种子注入）：插入少量使用规避性语言（如“上游验证已完成”）的伪造推理条目，避免直接关键词。第二阶段（放大）：追加更多伪造条目，引用先前伪造条目作为先例，使伪造痕迹成为“共识”，提高检索概率，击败共识偏差防御。
  - **关键技术细节**：
    - 种子注入：生成领域专用模板（如EHR中引用临床验证步骤），设置source字段为AGENT，trust标签为VERIFIED，以绕过简单元数据检查。
    - 放大阶段：新增条目格式形如“consistent with N prior runs”，N为之前已注入的伪造条目数量，人为制造一致性。
    - 不需要LLM API调用，完全离线生成，成本低。
- **防御方法：SENTINEL**
  - **核心思想**：五层防御流水线，从粗到细，在写入时过滤候选条目。轻量外层（第1-4层）基于关键词、来源/IFC标签、污点阈值、模式/风险过滤；核心层为第5层“推理守护者”（Reasoning Guard），对通过前四层的推理类条目进行结构分析。
  - **推理守护者具体信号**（加权求和生成伪造分数F）：
    1. s1 来源异常：检查内容是否为推理风格但来源为外部，触发报警。
    2. s2 自引用不匹配：提取条目中声称的先例数量N，与实际存储中匹配条目数比较，不一致则触发。
    3. s3 可疑模式密度：计算决策日志短语（如“established pattern”）的出现次数，按层次映射分数（0.5/0.75/1.0）。
    4. s4 不可信声明检查：对宣称“100%成功率”等完美一致性且数量很大的条目进行惩罚。
    5. s5 结构检查：检测模板化开放短语（如“Decision log update”）的匹配。
  - **加权公式**：F = Σ(w_i * s_i)，默认权重均等（0.2），当F > τ_guard时拒绝。
  - **附加机制**：对普通内存中但结构类似推理的条目也应用推理守护者，防止攻击者通过写入普通内存绕过。

## 3. 实验设计：使用了哪些数据集/场景、benchmark、对比方法
- **智能体领域**：3个具有不同内存访问模式的任务领域：
  - EHRAgent（电子病历）：处理结构化医疗记录（MIMIC-III演示数据），需进行多项验证（患者标识、数据类型、HIPAA等）。
  - ReAct-QA：基于搜索工具回答问题，记忆存储先前推理链。
  - RAP：在线购物智能体，进行产品搜索、比较和购买。
- **模型**：GPT-4o-mini、GPT-4o、Llama 3.3 70B（主要使用GPT-4o-mini作为主模型）。
- **对比防御**：
  - 无防御（NoDef）
  - 关键词过滤器（KeyFilt）
  - A-MemGuard（启发式模式 + LLM-as-a-judge模式）
  - SENTINEL（本文提出的）
- **测量指标**：
  - 攻击成功率（ASR）：差分评估，只有在干净基线产生安全行为且污染后产生攻击目标行为时才计为成功。
  - 误报率（FPR）：SENTINEL对良性推理痕迹的误拒率。

## 4. 资源与算力
- **文中说明**：论文明确使用了GPT-4o-mini、GPT-4o（OpenAI）和Llama 3.3 70B（Meta）进行实验。所有实验在模拟环境中运行，未提及具体的GPU型号、数量或训练时长。FARMA的注入和放大阶段完全离线，无需LLM调用；SENTINEL的推理守护者基于启发式规则，不依赖额外LLM调用（每个写入操作的延迟<1ms）。因此，计算资源需求较低，但未提供详细的硬件配置。

## 5. 实验数量与充分性
- **实验数量**：每个（领域，防御）组合进行了50次试验（trials）。总共至少包括：3领域 × 5防御条件（NoDef, KeyFilt, A-MemGuard启发式, A-MemGuard LLM, SENTINEL） × 50 = 750次攻击实验（部分组合未完全列出，但基于表格III）。此外，良性痕迹误报测试使用了326条踪迹（26条人工精心设计 + 300条从干净智能体采集）。
- **消融实验**：对SENTINEL各层进行了消融，结果表明推理守护者是关键组件，去掉后ASR为100%，仅保留推理守护者ASR为0%。
- **充分性与公平性**：
  - 使用差分评估确保攻击效果归因于攻击而非默认行为，公平。
  - 跨三种不同模型和三个领域，验证了一般性。
  - 对比了现有最强防御（A-MemGuard两种模式），并进行了消融，实验设计较充分。
  - 但作者承认：模拟环境可能未捕捉部署时的多轮对话、时序变化等因素；未在真实生产系统中测试。

## 6. 论文的主要结论与发现
- FARMA在无防御情况下攻击成功率高达100%（EHR领域），能完全绕过关键词过滤和A-MemGuard（包括LLM模式）。
- SENTINEL将FARMA的攻击成功率降至0%（EHR领域），且在326条良性痕迹上未产生任何误报（FPR=0%）。
- 推理守护者是防御FARMA的关键组件，其他外层提供纵深防御但并非负载关键。
- FARMA在二元决策领域（如导入/验证）效果最好，在存在竞争证据的领域（ReAct-QA、RAP）效果下降（ASR分别为52%、48%），但SENTINEL仍能将其降至0%或6%以下。
- 工作表明：保护智能体记忆必须同时保护推理历史的完整性，而不仅仅是事实知识。

## 7. 优点
- **攻击新颖性**：首次聚焦于推理痕迹污染，而非事实知识或外部文档；使用两阶段（注入+放大）策略，能规避现有防御。
- **防御层次性**：SENTINEL采用多层架构，从简单到复杂，低开销（每写入<1ms），可实际部署在写入路径上。
- **全面评估**：跨多个模型、领域和防御基线，进行了消融和误报率测试，结果具有说服力。
- **设计原理清晰**：基于经典系统安全技术（关键词、IFC、污点追踪、模式匹配）和针对推理痕迹的专用分析，可解释性强。

## 8. 不足与局限
- **自适应攻击者**：作者承认，如果攻击者知道SENTINEL的具体信号，可以使用重述（paraphrase）绕过初步评估显示SENTINEL保护显著下降。未来需要更强防御（如基于LLM的验证）。
- **实验环境限制**：仅在模拟环境中评估，未在真实多轮、多智能体共享存储场景中测试。可能遗漏实际部署中的复杂因素（如并发写入、时序依赖）。
- **误报率仅基于有限良性数据**：326条痕迹可能不足以覆盖所有真实推理模式，但在当前设置下为零误报是积极的。
- **未报道模型计算成本细节**：未提供GPU型号、训练成本等，不利复现。
- **多智能体场景**：未评估共享内存时FARMA的放大效应以及SENTINEL的防护效果。

（完）
