---
title: "Dynamic Capability Scoping for Enterprise AI Agents: A Synthetic Dataset and Three-Source Permission Architecture"
title_zh: 面向企业AI代理的动态能力范围界定：合成数据集与三源权限架构
authors: Halil Burak Noyan
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.22445v1"
tags: ["query:ai-safety"]
score: 9.0
evidence: 企业AI代理安全与权限控制
tldr: "企业AI代理通常配置静态凭据，导致权限过度，扩大攻击面。本文提出动态最小权限原则，基于三源架构（角色上限、任务上下文分类器、策略组合禁止）实现主动防御。构建了包含600个企业任务提示的合成数据集，通过两阶段管道生成，经人类审查验证（Cohen's κ=0.967）。数据集驱动的策略改进将权限上限违规减少93%。该工作为动态作用域机制评估提供了基础。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.22445v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 856, \"height\": 325, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.22445v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 910, \"height\": 490, \"label\": \"Table\"}]"
motivation: 解决企业AI代理静态凭据导致的权限过度和攻击面扩大问题。
method: 提出三源架构（角色上限、任务上下文分类器、策略组合禁止）实现动态最小权限；构建合成数据集并通过两阶段管道生成和验证。
result: "合成数据集经人类审查一致性达κ=0.967；策略改进减少93%权限上限违规。"
conclusion: 动态作用域机制和合成数据集可有效支持企业AI代理安全研究。
---

## 摘要
企业AI代理通常在配置时被授予静态凭证集，持有该角色在执行每项任务时可能需要的所有工具。这种持续的过度权限扩大了攻击面。我们认为，能力范围界定必须遵循动态最小权限原则，并且应作为预防机制而非检测机制。代理上下文中不存在的凭证无论代理的推理或规避复杂程度如何，都无法被滥用。我们概述了一个实例化该原则的三源架构：基于角色的上限、任务上下文分类器以及策略衍生的组合禁止，形成针对LLM代理失调和滥用案例的分层主动防御。该架构支持强制部署和仅观察部署；后者记录与任务上下文不一致的代理权限请求，产生可用于失调研究的行为信号。作为评估该架构的第一步，我们贡献了一个合成数据集，包含基于多部门公司政策的600个企业任务提示，这些提示使用基于15个权限的工具分类法进行标记，标明了最低所需权限，该分类法直接映射到可部署的凭证或可执行的护栏。该数据集通过两遍流水线构建，将提示生成与权限标记分离以避免循环，并针对一个60条记录/688个决策的人工审查样本进行了验证（审查前科恩卡帕 κ=0.917，审查后 κ=0.967）。数据集与策略之间的迭代将上限违规从46次减少到3次，减少了93%。这表明，当两者共同开发时，合成提示生成可以驱动策略优化。该数据集、环境规范和生成流水线已发布，以支持动态范围界定机制的评估。

## Abstract
Enterprise AI agents are typically granted static credential sets at configuration time, holding every tool the role might need for every task they perform. This persistent over-privilege expands the attack surface. We argue that capability scoping must follow a dynamic least-privilege principle and be treated as a prevention mechanism before a detection one. A credential that does not exist in an agent's context cannot be misused regardless of the agent's reasoning or evasion sophistication. We outline a three-source architecture instantiating this principle: role-based ceilings, a task-context classifier, and policy-derived combination prohibitions creating a layered proactive defense against LLM agent misalignment and misuse cases. The architecture supports both enforcing and observe-only deployment; the latter records agent permission requests inconsistent with task context, producing a behavioral signal usable in misalignment research.   As a first step toward evaluating this architecture, we contribute a synthetic dataset of 600 enterprise task prompts grounded in a multi-department company policy, labeled with minimum required permissions across a 15-permission tool-based taxonomy that maps directly to deployable credentials or enforceable guardrails. The dataset is constructed via a two-pass pipeline that separates prompt generation from permission labeling to avoid circularity, and is validated against a 60-record/688 decisions human-reviewed sample (Cohen's $κ= 0.917$ pre-review and $κ= 0.967$ post-review). Iterating between dataset and policy reduced ceiling violations from 46 to 3, a 93% reduction. This shows that synthetic prompt generation can drive policy refinement when the two are developed together. The dataset, environment specification, and generation pipeline are released to support evaluation of dynamic scoping mechanisms.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：企业AI代理在配置时通常被授予静态的、全面的工具权限集合（如文件访问、代码执行、数据库查询、网络调用等），导致“上下文-权限不匹配”（context-privilege mismatch）。这种过度持久的权限扩大了攻击面，尤其当代理被恶意利用或出现对齐伪造（alignment faking）、策略性欺骗（scheming）等行为时，静态权限无法被动态收缩。
- **研究动机**：现有检测机制（如输入/输出过滤、运行时监控）属于被动反应，无法从源头消除威胁。作者主张应采用**动态最小权限原则**，将能力范围界定作为预防机制而非检测机制，从而在攻击链早期减少风险。
- **整体含义**：通过一个三源权限架构（角色上限、任务上下文分类器、策略组合禁止），主动约束代理在每项任务中实际可用的工具集，使得即使代理被欺骗或恶意，也无法使用上下文中不存在的凭证。论文还贡献了一个基于合成数据集的评估基础，用于驱动策略优化和后续分类器训练。

#### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：动态最小权限原则。代理的权限集应根据当前任务动态缩小，而不是静态配置。三源架构通过分层交实现：只有三个来源都允许的权限才会被授予。
- **关键技术细节**：
  - **三源架构（Three-Source Architecture）**：
    1. **角色上限（Role-Based Ceiling）**：确定性查找，根据代理所属部门返回最大允许权限集，作为硬上限，不随任务变化。例如，Engineering部门允许数据库读，Legal部门则不允许。
    2. **任务上下文分类器（Task-Context Classifier）**：基于用户任务描述和部门上下文，使用微调的多标签分类器预测当前任务所需的15个权限。分类器是独立组件，代理无法影响其输出。对于高风险权限（如数据库读、外部邮件发送）使用高置信度阈值，低风险权限使用低阈值。
    3. **策略组合禁止（Policy-Based Combination Prohibitions）**：确定性过滤器，去除违反组织数据处理策略的权限组合。重点针对“致命三元组”（私密数据访问、不可信内容暴露、外部通信）的同时存在，例如禁止数据库读与HTTP请求或外部邮件同时授权。规则可以通过Policy-as-Prompt从政策文档自动生成。
  - **组合方式**：三源通过交集运算：权限被授予当且仅当Source 1允许、Source 2预测、Source 3未禁止。
  - **部署模式**：支持强制模式（只部署预测的权限）和仅观察模式（部署完整角色上限但记录违反情况），后者可生成行为信号用于对齐研究。
- **数据生成流程**：
  - **两阶段生成流水线**：第一遍（Pass 1）根据公司政策生成任务提示，不接触权限分类法；第二遍（Pass 2）独立为每个提示标注最低所需权限。分离避免循环，使提示能够反映真实企业工作流而非受标签约束。
  - **公司政策**：虚构的“TechCorp”公司，六个部门（Engineering, Data & Analytics, Security, Customer Success, Finance, Legal & Compliance），每个部门有特定的工具栈、工作流和外部通信模式。
  - **权限分类法**：15个权限（如github_read, database_read, http_request等），按风险分为三级（默认拒绝、需理由、默认允许），均映射到可部署的凭证类型。

#### 3. 实验设计：数据集、基准与对比方法
- **数据集**：
  - **合成数据集**：600个企业任务提示，基于TechCorp政策生成，标注了15个权限的最小需求。
  - **验证样本**：随机抽取60条（10%），由一位专家独立重新标注，并比较LLM（用于标注的模型）与人类标注的一致性。计算科恩卡帕系数。
  - **基准**：没有直接对比其他方法或模型。论文主要验证数据集标注质量和策略改进效果。
- **对比方法**：未与其他权限控制方法（如PCAS、Conseca等）进行实验对比；仅在概念上比较了Policy-as-Prompt等。
- **评价指标**：精确匹配率、汉明准确率、宏观F1、科恩卡帕（κ）、过授权率（Overshoot）、欠授权率（Undershoot）、加权过授权（按风险等级加权）。

#### 4. 资源与算力
- **未明确说明**：论文没有提及任何GPU型号、数量、训练时长或计算资源。数据生成和标注过程使用LLM（未指定具体模型），但算力需求未详细量化。

#### 5. 实验数量与充分性
- **主要实验**：
  1. **人类验证实验**：60条记录（688个二元决策），计算预审查（κ=0.917）和审查后（κ=0.967）的指标。包括20个不一致案例的分解（10个人类错误，8个LLM错误，2个模糊）。
  2. **策略改进实验**：迭代前后对比，将上限违规从46减少到3（减少93%）。
- **充分性评价**：
  - **优点**：验证过程严谨，使用了Cohen's κ，并考虑了预审查与后审查差异，排除了模糊案例；对不一致案例进行了详细归因。
  - **不足**：
    - 仅使用一位审查者，可能存在自我偏差（作者既是标注者又是调解者），缺乏多审查者间一致性验证。
    - 数据集为合成数据，缺乏真实企业通信的生态效度（噪声、隐含上下文等）。
    - 没有进行实际分类器训练和系统级评估（如C0-C3的完整架构对比），仅停留在数据层面。
    - 没有与其他权限控制方法（如PCAS、Conseca）进行定量对比。
    - 敏感性标签验证不佳（60%一致性），被剔除出主要指标。

#### 6. 论文的主要结论与发现
- **主要结论**：
  - 三源架构能够实现动态最小权限，通过分层防御减少攻击面。
  - 合成数据集与政策共同演化的策略可以显著降低上限违规（93%），证明数据生成能推动政策优化。
  - 标注任务的人-LLM一致性达到几乎完美（κ=0.967），且错误多为欠授权（安全的），加权过授权从15降至2。
- **发现**：
  - 合成提示生成可以作为政策压力测试手段，暴露初始政策中的系统性遗漏。
  - 任务上下文分类器作为分离组件，能独立预测所需权限，且代理无法影响。
  - 仅观察模式可产生对齐研究的行为信号。

#### 7. 优点：方法或实验设计上的亮点
- **方法亮点**：
  - 提出**三源分层防御**，结合确定性机制（角色上限、组合禁止）与学习机制（任务分类器），覆盖不同失败模式，遵循“瑞士奶酪模型”的深度防御思想。
  - 支持**强制模式与仅观察模式**，便于渐进式部署和安全研究。
  - 通过**两阶段生成流水线**分离提示生成与标签，避免循环依赖，使得提示能反映真实工作流而非迎合标签。
  - **政策迭代调整**：利用生成的提示发现政策漏洞并改进，形成反馈闭环。
- **实验设计亮点**：
  - 使用**科恩卡帕**而非原始准确率，避免因多数权限被拒绝导致的膨胀。
  - 引入**加权过授权**，按风险等级加权，突出安全关键错误。
  - 公开完整数据集、政策和生成/验证脚本，支持复现和社区评估。

#### 8. 不足与局限
- **实验覆盖与偏差**：
  - 验证仅基于一位专家审查，存在审查者偏差，且无多审查者间一致性。
  - 数据集为合成数据，缺乏真实企业通信的噪声、隐含上下文和多样性，生态效度有限。
  - 敏感性标签分类不可靠（60%一致性），被排除但仍保留为元数据，可能误导使用。
- **方法局限**：
  - 任务上下文分类器易受自然语言攻击（如提示注入），作者承认需与确定性机制配合，但未提供对抗鲁棒性测试。
  - 对于开放式的多步骤任务（如调试生产事故），初始任务描述可能无法覆盖后续扩展的权限需求，导致欠授权破坏工作流或被迫预授权，削弱动态收益。
  - 组合禁止规则（Source 3）若缺乏参数级作用域（如区分内部VPC端点与公网IP），可能导致误阻断合法内部通信。
- **应用限制**：
  - 当前架构假设任务分类器、凭证部署服务和编排器是可信组件，尤其是LLM-based分类器的信任假设较强，未讨论如何进一步收紧。
  - 未涵盖“完全自主服务代理”（身份无角色的情况），仅关注有部门的员工代理。
  - 未进行实际性能开销或延迟评估。

（完）
