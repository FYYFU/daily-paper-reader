---
title: "Penelope: Localized Latent Recurrence for Efficient Structured Reasoning"
title_zh: 佩内洛普：局部化潜在循环实现高效结构化推理
authors: "Yutong Chen, Shouqian Shi, Xinran Liu, Haochen Wang, Jiaying Wang, Tianxing Xu, Yuanxi Wang, Zirui Ding"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25915v1"
tags: ["query:eff-reason"]
score: 9.0
evidence: 局部潜在循环用于LLM高效结构化推理
tldr: 针对复杂结构化推理计算开销大的问题，提出Penelope框架，将循环推理局部化到选定的解码器区间，通过边界记忆迭代细化和渐进式课程学习将显式推理转移至潜在空间。在开源基准上，以验证选定的潜在预算获得了与现有潜在推理模型相当的精度，同时降低了推理延迟。表明潜在细化可局限在窄解码器区间，无需完整解码器重复执行或长中间步骤。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25915v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1497, \"height\": 813}, {\"url\": \"assets/figures/arxiv/2607.25915v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 867, \"height\": 612}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25915v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1853, \"height\": 777}, {\"url\": \"assets/tables/arxiv/2607.25915v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 886, \"height\": 238}, {\"url\": \"assets/tables/arxiv/2607.25915v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 470}]"
motivation: 现有语言模型通过增加参数规模或序列化中间步骤（CoT）获取额外计算，导致训练部署成本高或推理长度受限。
method: Penelope在预训练解码器-only Transformer中定位循环计算至选定区间，用渐进式CoT-to-latent课程将推理转移至潜在通道。
result: 在开源结构化推理基准上，以验证选定的潜在预算，Penelope达到与现有潜在推理模型相当的准确率，并降低测量推理延迟。
conclusion: 潜在细化可局限在窄解码器区间，减少完整解码器重复执行，不生成长显式推理轨迹，提供实用的准确率-效率权衡。
---

## 摘要
复杂的结构化推理任务通常需要额外的计算，然而当前的语言模型主要通过扩大参数规模或将中间步骤串行化为思维链（CoT）标记来获得这种计算能力。前者增加了训练和部署成本，而后者则将推理计算与自回归输出长度绑定。我们提出佩内洛普，一个高效的潜在推理框架，适用于预训练的仅解码器Transformer，它将循环计算局部化到选定的解码器区间内。较低层的解码器前缀被评估一次，以构建问题条件化的边界记忆，然后通过时间调制的GRU动态和循环读出状态进行迭代细化，最后生成答案。一种渐进式的从CoT到潜在的课程将可见推理转移到这个内部循环路径中，使得额外计算可以在潜在空间中进行分配，而无需重复执行整个解码器或生成冗长的中间轨迹。在开源的结构化推理基准上的实验表明，在验证集上选择的潜在预算下，佩内洛普相对于已有的潜在推理模型获得了有竞争力的准确率，同时减少了测量的推理延迟。这些结果表明，潜在细化可以局部化到狭窄的解码器区间，减少了重复的全解码器执行，而不产生长的可见推理轨迹，并为仅解码器Transformer模型提供了实用的准确率-效率权衡。

## Abstract
Complex structured reasoning tasks often require additional computation, yet current language models obtain it mainly by increasing parameter scale or by serializing intermediate steps as chain-of-thought (CoT) tokens. The former raises training and deployment costs, while the latter ties reasoning computation to autoregressive output length. We introduce Penelope, an efficient latent-reasoning framework for pretrained decoder-only Transformers that localizes recurrent computation to a selected decoder interval. The lower decoder prefix is evaluated once to construct a problem-conditioned boundary memory, which is then iteratively refined through time-modulated GRU dynamics and recurrent readout states before answer generation. A progressive CoT-to-latent curriculum transfers visible reasoning into this internal recurrent path, allowing additional computation to be allocated in latent space without repeatedly executing the complete decoder or generating a long intermediate trace. Experiments on open-source structured-reasoning benchmarks show that, at validation-selected latent budgets, Penelope attains competitive accuracy relative to established latent-reasoning models while reducing measured inference latency. These results show that latent refinement can be localized to a narrow decoder interval, reducing repeated full-decoder execution without generating a long visible reasoning trace and providing a practical accuracy-efficiency tradeoff for decoder-only Transformer models.

---

## 论文详细总结（自动生成）

# 论文《Penelope: Localized Latent Recurrence for Efficient Structured Reasoning》结构化总结

## 1. 核心问题与整体含义

- **研究动机**：当前大语言模型为了进行复杂结构化推理，通常依赖两种方式获取额外计算：一是扩大模型参数规模（训练/部署成本高），二是通过链式思维（CoT）将中间步骤串行化为可见推理token（计算量与输出长度绑定）。这两种途径均不够高效。
- **整体含义**：本文提出“局部化潜在循环”（Localized Latent Recurrence）的框架 Penelope，在不重复执行整个解码器、不生成冗长中间文本的前提下，通过将循环推理限制在选定的窄解码器区间内，实现推理计算的有效分配，从而在保持推理精度的同时显著降低推理延迟。

## 2. 方法论

- **核心思想**：将解码器划分为三个部分：下前缀（lower prefix）`F[0:ℓs)`、循环区间（recurrent interval）`F[ℓs:ℓe)`、上输出区间（output-side interval）`F[ℓe:L)`。只对循环区间进行多次迭代，其余部分只执行一次。
- **关键技术细节**：
  - **一次性下前缀**：将提示嵌入和固定大小的潜在锚点（8个记忆锚点+2个读出锚点）通过下前缀层，构建问题条件化的边界记忆 `M0` 和读出状态 `R0`，同时缓存循环区间内的KV `Cx`。
  - **局部循环细化**：对循环区间重复K步。每步输入当前记忆 `M_{t-1}` 加上组嵌入和步嵌入，通过共享的循环区间读取缓存 `Cx` 得到 `H^{lat}_t`；再经时间调制（time modulation，根据步归一化坐标产生调制和更新尺度）、残差适配器、GRU_M 更新记忆 `M_t`，同时用 GRU_R 更新读出状态 `R_t`。
  - **最终答案生成**：K步后，将最终记忆和读出状态通过上输出区间一次，构造一致的注意力上下文，然后标准自回归解码生成答案。
- **训练算法**：采用渐进式课程学习，逐步用潜在步骤替换可见CoT步骤，从全coT开始逐步增加潜在步数K（K=1至6），最后固定K=6并仅优化答案生成。损失是预测token的交叉熵。

## 3. 实验设计

- **数据集与场景**：
  - Deep ListOps：合成表达式归约任务，需要递归计算嵌套运算符，训练4.8k/验证800/测试1.6k，深度1-12。
  - ProsQA：多步逻辑推理（合成），训练17.9k/验证300/测试500。
  - PrOntoQA：本体推理（合成），训练9k/验证200/测试800。
- **评估指标**：精确匹配（EM）和推理延迟（毫秒，batch=1，BF16，NVIDIA RTX PRO 6000）。
- **对比方法**：
  - Visible CoT（标准链式思维）
  - Coconut（全解码器潜在循环，K=6或8）
  - CODI（压缩CoT到连续空间，6个隐状态）
  - 全解码器循环参考（固定K）
- **骨干网络**：主要使用 Llama-3.2-1B（16层），部分实验使用 Qwen3.5-0.8B-Base 进行跨骨干验证。
- **训练配置**：所有方法共享相同源检查点、数据集、batch size（4或8）、优化器更新步数（1.5k-3k），采用LoRA（rank-16）微调，AdamW优化器。Penelope额外包含循环模块参数（64.08M），总计75.35M可训练参数（vs Coconut 11.28M）。

## 4. 资源与算力

- **硬件**：推理在单张 NVIDIA RTX PRO 6000 上，使用BF16精度。
- **训练**：未明确指定GPU型号、数量或训练总时长。训练禁用自动混合精度，采用单个有效batch size（4或8）在1.5k-3k更新步内完成，三个任务各3次种子运行。
- **参数规模**：Penelope 训练参数量（75.35M）大于 Coconut（11.28M），但论文效率声明基于推理拓扑和延迟，而非参数效率。

## 5. 实验数量与充分性

- **主要实验**（表1）：三个任务各3次独立运行（不同种子），报告平均EM和标准差，并给出配对延迟数据（Deep ListOps和PrOntoQA使用一组配对检查点，ProsQA使用3次运行均值）。
- **消融与机制分析**：
  - 推理时K=0诊断（仅边界，无循环）→ 验证局部循环的贡献（平均增益0.69点）。
  - 替换简单覆盖 `M_t = H^{lat}_t` → 精度下降0.81点，证明时间调制、门控等设计的必要性。
  - 放置分析（表2）：固定K=6，比较循环区间位于早期、中期、输出侧、全解码器，发现输出侧区间与全解码器性能仅差0.27点，但层应用次数少2.51倍。
  - 跨骨干控制（表3）：在 Qwen3.5-0.8B 上重复 Deep ListOps，结果与Llama一致。
- **充分性评价**：实验覆盖三个不同难度的结构化推理任务、两种骨干、多种消融，对比了多个代表性基线，控制源检查点、数据、更新步数等变量，较为公平。但未进行更大模型（7B+）或更复杂开放域推理任务的验证，且训练参数不对等。

## 6. 主要结论与发现

- Penelope 在验证集上选择固定潜在步数后，在三个任务上均获得与全解码器潜在方法（Coconut）相当的 EM，且显著降低推理延迟：Deep ListOps 减少46.9%，ProsQA 减少33.3%，PrOntoQA 减少42.2%。
- 局部循环足以支持结构化推理：输出侧区间（5层）的循环性能与全解码器（16层）差距很小，而边际计算成本仅为 r/L ≈ 31%。
- 渐进课程训练有效将可见推理转移至潜在路径，K=0诊断显示少量改进，说明循环提供了修正能力。
- 时间调制、门控GRU等设计并非冗余，替换为简单覆盖后精度下降。

## 7. 优点

- **创新性**：提出“局部化潜在循环”范式，首次将循环推理限制在窄解码器区间，利用缓存避免重复执行全模型，是高效的推理计算分配思路。
- **方法完备性**：包含一次性下前缀、时间调制、GRU记忆更新、读出状态、渐进课程等成套设计，消融实验验证了各组件作用。
- **实验严谨性**：多任务、多骨干、多次运行，控制变量（源检查点、数据、更新步数、batch），给出置信区间和延迟分布，放置和机制分析充分。
- **实际价值**：在保持竞争精度的同时显著降低延迟，对资源受限或低延迟场景有直接意义。

## 8. 不足与局限

- **固定深度而非自适应**：Penelope 使用验证集选择的固定K（1-6），未实现输入自适应的迭代停止，限制了在更易/难问题上的效率优化。
- **规模受限**：仅评估1B左右模型，未在7B+等更大骨干上验证可扩展性；仅覆盖合成推理任务，对自然语言复杂推理（如数学、代码）缺乏分析。
- **训练参数不匹配**：Penelope 训练参数（75.35M）远大于 Coconut（11.28M），即使推理效率提升，训练成本更高；论文将效率声明限定于推理拓扑和延迟，未讨论训练公平性。
- **非单调深度缩放**：实验显示精度随K变化非单调，额外步数不一定提升性能，缺乏指导深度选择的稳定规律。
- **延迟统计处理**：部分延迟数值来自单对检查点而非多运行均值，且未报告所有实验的完整延迟分布，可能低估方差。

（完）
