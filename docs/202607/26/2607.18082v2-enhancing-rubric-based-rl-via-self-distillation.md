---
title: Enhancing Rubric-based RL via Self-Distillation
title_zh: 通过自蒸馏增强基于评分准则的强化学习
authors: "Mingxuan Xia, Yuhang Yang, Chao Ye, Shuai Zhu, Shenzhi Yang, Guangcheng Zhu, Yuhang Zhang, Cheng Peng, Haobo Wang, Siqing Wang"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18082v2"
tags: ["query:opd"]
score: 9.0
evidence: 基于评分标准的强化学习中的自蒸馏
tldr: 基于评分强化学习存在未探索准则和受抑制准则两类问题，前者因缺少探索信号，后者因优势聚合为负而丢失学习信号。现有探索方法引入外部指导导致训练-推理不匹配，且忽视受抑制准则。本文提出CriPO，通过在线自蒸馏同时处理两者：对未探索准则构造准则注入自教师施加局部前向KL损失，对受抑制准则用反事实自教师翻转令牌优势。在医学和科学基准上，CriPO一致超越基线，优化步骤减少约2倍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18082v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1454, \"height\": 423}, {\"url\": \"assets/figures/arxiv/2607.18082v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 573, \"height\": 425}, {\"url\": \"assets/figures/arxiv/2607.18082v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1447, \"height\": 689}, {\"url\": \"assets/figures/arxiv/2607.18082v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 629, \"height\": 409}, {\"url\": \"assets/figures/arxiv/2607.18082v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1437, \"height\": 418}, {\"url\": \"assets/figures/arxiv/2607.18082v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1441, \"height\": 396}, {\"url\": \"assets/figures/arxiv/2607.18082v2/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1387, \"height\": 405}, {\"url\": \"assets/figures/arxiv/2607.18082v2/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1396, \"height\": 433}, {\"url\": \"assets/figures/arxiv/2607.18082v2/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 716, \"height\": 445}, {\"url\": \"assets/figures/arxiv/2607.18082v2/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1414, \"height\": 1260}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18082v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1451, \"height\": 742}, {\"url\": \"assets/tables/arxiv/2607.18082v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 761, \"height\": 406}, {\"url\": \"assets/tables/arxiv/2607.18082v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 702, \"height\": 400}, {\"url\": \"assets/tables/arxiv/2607.18082v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1451, \"height\": 742}, {\"url\": \"assets/tables/arxiv/2607.18082v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1378, \"height\": 703}]"
motivation: 现有Rubric-based RL存在未探索准则和受抑制准则两类缺陷，且探索方法导致训练-推理不匹配。
method: 提出CriPO，通过在线自蒸馏同时处理两类准则：对未探索准则使用准则注入自教师和局部前向KL损失；对受抑制准则使用反事实自教师翻转令牌优势。
result: 在医学和科学基准上，CriPO一致优于Rubric-based RL，优化步骤减少约2倍。
conclusion: 通过自蒸馏机制同时解决未探索和受抑制准则，避免了训练-推理不匹配，显著提升了RL训练效率和性能。
---

## 摘要
基于评分准则的强化学习近期在改善大型语言模型（LLM）在开放式任务上的表现方面展现出潜力。该类方法公认的局限性在于探索不足：那些没有任何轨迹能满足的标准（即未探索准则，UC）无法获得优化信号。最近的方法通过在轨迹生成过程中引入评分标准信息作为外部指导来应对这一挑战，但它们引入了训练-推理不匹配问题：策略在外部指导下生成的轨迹上进行优化，而在推理时该指导不存在，导致自回归解码过程中误差累积。此外，这些以探索为导向的方法忽略了一种本质上不同的失败模式，我们称之为被抑制准则（SC）——某些轨迹满足了这些准则，但在优化过程中由于其标量奖励聚合赋予它们非正的总优势，导致学习信号丢失。我们的分析显示，SC非常普遍：超过57%的样本在整个训练过程中出现这种失败模式，每个样本平均有1.8个SC。为了在不引入训练-推理不匹配的情况下同时解决UC和SC，我们提出了准则蒸馏策略优化（CriPO），该方法通过同策略自蒸馏来增强基于评分准则的强化学习。针对UC，CriPO构建了一个准则注入的自教师模型，并计算局向前KL散度损失，将缺失的行为注入到策略中。针对SC，CriPO采用反事实自教师模型，在负优势轨迹中定位与准则相关的标记，并将其标记级优势翻转为正值，从而保留本会被抑制的有用模式。在医学和科学基准上的实验表明，CriPO始终优于基于评分准则的强化学习，能以大约减少2倍的优化步骤获得更强的最终性能。

## Abstract
Rubric-based RL has recently shown promise in improving LLMs on open-ended tasks. A widely recognized limitation of rubric-based RL is limited exploration: criteria that no rollout manages to satisfy (Unexplored Criteria, UC) receive no optimization signal. Recent methods address this by incorporating rubric information as external guidance during rollout, yet they introduce a train-inference mismatch: the policy is optimized on rollouts produced under external guidance while this guidance is absent at inference time, causing error accumulation through autoregressive decoding. Moreover, these exploration-focused approaches overlook a fundamentally different failure mode that we term Suppressed Criteria (SC) -- criteria that are satisfied by some rollouts yet whose learning signals are lost during optimization because scalar reward aggregation assigns them non-positive aggregate advantages. Our analysis reveals that SC are remarkably prevalent: over 57% of samples exhibit this failure mode throughout training, with an average of 1.8 SC per sample. To simultaneously address both UC and SC without introducing training-inference mismatch, we propose Criterion-Distilled Policy Optimization (CriPO), which enhances rubric-based RL via on-policy self-distillation. For UC, CriPO constructs a criterion-injection self-teacher and computes a localized forward-KL loss to inject missing behaviors into the policy. For SC, CriPO employs a counterfactual self-teacher to locate criterion-relevant tokens in negative-advantage rollouts and flips their token-level advantages to positive values, preserving useful patterns that would otherwise be suppressed. Experiments on medicine and science benchmarks demonstrate that CriPO consistently outperforms rubric-based RL, achieving stronger final performance with approximately $2\times$ fewer optimization steps.

---

## 论文详细总结（自动生成）

# 论文详细中文总结：Enhancing Rubric-based RL via Self-Distillation

## 1. 论文的核心问题与整体含义（研究动机和背景）

-   **研究背景**：基于可验证奖励的强化学习在数学和代码等确定性问题中取得了成功，但难以直接应用于医疗咨询、长文写作等开放式场景。为此，研究者引入**基于评分准则的强化学习**，使用大模型对多个准则打分并聚合为标量奖励，再用GRPO（Group Relative Policy Optimization）优化。
-   **核心问题**：该方法存在两大失败模式：
    -   **未探索准则**：模型未采样到任何满足该准则的轨迹，因此完全没有优化信号。
    -   **受抑制准则**：虽然某些轨迹满足了该准则，但由于标量奖励聚合，它们的整体优势为非正值，导致该准则对应的有用行为反而被抑制或忽略。
-   **现有方法缺陷**：旨在增强探索的方法（如RuscaRL、HeRL）通过外部准则信息指导轨迹生成，但引入了**训练-推理不匹配**（即生成时有无外部指导，推理时无，导致误差累积），且完全忽略了**受抑制准则**问题。

## 2. 论文提出的方法论：核心思想、关键技术细节

-   **方法名称**：CriPO（Criterion-Distilled Policy Optimization，准则蒸馏策略优化）。
-   **核心思想**：保留GRPO作为稳定的奖励优化骨干，同时利用**同策略自蒸馏**在不引入训练-推理不匹配的前提下，为两类失败准则提供密集的、标记级的修正信号。

### 关键技术与实现细节

-   **针对未探索准则：行为注入**
    -   **构造准则注入自教师**：通过一个提示模板（包含原查询、最优轨迹和未探索准则列表），让教师模型“修改轨迹，使之满足未满足的准则”。
    -   **局部化前向KL蒸馏**：
        -   仅在**组内最高优势的轨迹**上进行蒸馏，因为该轨迹已经满足大部分准则，修改更可靠。
        -   计算每个标记的前向KL散度，只选择累积KL贡献达到阈值γ（默认0.95）的最少标记集**T<sup>u</sup>** 施加损失，从而过滤掉因提示修改产生的噪声信号。
    -   **公式**：`L_OPSD = 1/|T^u| * Σ_{t∈T^u} D_KL(sg(πθ(·|x, Cu, y*_<t)) || πθ(·|x, y*_<t))`。

-   **针对受抑制准则：优势翻转**
    -   **构造反事实自教师**：在负优势轨迹上，通过提示模板要求教师“修改或删除那些满足受抑制准则的部分”。
    -   **精确定位关键标记**：选择同时满足两个条件的标记位置：
        1.  原始标记被反事实教师**降低概率**。
        2.  反事实教师**强烈偏好另一个标记**（概率超过原标记的α倍）。
    -   **局部优势翻转为正**：将定位到的关键标记的原始负优势**替换为一个小的正值τ<sub>flip</sub>**（默认0.1），其余标记保持原样。
    -   **结果**：GRPO在更新时，只在关键标记处获得正向优化信号，避免了抑制整个轨迹。

### 训练目标与流程

-   总损失：`L_CriPO = L_GRPO(使用修正后的优势) + β * L_OPSD`。
-   **算法流程**：采样轨迹 → 评价获取奖励和优势 → 识别未探索和受抑制准则 → 对未探索准则施加局部前向KL损失 → 对受抑制准则定位并翻转标记优势 → 计算总损失更新策略。

## 3. 实验设计

-   **数据集与场景**：
    -   **训练域**：RaR-Medicine（医学）、RaR-Science（科学）。
    -   **域内测试**：各自数据集的测试集。
    -   **跨域测试**：HealthBench、LLMEval-Med（医学），ResearchQA（科学）。
    -   **额外OOD测试**：IFEval、IFBench、MulDimIF（指令遵循）。
-   **Benchmark**：模型使用了**Qwen3-32B**作为训练时的评分法官，使用**GPT-4o-mini**作为评估时的法官（附录中也使用Qwen3-32B验证鲁棒性）。
-   **对比方法**：
    -   **基础模型**：Qwen3-1.7B、Qwen3-4B。
    -   **基线方法**：
        -   **GRPO**：标准基于组相对优势的策略优化。
        -   **HeRL**：一种利用失败轨迹反馈增强探索的基于评分准则的RL方法。
        -   **OPSD**：单独使用同策略自蒸馏。
    -   **本方法及变体**：
        -   **CriPO**：完整方法。
        -   **CriPO-U**：仅处理未探索准则。
        -   **CriPO-S**：仅处理受抑制准则。

## 4. 资源与算力

-   **说明**：论文及附录中**未明确提供具体使用的GPU型号、数量和总训练时长**等算力信息。
-   **可推断信息**：
    -   模型规模包括1.7B和4B参数。
    -   训练使用了分布式框架（verl），8个rollout/组，batch size 64，训练200步。
    -   论文提供了一个**相对效率对比**：CriPO达到GRPO最佳性能时，训练步数约**减少2倍**，但在绝对算力消耗上无具体数字。

## 5. 实验数量与充分性

-   **实验数量**：较全面，主要包括：
    -   **主实验结果**：报告了2种模型 × 4个测试集 × 5种方法的结果（表1），覆盖医学/科学、域内/跨域。
    -   **效率实验**：展示了不同训练步数的奖励曲线和不同方法的训练时间（图5）。
    -   **训练动态分析**：对比了奖励、熵、生成长度（图6）。
    -   **准则统计**：统计了未探索/受抑制准则的比例和数量（图7）。
    -   **消融实验**：对CriPO-U和CriPO-S的关键设计（标记过滤、最优轨迹选择、随机定位）进行了消融（表2）。
    -   **鲁棒性验证**：切换评估法官（Qwen3-32B）验证结论一致性（表4，附录）。
    -   **OOD评估**：在指令遵循基准上进行测试（表3，附录）。
-   **充分性与公平性**：
    -   **充分**：实验设计系统，覆盖了论文宣称的两大场景，并在不同模型规模、不同评估器和不同解析上进行验证。
    -   **客观公平**：
        -   虽然对比了多个基线，但**未与更多同类方法（如RuscaRL可能存在的其他变体）进行对比**。
        -   关于算力资源的缺失是评价实验公平性时的一个不足。
        -   整体上，实验设计清晰，消融实验有力地支撑了每个设计模块的作用。

## 6. 论文的主要结论与发现

-   **方法有效性**：CriPO在几乎所有基准测试上，均以较大优势**优于GRPO和HeRL**。例如，在Qwen3-1.7B上相比GRPO平均提升**+3.2**分。
-   **效率提升**：CriPO能以**约2倍更少的优化步骤**达到GRPO的最终收敛性能，并进一步获得更高性能。
-   **变体分析**：
    -   CriPO-U主要在减少未探索准则上有效，并带来更高的策略熵（探索多样性）。
    -   CriPO-S主要在减少受抑制准则上有效，并鼓励产生更长的回答以保留有价值的行为。
    -   完整CriPO结合两者优势，取得最佳综合性能。
-   **失败模式普遍性**：未探索和受抑制准则在标准GRPO训练中**非常普遍且持续存在**（图1，图7）。
-   **局部化修正的必要性**：消融实验证明，对全标记进行OPSD（如单独OPSD基线）会导致**性能退化**，而贡献引导的标记过滤和反事实定位的局部修正是成功的关键。

## 7. 优点

-   **问题定义清晰**：首次系统性地提出并量化了“受抑制准则”这一关键但此前被忽视的失败模式，问题定义深刻。
-   **方法精巧且有效**：
    -   **无训练-推理不匹配**：通过**自蒸馏**，在“在线”（on-policy）轨迹上提供修正信号，从根本上避免了外部指导法造成的分布偏移。
    -   **局部化处理**：不是对整个轨迹施加强信号，而是精确定位到**极少数关键标记**进行修正（通过贡献引导过滤/反事实定位），提高了效率并减少了噪声。
    -   **对两种失败模式分别设计处理策略**：探索式、生成式（前向KL注入行为） vs. 保留式、修正式（翻转优势），针对性强。
-   **效率与效果双赢**：虽然额外计算量，但由于信息更精准，优化速度显著提升（2×步骤减少），最终性能也更高。
-   **实验系统且深入**：从主结果、效率到消融、案例和鲁棒性，实验设计完整，逻辑严谨。

## 8. 不足与局限

-   **实验场景局限**：主要聚焦于医学和科学QA场景。不能保证在其他开放式任务（如长篇写作、通用对话、创意生成）中同样有效，可能对这些任务的处理方式不同。
-   **对通用能力的影响不够全面**：OOD指令遵循实验（表3）显示，CriPO在部分基准上有小幅波动（正负变化），说明其提升可能**以轻微牺牲部分通用能力为代价**（虽然总体上可接受，但未进行更系统的通用能力测试）。
-   **对法官模型依赖**：方法依赖于一个外部评分法官模型（Qwen3-32B或GPT-4o-mini）来提供准则评分，这引入了法官自身的偏差和计算开销。方法的鲁棒性受法官质量影响。
-   **未提供算力/能耗细节**：虽然提供了效率对比，但**未披露绝对算力需求**（GPU型号、运行时间），使得资源评估有难度，对复现和工业应用的预算估算构成困难。
-   **参数敏感性**：方法引入了几个新超参数（γ=0.95，β=0.05/0.03，α=0.1，τ_flip=0.1），其敏感性未经过深入探讨，不同任务可能需重新调整。
-   **未与更多最新方法对比**：仅与GRPO和HeRL对比，未与近期其他基于奖励或自蒸馏的RL方法（例如直接偏好优化结合自蒸馏的变体）进行比较，比较范围可更广泛。

（完）
