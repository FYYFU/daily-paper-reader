---
title: "PHF: Privileged Hidden Flow for On-Policy Self-Distillation"
title_zh: "PHF: 在线策略自我蒸馏中的特权隐式流"
authors: "Yuhan Li, Mingxu Zhang, Dazhong Shen, Ying Sun"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29340v1"
tags: ["query:opd"]
score: 10.0
evidence: 具有特权隐藏流的在线策略自蒸馏
tldr: "现有on-policy self-distillation仅监督输出分布，无法直接指导内部计算。本文提出Privileged Hidden Flow，额外蒸馏特权教师隐藏状态的转移方向和轨迹几何，而非逐点匹配。在Qwen3-1.7B、4B和8B上，相同100步训练后Average@12分别提升+2.2、+1.5、+1.7。该方法对共享偏移和正交变换保持不变性，消融实验验证了紧凑设计。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29340v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1873, \"height\": 1154}, {\"url\": \"assets/figures/arxiv/2606.29340v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1901, \"height\": 565}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29340v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1343, \"height\": 729}, {\"url\": \"assets/tables/arxiv/2606.29340v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 886, \"height\": 524}, {\"url\": \"assets/tables/arxiv/2606.29340v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 846, \"height\": 797}, {\"url\": \"assets/tables/arxiv/2606.29340v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 843, \"height\": 901}]"
motivation: 现有on-policy self-distillation仅监督输出分布，忽略了对内部隐藏状态计算过程的指导。
method: Privileged Hidden Flow通过对齐token间转移方向和轨迹几何，蒸馏特权教师的隐藏状态运动，而非点对点匹配。
result: "在Qwen3-1.7B/4B/8B上，相同100步训练Average@12提升+2.2/+1.5/+1.7。"
conclusion: PHF以紧凑的隐藏流扩展提升了OPSD性能，且对轨迹偏移和正交变换具有不变性。
---

## 摘要
在线策略自我蒸馏（OPSD）通过匹配一个也能看到已验证参考解的特权教师，在其自身策略生成的轨迹上训练推理模型。现有OPSD目标仅监督输出分布，因此特权上下文通过令牌级散度影响训练，而不直接监督产生该分布的内部计算。我们提出特权隐式流（PHF），它额外蒸馏了特权教师隐藏状态沿相同轨迹移动的方式。PHF并非强制每个学生隐藏向量与同一令牌位置的教师向量匹配，而是在选定的生成位置上对齐令牌到令牌的转移方向和轨迹几何。全层方案还包括从这些相同转移中计算的相邻层关系，而不进行逐点隐藏状态模仿。在相同的100步训练计划下，PHF在我们复现的OPSD基线上，针对Qwen3-1.7B、4B和8B模型，将Average@12聚合指标分别提高了约+2.2、+1.5和+1.7个百分点。传输目标对共享轨迹偏移完全不变；其局部几何项对转移方向的正交变换也不变。消融实验将固定的PHF方案与逐点隐藏状态匹配、单通道转移损失和层子集选择区分开来，支持PHF作为OPSD的紧凑隐式流扩展。

## Abstract
On-policy self-distillation (OPSD) trains a reasoning model on rollouts sampled from its own policy by matching a privileged teacher that also sees verified reference solutions. Existing OPSD objectives supervise only the output distribution, so privileged context affects training through a token-level divergence without directly supervising the internal computation that produced that distribution. We propose Privileged Hidden Flow (PHF), which additionally distills how a privileged teacher's hidden states move along the same rollout. Rather than forcing each student hidden vector to match the teacher vector at the same token position, PHF aligns token-to-token transition directions and trajectory geometry over selected generated positions. The all-layer recipe also includes an adjacent-layer relation computed from these same transitions, without pointwise hidden-state imitation. Under the same 100-step training schedule, PHF improves the Average@12 aggregate over our reproduced OPSD baseline on Qwen3-1.7B, 4B, and 8B, with observed gains of about +2.2, +1.5, and +1.7 points. The transport objective is exactly invariant to shared trajectory offsets; its local geometry term is also invariant to orthogonal transformations of transition directions. Ablations distinguish the fixed PHF recipe from pointwise hidden-state matching, single-channel transition losses, and layer-subset choices, supporting PHF as a compact hidden-flow extension to OPSD.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有在线策略自我蒸馏（OPSD）方法仅通过输出分布（token-level divergence）来利用特权上下文（如参考解法），忽略了特权教师内部隐藏状态的计算过程。这导致监督信息不完整，学生模型无法从更本质的表示运动模式中学习。
- **背景**：在数学推理任务中，OPSD通过让模型在自身策略采样的轨迹上匹配一个“特权教师”（可访问已验证参考解）来训练。但输出级监督只能改变下一词元的概率分布，无法直接约束产生该分布的隐藏层动态。已有过程级监督（如step-level reward）关注输出步而非内部表示。
- **动机**：提出一种能够蒸馏特权教师隐藏状态“过程”的方法，即不仅告诉学生“该输出什么词元”，还告诉学生“内部表示应如何沿轨迹移动”，以更充分地利用特权信息。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：在学生模型采样出的同一段轨迹上，额外对齐学生与特权教师的**隐藏状态转移方向**和**轨迹几何关系**，而非要求逐点隐藏状态匹配。该方法称为**特权隐式流（Privileged Hidden Flow, PHF）**。
- **关键技术细节**：
  1. **转移提取**：在模型每一层 ℓ，选取轨迹上的一组位置 W（最多128个），计算相邻选中位置的残差流隐层差：  
     ΔhS,Tℓ,i = hS,Tℓ,w_{i+1} - hS,Tℓ,wi，再归一化为单位方向 dS,Tℓ,i。
  2. **方向损失 Ldir(ℓ)**：余弦相似度损失，对齐学生与教师的转移方向：  
     Ldir(ℓ) = 1/(m-1) Σ (1 - ⟨dSℓ,i, dTℓ,i⟩)。
  3. **几何损失 Lgeo(ℓ)**：匹配转移方向之间的Gram矩阵（关系结构），即哪些转移平行、正交、相反：  
     Lgeo(ℓ) = 1/(m-1)² × ||GSℓ - GTℓ||²_F，其中 G = D D^T。
  4. **层级聚合**：先对各层求局部损失 Llayer(ℓ)=½(Ldir+Lgeo)；再引入**相邻层关系损失 Ladj**：匹配相邻层之间转移方向的关系矩阵（CS,Tℓ,ℓ+1 = Dℓ (Dℓ+1)^T）。最终隐藏流损失为：  
     LFlow = ½ Llocal + ½ Ladj，其中 Llocal = 1/L Σ Llayer(ℓ)。
  5. **整体目标**：LPHF = LOPSD + α LFlow，α=0.05 固定，不按模型调参。
  6. **特权教师来源**：使用EMA更新（ρ=0.999）的在线模型副本，在参考解和相同轨迹前缀上运行。
  7. **不变性**：提出的转移损失对轨迹公共偏移、正缩放（归一化后）和几何项的独立正交变换具有不变性，逐点隐藏状态损失缺乏这些性质。

### 3. 实验设计：数据集、基准、对比方法
- **数据集与场景**：三个竞赛数学数据集：AIME 2024、AIME 2025、HMMT 2025（各30题）。评估采用**Average@12**（12个样本的平均正确率）。训练数据为OPSD官方数学集（29,434例）。
- **模型与基准**：Qwen3-1.7B、4B、8B。主对比基线为经复现的**OPSD**（输出级蒸馏，相同100步训练）。上下文行包括 Base（未训练）、SFT（监督微调）、GRPO（策略优化），均在同一评估协议下。
- **对比方法**：PHF vs OPSD（控制变量：相同步数、LoRA配置、评估协议）。消融实验包括：方向仅、几何仅、选定层（手动选层子集）等变体。

### 4. 资源与算力
- **硬件**：单节点 8×NVIDIA H20 GPU（每卡143 GB显存）。
- **总计算量**：主实验结果与消融实验合计约 **600 GPU小时**。
- **训练细节**：所有实验使用LoRA（rank 64/α 128），有效batch 32，学习率5e-6（余弦退火），共100个优化步。PHF额外进行一次特权前向传播并存储128个位置的隐藏状态。

### 5. 实验数量与充分性
- **主要结果**：Table 1 报告了三个模型规模（1.7B/4B/8B）的OPSD vs PHF对比，覆盖三个基准数据集。PHF在所有规模上聚合Average@12提升分别为+2.2、+1.5、+1.7。
- **消融实验**：Table 2 在全部三规模上测试了方向仅、几何仅、选定层三种变体，显示完整配方最优。
- **动态分析**：Figure 2 提供了AIME 2024上的checkpoint轨迹曲线，验证PHF稳定性；并报告了各层能量分布，说明全层聚合的合理性。
- **充分性评价**：实验覆盖三个模型尺度和三种主要变体，控制变量严格（相同输出目标、相同训练预算），对比公平。但仅测试一个模型家族（Qwen3）和数学推理任务；检查点固定为第100步，未探索更长训练或最佳检查点。实验数量相对集中，但针对所提方法足够充分。

### 6. 论文的主要结论与发现
- PHF在OPSD输出级蒸馏的基础上，添加隐藏转移过程目标，在不改变采样流程、奖励模型或额外搜索的情况下，持续提升数学推理Average@12。
- 方向-几何组合优于单一组件，全层聚合优于选定层子集。
- 所提损失对常见表示漂移（偏移、缩放、旋转）具有天然不变性，这一特性使PHF比逐点隐藏匹配更稳定。
- 主发现：特权信息不仅可以通过输出分布传递，还可以通过隐藏状态的移动过程来蒸馏，从而更高效地利用训练时的验证参考。

### 7. 优点：方法或实验设计亮点
- **方法创新**：首次在在线策略自我蒸馏中引入隐藏状态转移方向和轨迹几何的匹配，强调“过程”而非“位置”监督。
- **实用性强**：不增加推理阶段开销，训练仅需一次额外前向和隐藏存储，α固定、窗口固定，无需调参。
- **理论支持**：提供转移损失关于偏移和正交变换的不变性证明，解释其为何优于逐点匹配。
- **实验设计严谨**：控制变量严格（OPSD与PHF同训练协议）、跨尺度验证（三个模型规模）、消融全面覆盖组件贡献。

### 8. 不足与局限
- **依赖特权信息**：训练需要已验证参考解，无法用于无标签或弱监督场景。
- **内存开销**：需存储128个位置的隐藏状态，更长窗口或更多层将增加显存需求。
- **实验覆盖有限**：仅测试Qwen3模型家族和数学推理任务；未验证其他模型（如Llama、DeepSeek）或通用下游任务。
- **评估固定**：仅报告第100步的结果，未探索更优检查点或训练稳定性；Average@12只衡量平均正确率，未分析推理质量、鲁棒性或错误恢复。
- **未放缩窗口**：128位置窗口是固定选择，未调研自适应选择或更大窗口的影响。
- **潜在偏差**：特权教师使用EMA与OPSD中的平均机制相同，但未证明隐藏目标携带独立于输出分布的新信息（虽然文中给出了耦合分析）。

（完）
