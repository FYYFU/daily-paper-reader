---
title: "CompactionRL: Reinforcement Learning with Context Compaction for Long-Horizon Agents"
title_zh: "CompactionRL: 基于上下文压缩的强化学习用于长程智能体"
authors: "Yujiang Li, Zhenyu Hou, Yi Jing, Jie Tang, Yuxiao Dong"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05378v1"
tags: ["query:l-context-rl"]
score: 9.0
evidence: 用于长程智能体的上下文压缩强化学习
tldr: 长时域智能体受有限上下文窗口限制，任务轨迹易因超长而中断。本文提出CompactionRL，通过强化学习联合优化任务执行与摘要生成，采用token级损失归一化和跨轨迹广义优势估计训练模型。在SWE-bench Verified和Terminal-Bench 2.0上，CompactionRL使开源模型GLM-4.5-Air分别提升7.0和3.1个点，并成功用于训练GLM-5.2模型。该方法为长时域强化学习提供了有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05378v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1387, \"height\": 543}, {\"url\": \"assets/figures/arxiv/2607.05378v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 710}, {\"url\": \"assets/figures/arxiv/2607.05378v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1416, \"height\": 724}, {\"url\": \"assets/figures/arxiv/2607.05378v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1421, \"height\": 414}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05378v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1280, \"height\": 225}, {\"url\": \"assets/tables/arxiv/2607.05378v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1459, \"height\": 900}, {\"url\": \"assets/tables/arxiv/2607.05378v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1441, \"height\": 757}]"
motivation: 长时域智能体因有限上下文窗口导致任务未完成即中断，现有上下文压缩方法未与强化学习结合。
method: 联合优化任务执行与摘要生成，通过token级损失归一化和跨轨迹GAE进行强化学习训练。
result: 在SWE-bench Verified和Terminal-Bench 2.0上，GLM-4.5-Air分别提升7.0和3.1个点，并用于训练GLM-5.2模型。
conclusion: 首次将上下文压缩嵌入强化学习，有效扩展了长时域智能体能力，为开源模型长时域训练提供新范式。
---

## 摘要
长程智能体LLM越来越受到有限上下文窗口的限制，因为扩展的交互轨迹可能超过任务完成前的最大上下文长度。上下文压缩通过总结先前的交互状态并在压缩上下文中继续展开提供了一种自然的解决方案，但将压缩整合到强化学习中仍未被充分探索。我们提出CompactionRL，一种训练长程智能体LLM进行上下文压缩的强化学习策略。我们的方法通过令牌级损失归一化和跨轨迹广义优势估计联合优化任务执行和摘要生成。这种设计使LLM智能体能够从压缩的长程轨迹中学习。我们在开放模型之上训练CompactionRL，并在代理编码任务上观察到一致的性能提升。CompactionRL使开放的GLM-4.5-Air模型（106B-A30B）在SWE-bench Verified上达到66.8%的Pass@1分数，在Terminal-Bench 2.0上达到24.5%，绝对增益分别为7.0和3.1个百分点。基于GLM-4.7-Flash（30B-A3B），CompactionRL将Pass@1提高了5.5和6.8个百分点，分别达到SWE-bench Verified的56.0%和Terminal-Bench 2.0的20.2%。因此，CompactionRL被部署在用于训练开放GLM-5.2模型（750B-A40B）的强化学习流程中。

## Abstract
Long-horizon agentic LLMs are increasingly limited by finite context windows, as extended interaction trajectories can exceed the maximum context length before a task is completed. Context compaction offers a natural solution by summarizing previous interaction states and continuing the rollout under a compressed context, but incorporating compaction into reinforcement learning remains underexplored. We propose CompactionRL, a reinforcement learning strategy to train long-horizon agentic LLMs with context compaction. Our approach jointly optimizes task execution and summary generation with token-level loss normalization and cross-trajectory generalized advantage estimation. This design enables the LLM agents to learn from compacted long-horizon trajectories. We train CompactionRL on top of open models and observe consistent performance gains on agentic coding tasks. CompactionRL enables the open GLM-4.5-Air model (106B-A30B) to achieve Pass@1 scores of 66.8% on SWE-bench Verified and 24.5% on Terminal-Bench 2.0, with absolute gains of 7.0 and 3.1 points, respectively. Built upon GLM-4.7-Flash (30B-A3B), CompactionRL improves Pass@1 by 5.5 and 6.8 points, reaching 56.0% on SWE-bench Verified and 20.2% on Terminal-Bench 2.0, respectively. CompactionRL is thus deployed in the RL pipeline for training the open GLM-5.2 model (750B-A40B).

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义

- **研究动机**：长程智能体（Long-horizon agents）在执行交互任务时，积累的大量历史轨迹（推理、工具调用、环境反馈等）容易超出LLM有限上下文窗口，导致任务中断。现有方案依赖更长的上下文窗口或推断时的启发式压缩，但未与强化学习训练结合，无法优化压缩过程本身。
- **整体含义**：论文主张将上下文压缩（Context Compaction）作为强化学习训练的一部分，使模型不仅能执行任务，还能自主生成高质量摘要，从而在固定上下文预算下有效扩展有效训练和推理时长，提升长程任务成功率。

## 2. 方法论

- **核心思想**：在PPO框架下，当剩余上下文小于阈值时，触发策略生成摘要并重建上下文（保留最近k步），然后继续交互。所有执行和摘要令牌均在同一最终任务奖励下优化。
- **关键技术细节**：
  - **压缩触发**：当 `C - |ht| < Tcomp` 时，追加摘要指令 `qsum`，采样摘要 `St`，重建上下文 `¯ht = (s) ⊕ uresume(St) ⊕ (zt-k+1,…,zt)`，默认k=2。
  - **训练分段**：一次展开自然分成多个片段（执行段/摘要段），所有片段共享最终任务奖励，不引入额外的摘要质量奖励。
  - **Token级损失归一化**：解决片段长度和数量不均导致长度偏差的问题。对所有片段中所有优化令牌的PPO损失取平均，公式为 `Lπ = -1/|M| Σ min(ρs,i(θ)Âs,i, clip(...))`。
  - **跨轨迹广义优势估计（Cross-Trajectory GAE）**：校正片段独立计算GAE时对早期片段的过度信用分配。对片段σs中第i个令牌，其优势 `Âs,i = (γλ)^{N>s} A^loc_{s,i}`，其中N>s是后续片段的总令牌数，使折扣与真实轨迹距离匹配。
- **算法流程**：
  1. 初始化策略πθ和值函数Vφ，进行价值预训练。
  2. 对每个prompt，在固定上下文预算内展开rollout，当达到压缩阈值时生成摘要并重建上下文，继续交互直到任务完成或达到最大步数。
  3. 收集所有片段（执行段和摘要段）的令牌及最终任务奖励。
  4. 计算跨轨迹GAE，得到每个令牌的Â和返回估计。
  5. 使用token级PPO损失更新策略，使用价值回归损失更新值函数。

## 3. 实验设计

- **数据集与benchmark**：
  - SWE-bench Verified：从完整benchmark中随机采样200个实例，评估软件工程任务。
  - Terminal-Bench 2.0：完整测试集，评估终端命令行任务。
- **对比方法**：
  - 基础模型：GLM-4.7-Flash (30B-A3B) 和 GLM-4.5-Air (106B-A30B)，以及它们的SFT版本。
  - 标准PPO（不含压缩）在相同及扩展上下文窗口下训练。
  - CompactionRL的变体：不训练摘要段（mask掉摘要损失）；移除token级损失或跨轨迹GAE。
  - 公开基线：GPT-5 mini, Qwen3-Coder系列, gpt-oss-120b等（仅参考，因agent scaffold可能不同）。
- **评估设置**：Harbor环境 + Terminus-KIRA scaffold。两种评估模式：Single(×1)（不压缩，单窗口）和Compacted(×4)（允许最多3次压缩，有效4倍窗口）。报告Pass@1准确率，两次运行取平均。

## 4. 资源与算力

- 文中未明确给出GPU型号、数量或训练总时长。
- 提到了训练环境：使用开源框架slime进行RL训练，全局batch size 128，group size 1，学习率2e-6（策略）和3e-6（价值函数），训练步骤数据未明确，但从图4展示的训练动力学看，约80步后稳定。
- 算力规模可推测为中等（因模型大小30B/106B），但具体硬件信息缺失。

## 5. 实验数量与充分性

- **主要实验**：表2展示了两类模型在两种benchmark上的对比（包括无压缩RL、CompactionRL、公开基线）。
- **消融实验**：
  - 表3：训练是否包含摘要损失、不同上下文窗口（64k、128k、80k、160k）的影响。
  - 表4：移除token级损失归一化或跨轨迹GAE的效果。
  - 表1：固定执行模型，改变摘要模型（Qwen3.5-27B等），验证摘要质量对性能的影响（6.5点差距）。
- **分析实验**：图3分析压缩次数、工具调用次数、压缩触发任务的准确率；图4展示训练过程中摘要长度、推理令牌、熵的变化。
- **公平性**：评估采用相同的scaffold和评估设置，对比公开基线时指出scaffold可能不同，标注“仅参考”，保持诚实。
- **充分性**：覆盖多种模型规模、多种消融、多种分析和敏感性研究，整体实验充分。但benchmark仅限于编码/终端任务，未扩展到其他agent场景（如web交互、游戏等），外部有效性有待验证。

## 6. 主要结论与发现

- **性能提升**：CompactionRL在固定峰值窗口下，显著超越基础模型和无压缩RL，且可比拟甚至超越长窗口训练的结果。例如GLM-4.5-Air在SWE-bench Verified上达到66.8%（+7.0），在Terminal-Bench 2.0上达到24.5%（+3.1）。
- **摘要训练收益**：包含摘要损失的完整版本性能最佳，且产生更长更详细的摘要（图4a），有助于保留关键信息。
- **训练行为**：CompactionRL导致更少的压缩次数和工具调用，说明更高效的交互；同时增加推理令牌和更稳定的熵（图4b,c）。
- **可靠性**：方法已部署于训练GLM-5.2（750B-A40B）的RL pipeline，表明可扩展性。

## 7. 优点

- **方法创新**：首次将上下文压缩系统性地嵌入RL训练，而非作为推理技巧，使摘要也成为可学习部分。
- **技术贡献**：提出token级损失归一化解决片段长度不均；提出跨轨迹GAE保持时序信用分配，两处设计简洁有效。
- **实验扎实**：在主实验之外做了详尽的消融和分析，清晰展示了各组件贡献；对比了不同模型规模和多种基线，结果一致。
- **实际价值**：在开源模型上取得有意义的提升，并应用到更大模型训练，具有工业实用性。
- **开放透明**：使用开源框架（slime），便于复现。

## 8. 不足与局限

- **单窗口评估不一致**：CompactionRL在禁止压缩的Single (×1)评估中表现不一定优于基线，存在训练-测试不匹配，限制了其在不支持压缩环境下的直接使用。
- **信用分配近似**：跨轨迹GAE仍是对完整轨迹信用分配的近似，尤其多次压缩时，早期摘要的长远影响可能无法完全捕捉。
- **领域局限**：实验仅覆盖代码导向的长程任务（SWE-bench, Terminal-Bench），是否能推广到web交互、机器人控制等不同观察结构和奖励信号的任务尚不清楚。
- **未报告训练成本**：缺少详细的GPU型号、数量和训练时间，影响可复现性和成本评估。
- **摘要质量评估**：未采用人工或自动摘要质量指标，仅通过下游任务性能间接衡量，可能忽略摘要保真度的其他方面。

（完）
