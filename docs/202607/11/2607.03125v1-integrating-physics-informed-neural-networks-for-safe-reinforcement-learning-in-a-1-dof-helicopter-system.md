---
title: Integrating Physics-Informed Neural Networks for Safe Reinforcement Learning in a 1-DoF Helicopter System
title_zh: 将物理信息神经网络集成到单自由度直升机系统的安全强化学习中
authors: "Georg Schäfer, Jakob Rehrl, Stefan Huber"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03125v1"
tags: ["query:ai-safety"]
score: 9.0
evidence: 使用物理信息约束的安全强化学习
tldr: 深度强化学习在工业应用中因黑盒探索易违反安全约束，传统方法依赖复杂奖励整形。本文提出在PPO损失中嵌入可微物理模型，通过模拟短时未来轨迹提前惩罚安全违规。在1自由度直升机仿真中，该方法大幅降低约束违规次数，同时保持目标跟踪性能。贡献在于将物理先验直接融入策略优化，无需额外奖励工程。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03125v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1194, \"height\": 273, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03125v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1233, \"height\": 453, \"label\": \"Figure\"}]"
motivation: 解决深度强化学习黑盒探索导致的安全约束违规问题，避免复杂奖励整形。
method: 在PPO actor损失函数中嵌入可微物理模型，训练时模拟短时未来轨迹，对预期违规施加软正则惩罚。
result: 在1自由度直升机仿真中，物理信息正则化显著减少俯仰约束违规，同时保持可靠的目标跟踪。
conclusion: 嵌入可微物理模型到策略优化中可有效保障安全，且不依赖任务奖励信号，为工业安全强化学习提供新途径。
---

## 摘要
深度强化学习为工业信息物理系统提供了强大的控制能力，但其“黑箱”探索过程可能会违反严格的硬件安全限制。通常，这些约束通过复杂的奖励塑形来管理。在这项进行中的工作中，我们将可微分的物理模型直接嵌入到近端策略优化（PPO）的actor损失函数中。通过在训练过程中模拟短期未来轨迹，该策略会因预期的安全违规行为而受到惩罚，且独立于任务奖励信号。在具有严格俯仰约束的模拟单自由度直升机测试平台上评估，我们的物理信息软正则化方法在保持可靠目标跟踪的同时，显著减少了约束违规。

## Abstract
Deep reinforcement learning (DRL) offers powerful control for industrial cyber-physical systems (ICPSs), but its "black-box" exploration risks violating strict hardware safety limits. Typically, these constraints are managed through complex reward shaping. In this work-in-progress paper, we embed a differentiable physics model directly into the proximal policy optimization (PPO) actor loss function. By simulating short-horizon future trajectories during training, the policy is penalized for anticipated safety violations independent of the task-reward signal. Evaluated on a simulated 1-degree-of-freedom helicopter testbed with strict pitch constraints, our physics-informed soft regularizations substantially reduce constraint violations while maintaining reliable target tracking.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：深度强化学习（DRL）在工业信息物理系统（ICPS）中具有强大控制能力，但其“黑箱”探索策略可能违反硬件安全限制（如机械系统的物理极限）。传统方法通过复杂的奖励塑形来管理安全约束，但这种方法脆弱、难以调优，且与任务奖励耦合紧密。
- **整体含义**：本文旨在提出一种可扩展的、解耦于任务奖励的软安全正则化机制，将可微物理模型直接嵌入策略优化损失函数中，使智能体在训练过程中能够“预见”未来安全违规并提前规避，从而在不降低任务性能的前提下提升安全性。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：在近端策略优化（PPO）的actor损失函数中嵌入一个可微分的物理模型（基于系统动力学微分方程），通过短视预测（short-horizon）模拟未来轨迹，对预期安全违规施加软惩罚，且惩罚信号独立于任务奖励。
- **关键技术细节**：
  - **基础算法**：PPO（连续动作空间）。
  - **物理模型**：使用已知的微分方程描述Quanser Aero 2直升机系统的1自由度动力学，状态包括俯仰角θ、角速度、电机角速度ω₀, ω₁；连续时间动力学表示为ẋ = f(x, u)。
  - **预测机制**：使用**4阶Runge-Kutta（RK4）** 数值积分，每环境步用5个微步，预测未来0.3秒（3步，10 Hz采样率）。
  - **训练时信息流**：在训练时，智能体在当前环境状态st下产生动作，该动作同时作用于环境（获得任务奖励）和可微物理模型（获得未来轨迹预测）。物理模型基于当前物理状态xt（从仿真器提取的“特权信息”）进行前向模拟。预测的俯仰角序列θ^{(h)}用来计算安全损失。
  - **安全损失定义**（公式1）：
    \[
    L_{\text{Safety}} = \frac{1}{H} \sum_{h=1}^{H} \max\left(0, \frac{|\theta^{(h)}|}{\theta_{\max}} - 1\right)
    \]
    其中θ_max = 30°，H=3。该损失为软正则化项，仅当预测角度超过阈值时激活惩罚，且完全可微。
  - **总损失**：L = L_PPO + λ·L_Safety，其中λ为权重因子。通过链式法则，梯度可从安全违规反向传播至actor网络。
- **计算图**：RK4积分由可追踪的tensor操作构建，因此整个预测过程保持可微，实现端到端梯度传递。

## 3. 实验设计：使用的数据集/场景、基准、对比方法

- **任务场景**：模拟Quanser Aero 2直升机系统的1自由度俯仰控制，要求跟踪目标信号（±40°），但人为设置安全俯仰限值为±30°（矛盾设置以检验算法能力）。
- **基准（baseline）**：标准PPO（λ=0），称为Naive Baseline。
- **对比方法**：
  - **PINN Balanced**（λ=150）：平衡的安全惩罚权重。
  - **PINN Over-penalized**（λ=1000）：过强的安全惩罚。
- **评估指标**：约束违规次数（实际俯仰角超过±30°的程度）、目标跟踪性能（动作/轨迹响应）。
- **环境访问**：通过Python-Simulink接口（Gym API）访问模拟器，动力学模型基于先前研究[7]。

## 4. 资源与算力

- **文中未明确说明**：论文未提及所使用的GPU型号、数量、训练时长等具体算力信息。仅说明训练使用了10个随机种子进行实验，但未报告每个种子的训练轮数或时间。
- **推测**：由于是工作进展论文且模型规模较小（1自由度+简单PPO），算力需求应不高，可能单GPU即可完成。

## 5. 实验数量与充分性

- **实验组数**：共3个配置（Naive Baseline, PINN Balanced, PINN Over-penalized），每个配置运行10个随机种子（共30次实验）。
- **充分性**：
  - **积极方面**：使用了多个随机种子，考虑了统计显著性；设置了极端矛盾条件（目标±40° vs 限制±30°）以凸显算法能力。
  - **不足**：
    - 未进行λ参数的帕累托扫描（论文说明“λ选择是经验性的，严格的统计评估保留给未来工作”）。
    - 仅在一个场景（1自由度直升机，单一安全限值）上测试，缺乏泛化性验证。
    - 未与现有其他安全强化学习方法（如CMDP、CBF、MPC投影）进行直接比较。
- **公平性**：由于所有配置共享相同的PPO超参数和网络结构，仅改变λ，控制变量较好。

## 6. 论文的主要结论与发现

- **主要发现**：
  - 嵌入可微物理模型进行软安全正则化是有效的：**PINN Balanced**配置大幅减少了安全违规，同时保持了良好的目标跟踪性能，仅在极端设定点突变时出现短暂瞬态违规。
  - 惩罚权重λ的选择至关重要：λ=0时完全忽视安全（严重违规）；λ=1000时过度保守（跟踪迟缓）；λ=150实现最佳平衡。
  - 所有配置均成功收敛，但高λ设置下价值损失自然增加（因目标矛盾），而策略损失相当。
- **主要结论**：该方法提供了一种可扩展的、解耦于任务奖励的软安全约束机制，降低了对复杂奖励塑形的需求，适用于工业信息物理系统。

## 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
  - **直接嵌入物理先验**：将可微物理模型集成到策略优化损失中，无需像CBF或MPC那样在部署时额外计算。
  - **安全与任务解耦**：安全惩罚独立于任务奖励，避免奖励塑形的脆弱性。
  - **软正则化而非硬约束**：允许一定程度的瞬态违规，避免过度保守，在实际中更具柔性。
- **实验设计亮点**：
  - **矛盾目标设置**：故意让目标范围（±40°）超出安全限值（±30°），极端测试算法的安全优先级能力。
  - **多随机种子**：10个种子提供统计可靠性。
  - **分析λ权重影响**：通过三个λ值展示了从忽视安全到过度保守的完整权衡。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅在仿真环境下测试，未进行实机（sim-to-real）验证。
  - 仅测试1自由度系统，泛化性未验证；更复杂的系统（如多自由度或非线性更强）中表现未知。
  - 未与其他安全RL方法（如CMDP、Lagrangian方法、CBF）进行定量比较。
- **偏差风险**：
  - 实验设置中安全限值（30°）比目标（40°）更严格，但缺乏对“安全限值与目标同向”场景的测试（例如目标也在30°以内），可能高估方法价值。
  - 罚权λ的选择经验性，未通过系统调参或自动化方法（如拉格朗日乘子）确定。
- **方法局限**：
  - 依赖精确的物理模型；模型误差可能降低预测准确性，导致安全违规或过度保守。
  - 预测时使用“特权信息”（直接从仿真器提取真实物理状态），在真实系统中不可用（需要状态估计），未讨论观测噪声影响。
  - 训练时额外引入物理模拟，可能增加计算成本，尤其对高维或长时域预测。
- **其他**：论文为“工作进展”，结论尚不完整，许多细节（如网络结构、超参数、训练时间）未报告。

（完）
